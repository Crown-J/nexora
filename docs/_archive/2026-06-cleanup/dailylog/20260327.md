# 2026-03-27 工作日誌

> 以下區塊請依**當日實際工作**填寫；勿將任務說明中的範例文字當成已完成事項。

## 專案資訊
- 專案：NEXORA
- 分支：（當日主要開發分支）
- 開發者：Crown Lin（Hank 執行）

## 今日完成事項
1. **nx-ui 純網頁 PWA（可安裝）+ DAILYLOG 標示**  
   - 檔案：`apps/nx-ui/src/app/manifest.ts`（Web App Manifest；`name`／`description` 含 **DAILYLOG**）、`apps/nx-ui/public/sw.js`（最小 Service Worker）、`apps/nx-ui/public/pwa-icon.svg`（PWA 圖示）、`apps/nx-ui/src/components/pwa-register.tsx`（production 註冊 SW）。  
   - `apps/nx-ui/src/app/layout.tsx`：`viewport.themeColor`、`metadata` 與 `appleWebApp` 標題與 **DAILYLOG** 對齊；掛載 `PwaRegister`。  
   - 說明：安裝條件依瀏覽器而定；HTTPS（或本機）下可透過「安裝應用程式／建立捷徑」使用；**DAILYLOG** 與本目錄 `dailylog/` 工作日誌對應。

2. **登入後首頁改版（對齊 demo/home）**  
   - 參考：`demo/home`（Dock、TopBar、問候／佈告欄／行事曆／備忘錄／任務清單）。  
   - 實作：`apps/nx-ui/src/app/home/page.tsx` 重構為上述版面；`src/components/home/*`（`dock` 連結實際路由 `/home`、`/dashboard/nx01`～`nx04`、`/dashboard`；`top-bar` 串接 `useSessionMe` 顯示名稱／登出／進入 Dashboard）。  
   - 依賴：`framer-motion`、`date-fns`、`react-day-picker`、`@radix-ui/*`、`class-variance-authority`、`clsx`、`tailwind-merge`。  
   - UI 基礎元件：`src/lib/utils.ts`（`cn`）、`src/components/ui/button|badge|card|dropdown-menu|scroll-area|avatar|calendar`。  
   - `globals.css`：併入 `react-day-picker/style.css`；`:root` 色票與 demo/home 深色主題對齊（金黃 primary、sidebar）。

3. **登入 401 修正 + 首頁微調（Logo / Dock / 手機 top-left）**  
   - 登入修正：`apps/nx-ui/src/app/login/page.tsx`，`username` 改回只送「使用者帳號」，不再組 `公司:使用者`，避免後端 `/auth/login` 回 `HTTP 401`。  
   - 品牌 Logo：新增 `apps/nx-ui/src/components/home/brand-planet-logo.tsx`，將 TopBar 黃點改為星球軌道 Logo；`top-bar.tsx` 已套用。  
   - Dock 優化：`apps/nx-ui/src/components/home/dock.tsx` 改為左側隱藏滑出式，加入 edge trigger、Spring 動畫調校、圖示間距拉開。  
   - 手機按鈕：`apps/nx-ui/src/components/home/top-bar.tsx` 移除左上角功能按鈕（Mobile 以底部 Dock 為主）。

4. **首頁再精簡與視覺優化（依最新設計回饋）**  
   - 版面重排：`apps/nx-ui/src/app/home/page.tsx` 移除歡迎區塊，保留置頂通知列；行事曆區塊改為最上方；下方改為「公告 + 未完成訂單」。  
   - 公告：`apps/nx-ui/src/components/home/bulletin-board.tsx` 將「佈告欄」改名「公告」，新增 TAB（全部 / 公司 / 系統）過濾。  
   - 行事曆細節：`apps/nx-ui/src/components/home/calendar-details.tsx` 移除出缺席區塊，僅保留事件清單，新增「建立事件」按鈕。  
   - 日期色彩：`apps/nx-ui/src/components/home/calendar-panel.tsx` 將選中日期調整為黃底橘字（改善可讀性）。  
   - 未完成訂單：`apps/nx-ui/src/components/home/task-list.tsx` 將「任務清單」改為「未完成訂單」，顯示單數與狀態（待報價 / 待出貨 / 待開立發票 / 急件優先）。  
   - Dock + 品牌：`apps/nx-ui/src/components/home/dock.tsx` 新增「主檔」按鈕；`apps/nx-ui/src/components/home/brand-planet-logo.tsx` 改為靜態版本（減少動畫負擔）。
   - 補調整：TopBar 套用 `demo/home` 視覺語言（同款 Logo/排版/通知樣式）；首頁背景改為近登入頁星空感（粒子 + 漸層疊層）；卡片改為白線邊框 + 玻璃光暈。  
   - 行事曆樣式修正：增加 `react-day-picker` 選中態覆蓋（`.home-calendar .rdp-selected .rdp-day_button`），確保「黃底橘字」不被預設藍色樣式覆寫。
   - Dock 觸發器：將左側窄條改為「Logo 按鈕點擊開關」模式，避免雙螢幕滑鼠誤觸造成難以開啟 Dock（檔案：`apps/nx-ui/src/components/home/dock.tsx`）。
   - 進一步改為「可拖拉小行星觸發器」：長按拖曳位置、點擊展開 Dock、點頁面空白處自動收合、hover 發亮；觸發器與 Dock 使用 `z-[9999]` 永遠置頂。
   - 使用者調整：取消拖拉需求，改為固定左側中央的小行星觸發器（保留點擊展開/點空白收合/hover 發亮/永遠置頂）。
   - Dock 置中修正：展開後面板改用 `top-1/2 -translate-y-1/2` 固定真正垂直置中。  
   - TopBar 功能補充：通知右側新增深淺色切換按鈕，並記憶於 `localStorage`（`nx-ui-theme-mode`）；新增 `.light` 主題變數切換。
   - 淺色重配色（首頁 + 登入）：`apps/nx-ui/src/app/globals.css` 重做 `.light` token 為中性淺灰商務風，並新增 `.light .glass-card/.glass-panel` 專屬樣式；首頁背景/粒子與登入卡片在 light 下降噪提對比（`home/page.tsx`、`login/page.tsx`）；登入錯誤訊息字色改為 `text-destructive` 以提升可讀性（`components/login/login-form.tsx`）。
   - 行事曆最終配色：移除黃橘 hardcode，改為深淺色共用的中性灰選中規則（selected/today/hover/outside/disabled 由 `home-calendar` 全域控制），避免 `react-day-picker` 預設藍色殘留（`components/home/calendar-panel.tsx`、`app/globals.css`）。
   - 追加修正：行事曆「今天」框大小改為與日期格一致（覆蓋 day button 寬高與圓角），避免 today 標示過小；TopBar 主題切換由雙態改為三態（深色/淺色/跟隨系統），並保留 `localStorage` 記憶。

5. **家用環境建置錯誤修正（`tw-animate-css` 無法解析）**
   - 錯誤現象：Next.js / Turbopack 於 `apps/nx-ui/src/app/globals.css` 編譯時出現 `Can't resolve 'tw-animate-css'`。
   - 根因：`tw-animate-css` 原本放在 `devDependencies`，在部分安裝情境（如 production-only）可能未被安裝，導致 CSS `@import` 失敗。
   - 修正：`apps/nx-ui/package.json` 將 `tw-animate-css` 移至 `dependencies`，執行 `pnpm install` 後再以 `pnpm --filter nx-ui build` 驗證通過。

6. **家用環境啟動穩定化（`npm run dev` / Turbopack 差異）**
   - 錯誤現象：家用環境以 `npm run dev` 啟動時，持續出現 CSS import 解析失敗（`tw-animate-css` 或 `./tw-animate.css`）。
   - 排查重點：確認 `node_modules` 實際安裝狀態、排除舊的 `next dev` 進程鎖檔（`.next/dev/lock`）、驗證 `npm` 與 `pnpm` 解析差異。
   - 修正：新增 `apps/nx-ui/src/app/tw-animate.css`（本地動畫樣式），`globals.css` 改為本地引入；並在 `apps/nx-ui/next.config.ts` 設定 `turbopack.root` 指向 monorepo root（`C:/nexora`），降低 root 推斷與 lockfile 警告造成的路徑不一致風險。
   - 驗證：`apps/nx-ui` 執行 `npm run build` 可成功完成。

7. **淺色模式首頁背景強化（保留星空粒子氛圍）**
   - 目標：淺色主題維持與深色相同的「粒子 + 光暈」層次，但避免白色粒子在淺底不清楚。
   - 實作：`apps/nx-ui/src/app/home/page.tsx` 將背景光暈層抽為 `home-aurora` class；`apps/nx-ui/src/app/globals.css` 新增 `.light .home-aurora` 配色（天空藍 / 靛藍 / 琥珀）與 `.light .home-stars` 濾鏡調整（提高可見度）。
   - 效果：淺色模式仍有漂浮粒子與多點光暈，視覺語言與深色模式一致，但對比更適合明亮背景。

8. **首頁 TopBar 滿版修正（大螢幕）**
   - 問題：大尺寸螢幕下，TopBar 視覺上未滿版，左側保留區過大。
   - 根因：`apps/nx-ui/src/app/home/page.tsx` 以 `lg:pl-20` 包住 TopBar 與內容區，導致 TopBar 同步被左側 Dock 預留空間擠壓。
   - 修正：將 TopBar 移出左側預留邏輯，改為內容區 `main` 使用 `lg:pl-24`；`apps/nx-ui/src/components/home/top-bar.tsx` 補上 `w-full`，確保 TopBar 在桌機可滿版顯示。

9. **行事曆週起始與版面滿版修正（桌機/手機）**
   - 需求：行事曆星期排序改為「日→一→…→六」，並改善左右留白，讓桌機與手機顯示更滿版。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`。
   - 實作：
     - `Calendar` 新增 `weekStartsOn={0}`，將每週起始日改為週日。
     - 調整 `CardContent` 與 `Calendar` 內距（減少左右 padding）。
     - 覆寫 `classNames.root = "w-full"`，並調整 `month_caption` / `row` 間距，提升可用寬度占比。

10. **行事曆版面二次修正（避免左貼邊與右側留白）**
   - 回饋：上一版在部分畫面出現「內容偏左、右側空白感更重」。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`、`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - `CalendarPanel` 內距改回較平衡設定（`CardContent` 使用 `px-3`）。
     - 移除不相容的 DayPicker 舊版 classNames 覆寫，避免版面計算失真。
     - 在 `globals.css` 對 `.home-calendar` 補強結構：`month_grid/table` 強制 `width:100% + table-layout: fixed`，`weekdays/week` 以 7 欄等分 grid 呈現，確保桌機與手機都平均分欄。

11. **行事曆導航與日期格一致性修正**
   - 回饋：月份左右箭頭位置偏上，未與月份標題同列；`today`（灰）與 `selected`（黃）視覺比例不一致。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`、`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - 導航列改為與月份標題同區塊定位（`month_caption` 增加左右保留，`nav` 固定於標題列兩側並下移）。
     - 日期按鈕統一尺寸（寬高皆 `2.2rem`，置中），避免灰/黃日期框比例不一致。
     - 移除造成跨列拉伸的 grid 版型覆寫，改為固定 7 欄寬度（每欄 `14.2857%`）確保桌機/手機一致。

12. **行事曆最終校正（箭頭同列 + 灰黃日期框等比）**
   - 回饋：箭頭仍偏上、`today` 與 `selected` 日期框比例仍不一致。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`、`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - 在 `CalendarPanel` 直接指定 DayPicker 版型欄位（`weekdays/weekday/week/day/day_button`），固定每格為置中且按鈕尺寸一致（`2.2rem`）。
     - 導航 `nav` 改為 `top-5 -translate-y-1/2`，與月份標題垂直同列。
     - 移除先前 `width: 14.2857%` 的強制欄寬，避免與元件 flex 版型互相拉扯造成選中狀態變形。

13. **行事曆樣式硬校正（箭頭高度 + 選中框拉伸）**
   - 回饋：箭頭仍偏高，且灰/黃日期框雖接近但仍有比例差異。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`、`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - `nav` 改為固定 `h-10` 並 `top-0`，與 `month_caption` 同高度同列。
     - `button_previous/button_next` 固定 `8x8`，避免因預設樣式造成垂直偏移。
     - `day_button` 改成 `inline-flex` 並固定 `min/max/width/height = 2.2rem`，徹底禁止選中態拉伸。
     - 清除 `.rdp-selected` 容器背景，僅保留按鈕層背景，避免黃框被外層 cell 背景放大。

14. **行事曆修正回歸（恢復黃框 + 箭頭下移至月份列）**
   - 回饋：箭頭仍在上方；黃框消失。
   - 根因：上一版把 `selected/today` 的 classNames 覆寫為空，導致選中態 class 無法命中；nav 定位過高。
   - 修正：
     - `calendar-panel.tsx` 移除 `selected/today` 空覆寫，恢復 DayPicker 選中狀態樣式。
     - `calendar-panel.tsx` 將 `month` 增加上方留位、`month_caption` 微下移，`nav` 調整為 `top-7`，把左右箭頭壓到月份標題同列。
     - `globals.css` 補上 `.home-calendar .rdp-nav { z-index: 2; }`，避免與其他層互相覆蓋導致視覺錯位。

15. **行事曆對齊參考圖調整（箭頭貼近月份 + 黃灰框等比）**
   - 目標：對齊參考圖（箭頭在月份標題兩側、日期框完全等比例）。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`、`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - 導航列由整行分散改為置中窄寬（`w-32`），並與月份同列；箭頭按鈕改為小圓形底。
     - 清除 `selected day cell` 外層背景，只保留 `day_button` 層上色，避免黃色選中框被 cell 寬度撐開。
     - 修正 `.light .home-calendar .rdp-day_button` 的寬度覆寫（由 `100%` 改回固定 `2.2rem`，加上 `max-width` 與置中），確保深淺色一致比例。

16. **行事曆參考圖最終貼齊（月份列導航 + 今日選中疊樣式清除）**
   - 回饋：箭頭仍未貼齊月份列；選中日仍出現 `today` 疊樣式內框。
   - 修正檔案：`apps/nx-ui/src/components/home/calendar-panel.tsx`、`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - 導航改為置中固定寬（`w-[14rem]`）並定位到月份列中心（`top-[2.05rem]`），箭頭按鈕設 `pointer-events-auto`。
     - 月份標題字級/字重提升（`text-xl font-semibold`）以貼近參考圖視覺比例。
     - 針對 `.rdp-today.rdp-selected`（深色/淺色）新增覆蓋規則，強制只顯示 selected 樣式（背景/邊框/陰影一致），移除內層重疊感。

17. **行事曆視覺定稿（使用者手動微調完成）**
   - 狀態：由使用者在 `apps/nx-ui/src/components/home/calendar-panel.tsx` 直接微調後，畫面已達預期（箭頭位置、月份標題對齊、選中日期比例與整體視覺一致）。
   - 結果：以使用者最終版本作為當日定稿，不再追加同題材樣式修補。

18. **行事曆 hover/鎖定外框行為微調**
   - 需求：滑鼠移動到日期時僅顯示外框，不要以黑底覆蓋選中（黃）或今天日期；深色外框為淺白、淺色外框為深灰。
   - 修正檔案：`apps/nx-ui/src/app/globals.css`。
   - 實作：
     - 深色與淺色分別定義 `:hover/:focus-visible` 為「透明背景 + 外框 + 輕陰影」。
     - 對 `.rdp-selected` 與 `.rdp-today.rdp-selected` 的 hover/focus 增加覆蓋規則，保持 selected 樣式不被 hover 狀態改寫。

19. **行事曆互動樣式定稿（使用者手動調整完成）**
   - 狀態：使用者自行完成最終互動樣式微調（含 hover 與 selected/today 顯示邏輯）。
   - 結果：目前畫面已符合預期，當日此項目以使用者最終手動版本為準。

## 討論與決策（Q&A）
無

## 明日待辦
- [ ] （依產品優先序填寫）

## 遇到的問題與解法
| 問題 | 解法 |
|------|------|
| 新登入頁填公司帳號後，前端把帳號送成 `公司:使用者`，後端僅接受單一 `username`，導致 `HTTP 401`。 | 登入 payload 改回只送 `userAccount`；公司帳號欄位保留 UI 顯示，不參與目前 API 驗證。 |
| 家用電腦執行 nx-ui 時，`globals.css` 的 `@import 'tw-animate-css'` 報 `Can't resolve`。 | 將 `tw-animate-css` 自 `devDependencies` 移至 `dependencies`，重裝套件並重新建置，確認 `next build` 成功。 |
| 家用環境使用 `npm run dev` 時，仍偶發 CSS import 解析錯誤，且伴隨 Turbopack root 推斷警告。 | 改為本地 CSS 檔案引用（避免套件 exports 解析差異），並於 `next.config.ts` 明確設定 `turbopack.root` 到 monorepo root；重啟 dev process 後可正常啟動。 |
