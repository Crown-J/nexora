# 2026-04-14 工作日誌

## 時間未記錄｜開始工作
- **時刻**：時間未記錄（當下未即時記錄實際開始時間，依規範如實填寫）

## 時間未記錄｜[Phase5-NX99] 後端 NX99 第一批 API（租戶／訂閱／功能模組查詢）

- **完成時間**：時間未記錄
- **完成內容**：
  - 新增 `apps/nx-api/src/nx99/`：`Tenant` / `Subscription` / `FeatureFlag` 之 Controller + Service + DTO；`Nx99Module` 掛入 `AppModule`
  - 租戶：`GET/POST /nx99/tenants`、`GET/PATCH/DELETE /nx99/tenants/:id`；`DELETE` 為軟刪（`is_active=false`）；變更寫入 `nx01_audit_log`（`Nx01AuditLogWriterService`）
  - 訂閱：`GET /nx99/subscriptions`、`GET /nx99/subscriptions/:id`（ADMIN 可帶 `tenantId` 查他租戶）
  - 功能開關：尚無 `nx99_feature_flag` 表；`GET /nx99/feature-flags` 依有效訂閱方案＋`nx99_product_module`／`nx99_product_module_map` 推算 `app_module_code` 清單（見 DTO 註解）
  - Auth／Prisma：`nx01_user`／`nx01_user_role` 登入與 RBAC；`ViewPermissionService` 移至 `shared/rbac`（`nx01_role_view`，`toggle` 對應 `canUpdate || canDelete`）；`PrismaModule` 不再依賴 `nx00` Guard
  - `pnpm --filter db-core build` 確保 `db-core` 匯出 `Prisma` 型別；`apps/nx-api` 補齊 `class-validator`／`class-transformer` 依賴
  - ~~`tsconfig` 暫縮 include~~（後續已刪除舊 nx00/nx01/nx02 並恢復全量 `src/**/*.ts`）
- **本機驗證**（`PORT=3011`，種子帳號 `admin` + `Nexoragrid2026` + `tenantCode=HENGYIN`）：`/health` 200；`/auth/login` 201；`/nx99/tenants` 200；`/nx99/subscriptions?tenantId=...` 200；`/nx99/feature-flags?tenantId=...` 200；租戶 POST／PATCH／DELETE／再 PATCH `isActive` 200
- **已知**：Controller 的 Body／Query DTO 不可使用 `import type`（否則 class-validator 無 metadata）；Request body 欄位為 **camelCase**（例：`isActive`）

## 時間未記錄｜[Phase5-NX01] 主檔 API 清理與八項 CRUD

- **完成時間**：時間未記錄
- **完成內容**：
  - 刪除 `apps/nx-api/src/nx00`、`nx01`（舊）、`nx02`（舊）整包目錄
  - `apps/nx-api/tsconfig.json` 恢復 `"include": ["src/**/*.ts"]`
  - `Nx01AuditLogWriterService` 改由 **`PrismaModule`（Global）** 提供，`Nx99Module` 不再重複註冊
  - 新增 `apps/nx-api/src/nx01/`：`Nx01Module`；共用 `shared/nx01/pagination.dto.ts`、`require-tenant.ts`
  - 八項資源（皆 `JwtAuthGuard` + `RolesGuard` + `@Roles('ADMIN')`；租戶資料帶 JWT `tenantId`；`nx01_currency` 無租戶欄位，稽核仍寫入操作者所屬租戶）：
    | 路徑前綴 | 說明 |
    |----------|------|
    | `/nx01/currencies` | 幣別（無 `tenant_id` 篩選） |
    | `/nx01/warehouses` | 倉庫 |
    | `/nx01/part-brands` | 零件品牌 |
    | `/nx01/parts` | 料號（可傳 `codeRuleId` 或 `partBrandId` 自動沿用／建立 `nx01_brand_code_rule`） |
    | `/nx01/partners` | 夥伴；列表可查 `partnerType`=`C`/`S`/`T`/`V`/`B` |
    | `/nx01/users` | 使用者（不回傳 `passwordHash`；建立／變更密碼 bcrypt） |
    | `/nx01/roles` | 職務（`is_system=true` 不可 PATCH／DELETE） |
    | `/nx01/bulletins` | 公告 |
  - 變更寫入 `nx01_audit_log`（`moduleCode: NX01`）
  - 驗證腳本：`apps/nx-api/scripts/nx01-crud-fetch-test.mjs`（等同 curl 流程）
- **指令**：`pnpm --filter nx-api exec tsc --noEmit` 通過；`pnpm --filter nx-api run build` 通過
- **本機驗證**：`PORT=3011` 執行上述腳本，八項資源皆 **GET 列表 → POST → GET :id → PATCH → DELETE（軟刪）→ PATCH isActive** 通過

## 時間未記錄｜[Phase5-NX02] 採購第三批 API（RFQ／PO／RR／退供應商）

- **完成時間**：時間未記錄
- **路由**（皆 `JwtAuthGuard` + `RolesGuard` + `@Roles('ADMIN')`；稽核 `nx01_audit_log`，`moduleCode: NX02`）：
  - `GET/POST /nx02/rfq`、`GET/PATCH/DELETE /nx02/rfq/:id`、`POST/PATCH/DELETE /nx02/rfq/:id/items[/:itemId]`
  - `GET/POST /nx02/po`、`GET/PATCH/DELETE /nx02/po/:id`、`POST/PATCH/DELETE /nx02/po/:id/items[/:itemId]`
  - `GET/POST /nx02/rr`、`GET/PATCH/DELETE /nx02/rr/:id`、`POST/PATCH/DELETE /nx02/rr/:id/items[/:itemId]`；狀態 **DRAFT→INSPECTING→POSTED** 時寫入 **`nx03_stock_balance`** + **`nx03_stock_ledger`**（規格中的 stock_txn 對應 ledger）；可 **REJECTED**／**CANCELLED**（作廢）
  - `GET/POST /nx02/purchase-return`（DB：`nx02_pr`）、`GET/PATCH/DELETE ...`、明細子路由同上；API 狀態 **DRAFT／POSTED／CANCELLED**，DB 仍 **D／P／V**
- **共用**：`shared/nx02/nx02-list-query.dto.ts`、`nx02-doc-no.ts`、`nx02-state-machine.ts`、`nx02-currency.ts`（幣別 code → `nx01_currency.id`）
- **DB migrations**（配合單號／幣別 FK／狀態字串）：
  - `20260414100000_nx02_po_rr_status_varchar30`：`nx02_po`／`nx02_rr` 之 `status` 擴為 VARCHAR(30) 並轉換舊單字元值
  - `20260414103000_nx02_doc_no_varchar30`：`nx02_rfq`／`nx02_po`／`nx02_rr`／`nx02_pr` 之 `doc_no` → VARCHAR(30)
  - `20260414104500_nx02_currency_fk_len`：`nx02_rfq_item`／`nx02_po`／`nx02_rr`／`nx02_pr` 之 `currency_id` → VARCHAR(15)
- **驗證**：`apps/nx-api/scripts/nx02-crud-fetch-test.mjs`（會讀 `packages/db-core/.env`、必要時建立測試用 `nx01_location`）；`pnpm --filter nx-api exec tsc --noEmit` 通過；腳本全流程 **OK**

## 時間未記錄｜[Phase5-NX03] 庫存第四批 API（台帳／異動／入出庫／盤點／調撥）

- **完成時間**：時間未記錄
- **DB migration**：`20260415120000_nx03_inbound_outbound_phase5` — 新增 `nx03_inbound`／`nx03_inbound_item`／`nx03_outbound`／`nx03_outbound_item`（ID 函式 `NX03IBHT`／`NX03IBIT`／`NX03OBHT`／`NX03OBIT`）；`nx03_stock_take`／`nx03_st` 之 `status`、`doc_no` 擴欄並將舊單字元狀態轉為 API token（盤點含 DRAFT／COUNTING／ADJUSTING／POSTED／CANCELLED；調撥含 DRAFT／TRANSIT／RECEIVED／CANCELLED，舊 `P`→`RECEIVED`）
- **Prisma**：`Nx03Inbound`／`Item`、`Nx03Outbound`／`Item`；`Nx03StockTake`／`Nx03St` 欄位寬度與預設值同步
- **後端**：`apps/nx-api/src/nx03/`（`Nx03Module` 掛入 `AppModule`）；`shared/nx03/`：`nx03-list-query`、`nx03-stock-balance-list-query`、`nx03-ledger-list-query`、`nx03-doc-no`（IB／OB／SL／ST）、`nx03-state-machine`、`nx03-inventory`（balance+ledger 單筆 transaction 內呼叫）
- **路由**（皆 `JwtAuthGuard` + `RolesGuard` + `@Roles('ADMIN')`；稽核 `moduleCode: NX03`）：
  - `GET /nx03/stock-balance`、`GET /nx03/stock-balance/:partId`、`GET /nx03/stock-ledger`
  - `GET/POST /nx03/inbound`、`GET/PATCH/DELETE /nx03/inbound/:id`、明細 `POST/PATCH/DELETE .../items[/:itemId]`；過帳 **POSTED** 寫 ledger `sourceModule=NX03`、`sourceDocType=I`
  - 出庫同上前綴 `outbound`；**SHIPPED** 扣帳，ledger `sourceDocType=O`
  - 盤點 `stocktake`：**POSTED** 依差異調整，`sourceDocType=T`（schema 盤點）
  - 調撥 `transfer`（**PLUS+**：`planSupportsNx02PlusFeatures`）：**RECEIVED** 來源倉出／目標倉入各一筆 ledger，`sourceDocType=X`（schema 調撥）
- **驗證**：`pnpm --filter db-core exec prisma migrate deploy`；`pnpm --filter nx-api exec tsc --noEmit`；`node apps/nx-api/scripts/nx03-crud-fetch-test.mjs`（入庫前後讀 `nx03_stock_balance.on_hand_qty`；全流程 OK）

## 時間未記錄｜[Phase5-NX04] 銷售第五批 API（報價／銷貨／銷退）

- **完成時間**：時間未記錄
- **完成內容**：
  - 新增 `apps/nx-api/src/nx04/nx04.module.ts`：`QuoteController`／`SoController`／`SalesReturnController` 掛入 `AppModule`
  - `so.service`：`POST /nx04/so/from-quote/:quoteId`；**SHIPPED** 於單一 transaction 內呼叫 `applyQtyOutWithLedger`（`sourceModule=NX04`、`sourceDocType=S`）；狀態轉換前於 tx 內讀表頭；`Decimal` 以 `String(...)` 建構
  - `sales-return.service`：**POSTED** 呼叫 `applyQtyInWithLedger`（`sourceDocType=R`）；銷退數量驗證（含同批多筆明細）；作廢 `DELETE` → `CANCELLED`
  - 稽核：`moduleCode: NX04`（`Nx01AuditLogWriterService`）
  - **修正**：`so.controller`／`sales-return.controller` 的 Body DTO **不可** `import type`（與 NX99 日誌相同原因，否則 PATCH `status` 被 whitelist 剥除、狀態不寫入 DB）
  - 腳本：`apps/nx-api/scripts/nx04-crud-fetch-test.mjs`（無客戶時自動建立 `partnerType=C` 測試客戶；預設 `NX_API_BASE=http://localhost:3011`）
- **驗證**：`pnpm --filter db-core exec prisma generate`；`pnpm --filter nx-api exec tsc --noEmit`；`node apps/nx-api/scripts/nx04-crud-fetch-test.mjs`（報價→`from-quote`→SO **SHIPPED** 庫存 -2；銷退 **POSTED** 庫存 +1；`GET /nx03/stock-ledger?sourceModule=NX04` 200）

## 時間未記錄｜[Phase5-NX06] 物流第七批 API（送貨／取貨／國際／退貨取件）

- **完成時間**：時間未記錄
- **完成內容**：
  - DB：`20260416120000_nx06_dn_logistics_status_gps_intl`（已於本機 `prisma migrate deploy`）
  - `apps/nx-api/src/nx06/`：`delivery`／`pickup`／`intl-shipping`／`return-pickup` 四組 Controller + DTO；`DnLogisticsService`（`logisticsType` 分流、狀態機、簽收、`PATCH .../delivery/:id/location` GPS、稽核 `moduleCode: NX06`）
  - `shared/nx06/`：`nx06-create-delivery-from-so`；`so.service` **SHIPPED** 後冪等建立送貨草稿（`deliveryType=D` 且 `deliveryAddress` 有值）
  - **SO 表頭**：`UpdateSoDto` 支援 `deliveryType`／`deliveryAddress`（出貨前可改；出貨後拒絕），`SO_SEL` 含 `deliveryAddress`
  - **全域**：`main.ts` 的 `ValidationPipe` 增加 `transformOptions: { enableImplicitConversion: true }`；NX06 明細 `qty` DTO 補 `@IsNumber()`+`@Min(0.0001)`（避免 `whitelist` 剝除未裝飾欄位導致 `qty` 遺失）
  - 驗證腳本：`apps/nx-api/scripts/nx06-crud-fetch-test.mjs`（預設 `NX_API_BASE`，與其他腳本一致；本機驗證時需已 `nest build` 並啟動 API）
- **驗證**：`pnpm exec prisma migrate deploy`（`packages/db-core`）；`pnpm exec tsc --noEmit -p apps/nx-api`；`node apps/nx-api/scripts/nx06-crud-fetch-test.mjs`（`NX_API_BASE=http://localhost:3012`）— SO 出貨自動 DN、**DELIVERED** 簽收（`signerType`／`signedByName`／`signedAt`）、GPS、`PICKED_UP`、國際 **CUSTOMS→IN_TRANSIT→DELIVERED**、退貨取件、手動送貨 **DELETE** 作廢，**全流程 OK**

## 時間未記錄｜[Phase5-NX07] 人資第八批 API（PRO + 非財務 + 薪資明細）

- **完成時間**：時間未記錄
- **完成內容**：
  - `apps/nx-api/src/nx07/nx07.module.ts`：`attendance`／`leave`／`overtime`／`payroll`／`performance`／`training`／`employee-change`；`AppModule` 掛入 `Nx07Module`
  - `shared/nx07/`：`Nx07ProPlanGuard`、`Nx07NoFinanceGuard`（FINANCE 全擋）；狀態機補 **績效／訓練作廢 `VOIDED`**、**員工異動 DRAFT→REJECTED**（DELETE 作廢）
  - 薪資：`GET /nx07/payroll` 僅總額欄位；`GET /nx07/payroll/:id` 依角色 `canViewPayrollSalaryDetail`（ADMIN／HR_ADMIN）決定是否附 `items`（`payroll.controller` 不另鎖 `:id`，由 service 脫敏）
  - `employee-change`：`APPROVED` 時 **TRANSFER** 同步 `nx01_user.role_id`／`department_id` 與主要 `nx01_user_role.role_id`；**RESIGN** 設 `nx01_user.is_active=false`；**HIRE** 啟用帳號並可帶新主檔職務／部門
  - 種子：`packages/db-core/prisma/seed/default/nx01_role.ts` 新增 **`HR_ADMIN`**
  - 驗證腳本：`apps/nx-api/scripts/nx07-crud-fetch-test.mjs`（內建 `nx07ftest_finance`／`nx07ftest_hr`／`nx07ftest_resign` fixture；測 FINANCE 403、薪資明細、離職 `is_active`）
- **驗證**：`pnpm exec tsc --noEmit -p apps/nx-api` **通過**。`node apps/nx-api/scripts/nx07-crud-fetch-test.mjs` 需 **已編譯並重啟** 含 `Nx07Module` 之 nx-api；本機若仍跑舊行程會出現 **`Cannot GET /nx07/leave`（404）**，重啟後再跑腳本即可驗 FINANCE **403** 與其餘流程

## 時間未記錄｜[Phase5-NX08/NX09] 經營分析 + 知識管理第九批 API（PRO）

- **完成時間**：時間未記錄
- **完成內容**：
  - **共用**：`apps/nx-api/src/shared/nexora-pro-plan.ts`（PRO 方案判斷）；`nx07-plan.ts` 改為 re-export 別名；`Nx08ProPlanGuard`／`Nx09ProPlanGuard`
  - **DB**：`20260417100000_nx08_daily_monthly_unique_fix` — `nx08_daily_report`／`nx08_monthly_report` 唯一鍵改為 **`(tenant_id, user_id, report_date / year_month)`**（修正原僅單欄位之錯誤）
  - **NX08**：`nx08/daily-report`（列表篩選、每人每日一筆、`PATCH .../complete` 同步 **nx07_attendance.clock_out_at**）；`nx08/monthly-report`（`GET` 列表 + **`GET .../summary`**，資料來源 **nx01_kpi_record** 聚合，非寫入 `nx08_monthly_report`）；`nx08/kpi-target`（`nx01_kpi_target`）；`nx08/kpi-record`（`nx01_kpi_record` upsert）；`Nx08Module` → `AppModule`
  - **NX09**：`nx09/articles`（標籤篩選、軟刪 `is_active`）；`nx09/documents`（版本列、`PATCH` 新增 **nx09_document_version**、軟刪）；`nx09/meetings`（出席者／會議紀錄／追蹤 action、軟刪 **status=X**）；`Nx09Module` → `AppModule`
  - 稽核：`nx01_audit_log`，`moduleCode` **NX08**／**NX09**
  - 驗證腳本：`apps/nx-api/scripts/nx08-nx09-crud-fetch-test.mjs`（含日報完成 → 打卡 checkout、KPI、文件多版本）
- **驗證**：`pnpm exec prisma migrate deploy` + `prisma generate`（`packages/db-core`）；`pnpm exec tsc --noEmit -p apps/nx-api` **通過**。fetch 腳本需 **重啟 nx-api** 後執行

## 時間未記錄｜[Phase5-NX10] 遊戲化第十批 API（PRO + 今日待辦跨方案）

- **完成時間**：時間未記錄
- **完成內容**：
  - **DB**：`20260418120000_nx10_checkin_log` — `nx10_checkin_log` + `gen_nx10_checkin_log_id()`（`NX10CKLG`）；Prisma `Nx10CheckinLog`
  - **後端**：`apps/nx-api/src/nx10/` — `Nx10ExpService`（`nx10_emp_medal`／`nx10_emp_exp_log`、勳章晉級）、`Nx10CheckinService`（每日一次、`STREAK_D{n}` 自 `nx10_task_template`）、`Nx10TasksService`（`GET /nx10/tasks/today` 彙整 NX02～05；PRO 加 `nx10_emp_task_log`）、`Nx10MedalsService`、`Nx10LeaderboardService`；`shared/nx10/nx10-pro-plan.guard.ts`
  - **路由**：`GET /nx10/exp/me`、`GET /nx10/exp/:userId` + `POST /nx10/exp/award`（ADMIN）；`GET/POST /nx10/checkin`；`GET /nx10/tasks/today`（**僅 Jwt**）；`GET /nx10/tasks`、`PATCH /nx10/tasks/:id/done`（PRO）；`GET /nx10/medals`、`GET /nx10/medals/me`；`GET /nx10/leaderboard?period=week|month|all`
  - 稽核：`nx01_audit_log`，`moduleCode: NX10`（簽到、任務完成、ADMIN 發 EXP）
  - 驗證腳本：`apps/nx-api/scripts/nx10-crud-fetch-test.mjs`（會清除當日簽到 fixture 以驗證 **STREAK_D1**；`NX_API_BASE` 需對應執行中 nx-api 之 `PORT`）
- **驗證**：`pnpm exec prisma migrate deploy` + `prisma generate`（`packages/db-core`）；`pnpm exec tsc --noEmit -p apps/nx-api` **通過**；`NX_API_BASE=http://localhost:3012 node apps/nx-api/scripts/nx10-crud-fetch-test.mjs` **全流程 OK**；同埠 **nx01-crud-fetch-test** 迴歸 **OK**

## 時間未記錄｜結束工作
- **時刻**：時間未記錄（當下未即時記錄）
- **今日完成摘要**：**Phase 5 NX99 第一批 API**；**Phase 5 NX01 主檔八項 CRUD**（含舊模組刪除、`tsconfig` 恢復、型別檢查／建置／整合腳本驗證）；**Phase 5 NX02 採購第三批 API**（含三筆 migration、`nx02-crud-fetch-test.mjs`）；**Phase 5 NX03 庫存第四批 API**（`nx03` migration + `nx03-crud-fetch-test.mjs`）；**Phase 5 NX04 銷售第五批 API**（`nx04-crud-fetch-test.mjs`）；**Phase 5 NX06 物流第七批 API**（`nx06-crud-fetch-test.mjs`、SO 配送欄位 PATCH、`ValidationPipe` 隱式轉型）；**Phase 5 NX07 人資第八批 API**（`Nx07Module`、`nx07-crud-fetch-test.mjs`、`HR_ADMIN` 種子）；**Phase 5 NX08/NX09 第九批 API**（`nexora-pro-plan`、`nx08-nx09-crud-fetch-test.mjs`、日報 unique migration）；**Phase 5 NX10 遊戲化 API**（`nx10_checkin_log` migration、`nx10-crud-fetch-test.mjs`）
- **明日待辦**：依產品需求補 RBAC（非僅 ADMIN）、或展開 NX02／其餘模組
