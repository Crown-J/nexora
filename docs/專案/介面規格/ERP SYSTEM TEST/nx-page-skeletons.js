// NEXORA GRID — 核心主檔｜骨架頁設定（已接導覽，逐頁補做）
// 權限管理 / 據點倉庫 / 往來對象 / 產品與廠牌 / 字典主檔。摘要欄位取自規格 v3.6。
(function () {
  'use strict';
  var CFG = window.NX_PAGE_CONFIGS = window.NX_PAGE_CONFIGS || {};
  function skel(id, title, kind, note, fields) {
    CFG[id] = { id: id, title: title, kind: kind, skeleton: true, note: note, fields: fields || [] };
  }

  /* ---- 權限管理 ---- */
  skel('orgchart', '組織架構圖', 'assign',
    '以圖像化多層級面板「組裝」整個組織的人事歸屬，取代舊版「使用者職務設定／使用者據點設定」。由左至右四層串接：部門 → 組別 → 職務 → 成員，逐欄往右展開指派；未派職員工停留在「未指派成員池」。',
    ['第一層 部門', '第二層 組別', '第三層 職務', '第四層 成員', '未指派成員池', '指派員工到職務', '移除員工指派', '指派職務到組']);
  skel('permmatrix', '職務權限設定', 'matrix',
    '決定「某個職務能對某個畫面做哪些事」。上方選一個職務，縱軸為各畫面（依模組分群），橫軸六種權限打勾設定。採暫存後一次存檔。系統管理員與公司負責人不出現在下拉（本就全權限）。',
    ['瀏覽', '新增', '修改', '停用', '匯出', '核准', '整列全選/清除', '整模組全選/清除', '暫存後存檔']);

  /* ---- 據點倉庫 ---- */
  skel('sitechart', '據點架構圖', 'assign',
    '以圖像化多層級面板組裝倉儲層級與員工據點歸屬，取代舊版「使用者據點設定」。由左至右三層串接：據點 → 倉庫 → 庫位；並可在據點節點指派隸屬員工。',
    ['第一層 據點', '第二層 倉庫', '第三層 庫位', '指派員工到據點', '移除員工據點']);

  /* ---- 往來對象 ---- */

  /* ---- 產品與廠牌 ---- */

  /* ---- 字典主檔 ---- */
})();
