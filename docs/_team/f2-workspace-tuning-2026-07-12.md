<!-- docs/_team/f2-workspace-tuning-2026-07-12.md -->
<!-- 位置：docs/_team/（團隊工作檔） -->
<!-- 版本：v1.0（2026-07-12、七步全部完工、Hank） -->
<!-- 說明：執行長 F2 報價工作台逐環節調整清單。規則：執行長實走一關記一關、
     「純紀錄不動工」、五階段全部 RUN 完後 Hank 一次動工。
     完工紀錄見文末「## 完工（Hank 2026-07-12）」。 -->

# F2 報價工作台・逐環節調整清單（2026-07-12 執行長實走）

**工作模式**：純紀錄 → 流程 RUN 完 → 一次動工。動工前逐條回讀確認。

## ✅ 執行長定案（07/12、五題全回＋開工令）

1. **取貨方式**：客戶主檔**補「預設取貨方式」欄位**（後端小案：schema 加欄
   `default_delivery_type`、值域同銷貨單出貨方式 P自取/D配送/S寄送、nullable；
   DDL 記得進 pending-production.sql）
2. **階段③ Alt+D 加調貨**：**退場**、統一走④出貨倉庫屬性
3. **④移除鍵**：**定案 Alt+D**
4. **⑤編輯模式換行**：**Shift+Enter**（Enter＝回副容器）
5. **⑤手編 vs 設定卡**：**(a)** 設定卡變更即重生成、以最後動作為準

## S5-6・訊息格式（執行長 07/12 補充、定案）

同品名（同通用件群組）**分組顯示**：品名固定當組標題（取其中一個即可、
同群組品名相同）、組內每行＝識別（正廠 或 廠牌名）＋報價：

```
機油芯
正廠 報價 NT$ 200
BOSCH 報價 NT$ 320
```

- 設定卡新增「**廠牌**」選項；**品名固定顯示在組標題**（不再是每行開關）；
  既有：基準料號/副廠料號/數量恆顯 照舊為可選。

## 動工順序（Hank）

1. 後端：partner 補 default_delivery_type（schema + 本機 DDL + pending-production.sql + API DTO）
2. T1 引導精靈（角標換掉、行內 kbd 提示全收）
3. ①對象（B→C→B 選人流＋基本資料八欄）
4. ②搜尋（[] 切法、Combobox/注音下拉、群組樹卡片）
5. ③檢查庫存（B 上半加大+圖片 Alt+P、C 狀態卡+各倉卡片、Alt+D 退場）
6. ④報價（卡片＋Enter 屬性面板五列＋出貨倉庫/調貨＋Alt+S/Alt+2/Alt+D）
7. ⑤發送訊息（滿版固定框、卡片設定、Alt+E/Shift+Enter/Enter 存檔關窗、分組訊息格式）
每完成一項 commit 一筆、全部完成後逐條回讀核對。

---

## 通用（跨階段）

- [ ] **T1・右上角標換引導精靈**：移除「F2 · QUOTE WORKSPACE」角標、改成引導精靈鈕
  （「?」＝Alt+H 模式、記錄本功能全部快捷鍵）；主容器與副容器**不再**顯示行內快捷鍵
  說明（如「Enter 選定｜Esc 離開」、kbd 小標全部收進精靈）。

---

## 階段①・對象（客戶）

- [ ] **S1-1・客戶搜尋流改 B→C→B**：輸入客戶關鍵字按 Enter →
  **候選清單顯示在右側副容器**（跳到右邊確認選哪一個、↑↓ Enter 選定）→
  選定後**客戶顯示在主容器下半部**。
  （對齊搜尋階段的「左輸入、右結果」節奏；現況是輸入框自帶下拉、要拆開。）
- [ ] **S1-2・右側基本資料加三欄**：現有 客編/名稱/地址/電話/備註 之外、加
  **交易條件、取貨方式、預設倉庫**。
  - 資料源核對（Hank 07/12）：交易條件＝客戶主檔國內付款條件 ✅ 現成；
    預設倉庫 ✅ 現成；**取貨方式 ⚠️ 客戶主檔沒有此欄位**（出貨方式目前是
    開銷貨單時才選）——動工前請執行長拍板：(a) 客戶主檔補「預設取貨方式」
    欄位（後端小案）(b) 先不顯示。
  - 地址仍為結構化衛星表待接（前次已記）。

## 階段②・搜尋

- [ ] **S2-1・三查法切換鍵＝`[` `]`**：料號／品名+車型／進階 三種方式用
  中括號鍵左右循環切換（取代現在要滑鼠點的頁籤鈕；頁籤仍可點）。
- [ ] **S2-2・右側結果改回原卡片呈現**：跟 F1 搜尋窗「之前一樣」的結果卡
  （通用件群組樹、主/替代徽章、總數量/可出量/不可出）——取代現在的細列表。
  - Hank 註：搜尋 API 帶 groupByCompat=true 即回群組樹；卡片樣式從
    PartQuickSearchModal 結果區搬用。
- [ ] **S2-3・關鍵字下拉提示**：輸入欄要像客戶欄一樣「即打即出下拉建議」
  （料號/品名 autocomplete）。
  - Hank 註：F1 搜尋窗的 Combobox／PhoneticPicker（注音鍵盤碼）是現成
    元件、直接搬；順便把上輪坦白過的注音碼查法補回來。

## 階段③・檢查庫存

- [ ] **S3-1・B 欄比例調整**：上半（基本資料）加大——要放**基本資料＋產品圖片**
  （通用零件列表不會太多、下半可縮）。
- [ ] **S3-2・圖片 Alt+P 放大**：沿用 F1 主視窗的放大鍵位與 Lightbox。
- [ ] **S3-3・C 欄上半＝出貨狀態區（跟之前一樣）**：大狀態卡片（有貨可出／
  低於安全／缺貨）＋ 總庫存、可出量、不可出量 三指標。
  - Hank 註：即 F1 主視窗中欄那套（STATUS_META 大卡 + KpiTile）、指標改
    執行長點名的三個（總庫存/可出/不可出、在途退場或另示）。
- [ ] **S3-4・C 欄下半＝各倉卡片**：每倉一張卡（取代現在的細表格）。

## 階段④・報價（重點環節：快速掌握報價項目、可跨搜尋輪累加）

- [ ] **S4-1・B 欄改卡片式項目**：每個報價項目一張卡（取代表格列）；
  **Enter＝編輯該卡的數量/單價**（進入編輯、再 Enter 完成該卡）——
  本階段按 Enter **不會**跳到發送訊息（取消現行「末欄 Enter → ⑤」）。
- [ ] **S4-2（修正版）・Enter 進副容器屬性面板**（執行長二稿、取代一稿的
  「Enter 就地編輯量價」與「`<` `>` 切檢視」）：
  B 欄聚焦卡片按 **Enter → 進入 C 副容器**、↑↓ 在五列屬性間移動：
  1. **建議售價** → Enter 展開 ABCD 價
  2. **報價/成交歷史紀錄** → Enter 展開歷史列表（報價+成交整合一張表、
     顏色區分：成交綠/報價金；含該客戶與同級距）
  3. **出貨倉庫** → Enter 選擇**倉位或調貨**（調貨＝入 F5 調貨詢價清單；
     調貨決策跟著項目走、解掉 Alt+D 跨階段衝突）
  4. **數量** → Enter 編輯
  5. **報價** → Enter 編輯價格
  - **Esc 退出回主容器**（B 欄卡片）。
  - Hank 註：倉位選項＝該料有庫存的倉（各倉可出量帶出）；選「調貨」時
    沿用「公司有現貨→確認提示」防呆。
- [ ] **S4-3・結案鍵位**：`Alt+S` → 確認結案提示 → 進⑤發送訊息；
  或 `Alt+2` 回搜尋繼續報下一顆（項目累加保留）。
- [ ] **S4-4・移除項目**：聚焦卡片按 `Alt+D` 移除、需確認訊息。
  - Hank 註：調貨已併入 S4-2 出貨倉庫屬性 → Alt+D 衝突解除；
    連動待確認：**階段③的 Alt+D 加調貨是否退場**（統一走④出貨倉庫）。

## 階段⑤・發送訊息

- [ ] **S5-1・訊息框滿版＋固定大小**：主容器整版就是訊息內容、尺寸固定
  不隨內容伸縮（內部捲動）。
- [ ] **S5-2・按鈕移位**：「複製訊息」「存檔」移到**副容器下方**。
- [ ] **S5-3・設定值改卡片設計**（副容器）。
- [ ] **S5-4・操作流（焦點預設在副容器）**：
  1. ↑↓ 選設定卡、**Space 啟用/取消**（即時反映左側訊息）
  2. **Alt+E** 進主容器＝**編輯模式**：訊息可自由編輯（textarea 改可寫）、
     可 Ctrl+A 全選複製；**Enter 回副容器**
  3. 副容器按 **Enter＝存檔** → 存檔確認訊息 → 確認後**關閉整個即時報價視窗**
  - Hank 註 ⚠️（動工前確認）：編輯模式中 Enter＝返回 → 訊息內**換行**改
    Shift+Enter？（訊息天生多行、純 Enter 返回會打不了換行）
  - Hank 註：現行「下一通（回對象重來）」按鈕依此稿退場、存檔即關窗；
    手動編輯過的文字在改設定值時會被重算蓋掉——實作採「手動編輯後
    設定卡變更即重生成、編輯內容以最後編輯為準」還是鎖設定？動工前確認。

---

## 已知背景（本輪之前已修）

- FocusLockedDialog 焦點被搶根因（打一字跳出）已修（32a984a8 前後兩刀）
- 階段①開啟自動聚焦客戶輸入框已修（32a984a8）
- 階段軌車站路線式、客戶資訊五欄已上（02f483b3）

---

## 完工（Hank 2026-07-12、七步一項一 commit）

1. `975c1481` 後端 partner.default_delivery_type（schema + 本機 DDL 已套 + pending-production.sql 第 4 段 + API DTO）
2. `755a8bf6` T1 引導精靈（角標退役、Alt+H、行內 kbd 全收；順手接上原本沒接鍵的 Alt+N）
3. `37303099` ①對象 B→C→B 選人流 + 基本資料八欄
4. `f4593751` ②搜尋 [ ] 切法 + Combobox 即打即出（含注音碼雙路）+ 群組樹卡片
5. `dff5bb5c` ③檢查庫存 B 圖片+基本資料加大（Alt+P Lightbox 共用 F1）+ C 狀態大卡/三指標/各倉卡片 + Alt+D 退場
6. `9a9a0b2f` ④報價卡片 + 五列屬性面板（ABCD/歷史/出貨倉庫含調貨/量/價）+ Alt+S 結案 + Alt+D 移除 + 新後端 price-history API
7. `29163882` ⑤滿版固定訊息框 + 設定卡鍵盤流 + Alt+E/Shift+Enter + S5-6 分組訊息格式

**值域訂正（⚠️ 執行長請看）**：定案 1 筆記寫「S寄送」、但系統真相是 **C=寄貨**
（NX03 包貨後端 IsIn(D/P/C/T) 強制、schema 註解同）——default_delivery_type 依
「值域同銷貨單」原意採 D/P/C。SO 前端下拉用 S 是既有漂移 bug（S 單會卡包貨）、
已另開修復任務、不在本輪。

**瀏覽器實測（2026-07-12、dev DB）**：C0049 全流程 ①選人八欄（取貨方式端到端）→
②機油芯群組樹 → ③狀態卡/各倉卡 → ④選倉 Z00/數量 2/價 350/歷史展開 →
Alt+S → ⑤分組訊息/Space 重生成/Alt+E 編輯/Enter 存檔關窗 → DB 紀錄帶項目自選倉 Z00 ✅。
測試殘留：dev DB C0049 default_delivery_type='D'、C2250='P'（留著方便執行長驗）。

## 二輪回饋（執行長 07/12 實測五條、Hank 當日修完）

1. `fe7524a8` 1-1 客戶欄即打即出（debounce 250ms 候選即時進右欄、Enter 才跳）
2. `ef4e54c0` 3-1 基本資料標籤/值對齊卡＋3-2 已加入改實心綠邊綠底＋「✓ 已加入」徽章
3. `bafcb655` 4-1 操作側指示（非操作側變暗 45%＋「操作中」徽章＋對側提示）；
   4-2 歷史列改顯示該客戶前價（報價/成交取最近）、Enter 改彈窗
   （↑↓ 選、Enter 帶價入報價欄 → 自動跳出貨倉庫續編、Esc 關）
4. `230e87d4` 5-1 訊息設定加「顯示出貨倉庫」（項目自選倉→調貨→客戶預設倉）
- 4-3 測試資料：dev DB 種 12 筆報價紀錄（562/562A、C0049+同級距 C0048/C0020、
  remark=seed:F2測試）；三家客戶補掛 B 級。成交（綠）列要等有實際銷貨單才會出現。
- 二輪全部瀏覽器實測通過（前價 315 顯示、彈窗選 310 帶入、出貨倉訊息行）。
