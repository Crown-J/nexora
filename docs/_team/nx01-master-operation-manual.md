<!-- docs/_team/nx01-master-operation-manual.md -->
# NEXORA NX01 主檔操作手冊（總經理驗收用）

> 對象：總經理（Crown）驗收 / 上傳給專案經理（Alex）對齊
> 撰寫：Hank（工程師）
> 時間：2026-05-26
> 分支：`feature/nx01-master-complete`（本地，未 push）
> 性質：操作手冊 + 進度真相揭露
>
> ⚠️ **截圖說明**：我在開發環境沒辦法把 app 真的跑起來截圖，所以下面用「文字版面示意（ASCII）」代替畫面。真正的截圖需要在實機跑起來補。手機與鍵盤的實際手感也請在實機驗收。

---

## 一、這次做了什麼（一分鐘版）

1. **後端補完**：倉庫指派的後端做好了，使用者主檔到 100%。
2. **系統管理員鎖定**：一般用戶完全看不到、也碰不到系統管理員（伊諾瓦後台）。
3. **鋼鐵星球通用主檔範式**：做了一套通用畫面，套上去的主檔自動有「桌面 + 手機 + 全鍵盤」。
4. **通用畫面增強**：加了「下拉選單 / 外鍵選擇 / 三種停用約定 / 唯讀表」，所以能套的主檔大增。
5. **遷好 18 個主檔上鋼鐵星球新範式**：使用者、幣別、國家、零件群組、職務、傳動方式、車體類型、車廠品牌、引擎、變速箱、車型、零件廠牌、零件關聯、料件車型適配、倉庫、客戶分級、注音字典、往來對象。
6. **3 個複雜主檔暫留舊範式（功能照常可用）**：零件、品牌料號規則、公告 —— 原因見第六節（各有子表/狀態機，硬遷會壞）。
7. 據點、職務權限視圖：後端目前沒有這兩個主檔（只有前端路由殼），見第六節。

---

## 二、主檔入口位置

| 進入方式 | 路徑 |
|---|---|
| 主檔中心（卡片）| 首頁 →「主資料中心」卡片牆 |
| 直接網址 | `/dashboard/base/{主檔}`，例：`/dashboard/base/currency` |
| 左側選單 | 儀表板左側「帳號與權限 / 產品料號 / 系統設定 …」分群 |

已上線鋼鐵星球新範式的主檔入口：

| 主檔 | 網址 | 分類 |
|---|---|---|
| 使用者 | `/dashboard/base/users` | 帳號與權限 |
| 幣別 | `/dashboard/base/currency` | 系統設定 |
| 國家 | `/dashboard/base/country` | 系統設定 |
| 零件群組 | `/dashboard/base/part-group` | 產品料號 |

---

## 三、通用操作（鋼鐵星球範式，所有新範式主檔一致）

### 畫面結構（ASCII 示意）

```
┌─────────────────────────────────────────────┐
│ ← 返回   📁 系統設定 / 幣別主檔        12 筆幣別 │  ← 標題列
├─────────────────────────────────────────────┤
│ [資料瀏覽 Alt+1]  [詳細資料 Alt+2]            │  ← Tab
├─────────────────────────────────────────────┤
│ ◀ ◀ 1/3 ▶ ▶  A新增 E更正 F查詢 | D停用 匯出 R │  ← ERP 工具列
│                          顯示停用  選取  Q結束 │
├─────────────────────────────────────────────┤
│ 代碼   名稱      符號  小數  狀態              │  ← 列表
│ TWD    新台幣    NT$   0     ● 啟用            │
│ USD    美金      $     2     ● 啟用            │
│ JPY    日圓      ¥     0     ○ 停用            │
└─────────────────────────────────────────────┘
```

### 新增一筆
1. 按 **A**（或工具列「新增」）→ 自動跳到「詳細資料」Tab、出現空白表單。
2. 填欄位（標 `*` 為必填）。
3. 按 **S** 存檔 → 跳出確認框 → 確認 → 寫入。

### 編輯一筆
1. 在「資料瀏覽」用 **↑ ↓** 選列（或滑鼠點）。
2. 按 **E** 更正 → 進編輯模式（代碼類欄位鎖定不可改）。
3. 改完按 **S** 存檔。**改到一半想離開**會跳「存檔後離開 / 丟棄變更 / 取消」三選一，不會默默丟資料。

### 停用 / 啟用（軟刪除，系統不刪資料）
- 選一筆按 **D**：選到「啟用中」的 → 變停用；選到「已停用」的 → 變啟用。
- 停用的資料不會消失，按工具列「顯示停用」就看得到、可重新啟用。

### 查詢
- 按 **F** 開搜尋條，輸入代碼 / 名稱即時過濾（停 300 毫秒自動查），**ESC** 關閉。

### 批次
- 按「選取」→ 列表出現勾選框 → 勾多筆 → 「批次啟用 / 批次停用」。

---

## 四、鍵盤快捷鍵列表（全鍵盤、不用滑鼠也能跑完）

| 按鍵 | 動作 | 模式 |
|---|---|---|
| `Alt+1` | 切到「資料瀏覽」 | 都可 |
| `Alt+2` | 切到「詳細資料」 | 都可 |
| `↑` / `↓` | 上下切換列 | 瀏覽 |
| `Enter` | 進入該列詳細 | 瀏覽 |
| `Alt+A` | 新增 | 瀏覽 |
| `Alt+E` | 更正（編輯選中列） | 瀏覽 |
| `Alt+F` | 開 / 關查詢 | 瀏覽 |
| `Alt+D` | 停用 / 啟用選中列 | 瀏覽 |
| `Alt+R` | 重新整理 | 瀏覽 |
| `Alt+Q` | 結束（返回） | 瀏覽 |
| `Alt+S` | 存檔 | 編輯 |
| `Alt+C` | 取消 | 編輯 |
| `ESC` | 關搜尋 / 取消編輯 | 都可 |

---

## 五、手機操作

- 同一套畫面，手機自動變單欄：標題列 → Tab → 工具列（**可左右滑動**看到全部按鈕）→ 內容。
- 「資料瀏覽」「詳細資料」用上方 Tab 切換（手機沒鍵盤就用點的）。
- 表單在手機是單欄、電腦是兩欄，自動適應。
- ⚠️ 手機實際手感（按鈕大小、滑動順不順）請在實機驗收，我這邊沒辦法實機測。

---

## 六、各主檔狀態 + 欄位（真實進度）

### ✅ 已完成（鋼鐵星球新範式、編譯檢查通過）— 共 17 個

| 主檔 | 入口 | 主要欄位 | 方案 |
|---|---|---|---|
| 使用者 | `/dashboard/base/users` | 帳號、姓名、Email、電話、擔任職務、隸屬倉庫 | 職務/倉庫批次工具 PRO |
| 職務 | `/dashboard/base/roles` | 職務代碼、職務名稱、說明、排序 | LITE |
| 幣別 | `/dashboard/base/currency` | 幣別代碼、名稱、符號、小數位數、排序 | LITE |
| 國家 | `/dashboard/base/country` | 國家代碼、名稱、排序 | LITE |
| 注音字典 | `/dashboard/base/phonetic-dictionary` | 字、主注音、主聲母、使用頻率 | LITE |
| 零件群組 | `/dashboard/base/part-group` | 族群代碼、名稱、排序 | LITE |
| 零件廠牌 | `/dashboard/base/part-brand` | 廠牌代碼、名稱、國別*、排序 | LITE |
| 零件關聯 | `/dashboard/base/part-relation` | 來源零件*、目標零件*、關聯類型*(改號/同款/改版/組合/拆解) | PLUS |
| 料件車型適配 | `/dashboard/base/part-model` | 零件*、車型*、適配等級*(原廠/副廠等效/通用) | PLUS |
| 車廠品牌 | `/dashboard/base/car-brand` | 品牌代碼、名稱、英文名、國別*、Logo、排序 | LITE |
| 引擎 | `/dashboard/base/engine` | 代碼、名稱、燃料別*(汽油/柴油/油電/純電)、排氣量、進氣方式、車廠* | LITE |
| 變速箱 | `/dashboard/base/transmission` | 代碼、名稱、變速型式*(手排/自排/雙離合/CVT/AMT/其他)、檔位、車廠* | LITE |
| 傳動方式 | `/dashboard/base/drivetrain` | 代碼、名稱、英文名、排序 | LITE |
| 車體類型 | `/dashboard/base/model-type` | 代碼、名稱、英文名、排序 | LITE |
| 車型 | `/dashboard/base/model` | 車型代碼、名稱、車廠*、年式起*、年式迄、引擎、變速箱、傳動、車體類型 | LITE |
| 倉庫 | `/dashboard/base/warehouses` | 倉庫代碼、名稱、倉別*、備註、排序 | LITE |
| 客戶分級 | `/dashboard/base/customer-grade` | 分級代碼(A/B/C/D 固定)、名稱、加成率、排序（不可新增、只能改/停用）| LITE |
| 往來對象 | `/dashboard/base/partners` | 對象代碼、名稱、類型*(客戶/供應商/運輸商/廠商/銀行/兼具)、聯絡人、電話、Email、統編、地址、客戶分級*、信用狀態*、付款條件* | LITE |

> `*` = 下拉 / 外鍵選擇欄位。往來對象類型代碼語意：C 客戶、S 供應商、T 運輸商、V 廠商、B 銀行、BOTH 客戶兼供應商（Crown 2026-05-26 定案）。

### 🟡 暫留舊範式（功能照常可用，未換鋼鐵星球）— 3 個

> 這 3 個各有特殊結構，用通用畫面套會在實機上出錯（編譯檢查看不出來），所以誠實留著舊畫面、下一輪客製：

| 主檔 | 原因 |
|---|---|
| 零件 | 有「價格版本」子表 + 編碼規則強制 + 預覽料號功能 → 需專屬編輯畫面 |
| 品牌料號規則 | 規則是巢狀的「分段定義」JSON → 需專屬規則編輯器 |
| 公告 | 有狀態流程（草稿→發布→撤回）+ 附件 + 已讀統計 → 需專屬畫面 |

### ⬜ 後端尚未實作 — 2 個

| 主檔 | 真相 |
|---|---|
| 據點 | `nx01` 後端**沒有**這個主檔的 controller（只有前端路由殼）→ 要先補後端 |
| 職務權限視圖 | 同上，後端不存在；且這是權限矩陣、需專屬畫面 |

### 🔒 系統管理員鎖定（已完成、後端層）

- 一般用戶在「職務」清單看不到系統管理員、在「使用者」清單看不到伊諾瓦後台帳號。
- 一般用戶不能指派系統管理員職務、也停用不了它。
- 做在後端最底層，所有畫面只要走 API 就自動擋住。

---

## 七、PRO / PLUS 版功能

| 主檔 / 功能 | 最低方案 |
|---|---|
| 使用者「職務 / 倉庫」批次工具 | PRO |
| 品牌料號規則、零件關聯、料件車型適配 | PLUS |
| 其餘主檔 | LITE（都可用） |

---

## 八、已知問題 / 限制（誠實揭露）

1. **我無法在此環境跑 app**：17 個都通過 TypeScript 編譯檢查，但畫面實際長相、手機手感、鍵盤順不順、API 串接 runtime 是否正確，**需要實機驗收**。截圖也要實機補。請您驗收時特別點幾個有外鍵的（如車型、引擎）看下拉有沒有正確帶出選項。
2. **外鍵下拉目前一次載前 100 筆**：零件、車型資料量大時，下拉只看得到前 100 筆。零件關聯 / 料件車型適配這種要選零件的，未來要改成「搜尋式挑選」。
3. **4 個複雜主檔暫留舊範式**（往來對象 / 零件 / 品牌料號規則 / 公告）：仍可正常使用，只是還沒換鋼鐵星球外觀。
4. **據點 / 職務權限視圖後端不存在**：要先補後端才能做。

---

## 九、建議下一步

1. **零件 / 品牌料號規則 / 公告**：各自客製專屬編輯畫面（子表 / 規則編輯器 / 狀態機）。
2. **據點 / 職務權限視圖**：先補後端 controller（目前 nx01 後端沒有這兩個）。
3. **外鍵下拉升級**為搜尋式挑選（解決前 100 筆限制）。
