import type { PrismaClient } from '../../../../generated/prisma';

/** LITE 測試資料占位（Phase 4：結構就緒，後續補料號／單據）。 */
export async function seedTestLite(_prisma: PrismaClient): Promise<void> {
  console.log('✅ test/lite: （尚無額外測試資料）');
}
