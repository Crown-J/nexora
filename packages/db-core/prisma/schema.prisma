// C:\nexora\packages\db-core\prisma\schema.prisma
// NEXORA / db-core schema
// - Prisma 欄位 camelCase；DB 欄位 snake_case 用 @map 對應
// - 欄位說明：每個 scalar／關聯欄位上方使用 ///（Prisma doc）；PostgreSQL 另以 migration COMMENT ON 與 schema 對齊（NX02～NX06 已落地）
// - ID 規則：VARCHAR(15) 由 DB DEFAULT gen_xxx_id() 產生
// - Prisma v7：連線資訊由 prisma.config.ts 管理，因此 datasource 不可寫 url

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

/// =======================================================
/// NX00 - User
/// Table: nx00_user
/// =======================================================
model Nx00User {
  id           String    @id @default(dbgenerated("gen_nx00_user_id()")) @db.VarChar(15)
  username     String    @unique @db.VarChar(50)
  passwordHash String    @map("password_hash") @db.VarChar(255)
  displayName  String    @map("display_name") @db.VarChar(50)
  email        String?   @db.VarChar(100)
  phone        String?   @db.VarChar(20)
  isActive     Boolean   @default(true) @map("is_active")
  lastLoginAt  DateTime? @map("last_login_at")

  createdAt    DateTime  @default(now()) @map("created_at")
  createdBy    String?   @map("created_by") @db.VarChar(15)
  updatedAt    DateTime  @default(now()) @updatedAt @map("updated_at")
  updatedBy    String?   @map("updated_by") @db.VarChar(15)

  // 租戶綁定（nullable，待 NX99 資料建立後補資料改為必填）
  tenantId String?     @db.VarChar(15) @map("tenant_id")
  tenant   Nx99Tenant? @relation(fields: [tenantId], references: [id])

  // self audit relations
  createdByUser Nx00User? @relation("Nx00UserCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00UserUpdatedBy", fields: [updatedBy], references: [id])

  createdUsers  Nx00User[] @relation("Nx00UserCreatedBy")
  updatedUsers  Nx00User[] @relation("Nx00UserUpdatedBy")

  // RBAC (UserRole / RoleView)
  userRoles Nx00UserRole[]

  // reverse relations (created/updated)
  rolesCreated       Nx00Role[]      @relation("Nx00RoleCreatedBy")
  rolesUpdated       Nx00Role[]      @relation("Nx00RoleUpdatedBy")
  viewsCreated       Nx00View[]      @relation("Nx00ViewCreatedBy")
  viewsUpdated       Nx00View[]      @relation("Nx00ViewUpdatedBy")
  roleViewsCreated   Nx00RoleView[]  @relation("Nx00RoleViewCreatedBy")
  roleViewsUpdated   Nx00RoleView[]  @relation("Nx00RoleViewUpdatedBy")

  partsCreated       Nx00Part[]      @relation("Nx00PartCreatedBy")
  partsUpdated       Nx00Part[]      @relation("Nx00PartUpdatedBy")
  brandsCreated      Nx00Brand[]     @relation("Nx00BrandCreatedBy")
  brandsUpdated      Nx00Brand[]     @relation("Nx00BrandUpdatedBy")
  warehousesCreated  Nx00Warehouse[] @relation("Nx00WarehouseCreatedBy")
  warehousesUpdated  Nx00Warehouse[] @relation("Nx00WarehouseUpdatedBy")
  locationsCreated   Nx00Location[]  @relation("Nx00LocationCreatedBy")
  locationsUpdated   Nx00Location[]  @relation("Nx00LocationUpdatedBy")
  partnersCreated    Nx00Partner[]   @relation("Nx00PartnerCreatedBy")
  partnersUpdated    Nx00Partner[]   @relation("Nx00PartnerUpdatedBy")

  nx02DeptsCreated   Nx02Dept[]      @relation("Nx02DeptCreatedBy")
  nx02DeptsUpdated   Nx02Dept[]      @relation("Nx02DeptUpdatedBy")
  nx03EmpsCreated    Nx03Emp[]       @relation("Nx03EmpCreatedBy")
  nx03EmpsUpdated    Nx03Emp[]       @relation("Nx03EmpUpdatedBy")
  nx04UnitsCreated   Nx04Unit[]      @relation("Nx04UnitCreatedBy")
  nx04UnitsUpdated   Nx04Unit[]      @relation("Nx04UnitUpdatedBy")
  nx05CategoriesCreated Nx05Category[] @relation("Nx05CategoryCreatedBy")
  nx05CategoriesUpdated Nx05Category[] @relation("Nx05CategoryUpdatedBy")
  nx06ProductsCreated Nx06Product[]  @relation("Nx06ProductCreatedBy")
  nx06ProductsUpdated Nx06Product[]  @relation("Nx06ProductUpdatedBy")

  // assignment/grant/revoke actors
  userRolesAssigned   Nx00UserRole[]  @relation("Nx00UserRoleAssignedBy")
  roleViewsGranted    Nx00RoleView[]  @relation("Nx00RoleViewGrantedBy")
  roleViewsRevoked    Nx00RoleView[]  @relation("Nx00RoleViewRevokedBy")

  // audit log actor
  auditLogs Nx00AuditLog[]

  @@map("nx00_user")
}

/// =======================================================
/// NX00 - Role
/// Table: nx00_role
/// =======================================================
model Nx00Role {
  id          String   @id @default(dbgenerated("gen_nx00_role_id()")) @db.VarChar(15)
  code        String   @unique @db.VarChar(30)
  name        String   @db.VarChar(50)
  description String?  @db.VarChar(200)
  isSystem    Boolean  @default(false) @map("is_system")
  isActive    Boolean  @default(true) @map("is_active")
  sortNo      Int      @default(0) @map("sort_no")

  createdAt   DateTime @default(now()) @map("created_at")
  createdBy   String?  @map("created_by") @db.VarChar(15)
  updatedAt   DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy   String?  @map("updated_by") @db.VarChar(15)

  createdByUser Nx00User? @relation("Nx00RoleCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00RoleUpdatedBy", fields: [updatedBy], references: [id])

  userRoles Nx00UserRole[]
  roleViews Nx00RoleView[]

  @@map("nx00_role")
}

/// =======================================================
/// NX00 - UserRole
/// Table: nx00_user_role
/// =======================================================
model Nx00UserRole {
  id         String    @id @default(dbgenerated("gen_nx00_user_role_id()")) @db.VarChar(15)
  userId     String    @map("user_id") @db.VarChar(15)
  roleId     String    @map("role_id") @db.VarChar(15)

  isPrimary  Boolean   @default(false) @map("is_primary")
  assignedAt DateTime  @default(now()) @map("assigned_at")
  assignedBy String?   @map("assigned_by") @db.VarChar(15)
  revokedAt  DateTime? @map("revoked_at")
  isActive   Boolean   @default(true) @map("is_active")

  user          Nx00User @relation(fields: [userId], references: [id])
  role          Nx00Role @relation(fields: [roleId], references: [id])
  assignedByUser Nx00User? @relation("Nx00UserRoleAssignedBy", fields: [assignedBy], references: [id])

  @@unique([userId, roleId])
  @@index([userId])
  @@index([roleId])
  @@index([isActive])
  @@map("nx00_user_role")
}

/// =======================================================
/// NX00 - View
/// Table: nx00_view
/// =======================================================
model Nx00View {
  id         String   @id @default(dbgenerated("gen_nx00_view_id()")) @db.VarChar(15)
  code       String   @unique @db.VarChar(50)
  name       String   @db.VarChar(100)
  moduleCode String   @map("module_code") @db.VarChar(10)
  path       String   @db.VarChar(200)
  sortNo     Int      @default(0) @map("sort_no")
  isActive   Boolean  @default(true) @map("is_active")

  createdAt  DateTime @default(now()) @map("created_at")
  createdBy  String?  @map("created_by") @db.VarChar(15)
  updatedAt  DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy  String?  @map("updated_by") @db.VarChar(15)

  createdByUser Nx00User? @relation("Nx00ViewCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00ViewUpdatedBy", fields: [updatedBy], references: [id])

  roleViews Nx00RoleView[]

  @@index([moduleCode])
  @@index([isActive])
  @@map("nx00_view")
}

/// =======================================================
/// NX00 - RoleView
/// Table: nx00_role_view
/// =======================================================
model Nx00RoleView {
  id        String   @id @default(dbgenerated("gen_nx00_role_view_id()")) @db.VarChar(15)
  roleId    String   @map("role_id") @db.VarChar(15)
  viewId    String   @map("view_id") @db.VarChar(15)

  canRead   Boolean  @default(true)  @map("can_read")
  canCreate Boolean  @default(false) @map("can_create")
  canUpdate Boolean  @default(false) @map("can_update")
  canDelete Boolean  @default(false) @map("can_delete")
  canExport Boolean  @default(false) @map("can_export")

  isActive  Boolean  @default(true) @map("is_active")

  grantedAt DateTime @default(now()) @map("granted_at")
  grantedBy String?  @map("granted_by") @db.VarChar(15)
  revokedAt DateTime? @map("revoked_at")
  revokedBy String?  @map("revoked_by") @db.VarChar(15)

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  role Nx00Role @relation(fields: [roleId], references: [id])
  view Nx00View @relation(fields: [viewId], references: [id])

  grantedByUser Nx00User? @relation("Nx00RoleViewGrantedBy", fields: [grantedBy], references: [id])
  revokedByUser Nx00User? @relation("Nx00RoleViewRevokedBy", fields: [revokedBy], references: [id])

  createdByUser Nx00User? @relation("Nx00RoleViewCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00RoleViewUpdatedBy", fields: [updatedBy], references: [id])

  @@unique([roleId, viewId])
  @@index([roleId])
  @@index([viewId])
  @@index([isActive])
  @@map("nx00_role_view")
}

/// =======================================================
/// NX00 - Part
/// Table: nx00_part
/// =======================================================
model Nx00Part {
  id       String  @id @default(dbgenerated("gen_nx00_part_id()")) @db.VarChar(15)
  code     String  @unique @db.VarChar(50)
  name     String  @db.VarChar(200)
  brandId  String? @map("brand_id") @db.VarChar(15)
  spec     String? @db.VarChar(200)
  uom      String  @default("pcs") @db.VarChar(10)
  isActive Boolean @default(true) @map("is_active")

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  brand Nx00Brand? @relation(fields: [brandId], references: [id])

  createdByUser Nx00User? @relation("Nx00PartCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00PartUpdatedBy", fields: [updatedBy], references: [id])

  rfqItems Nx01RfqItem[]
  poItems  Nx01PoItem[]
  quoteItems Nx07QuoteItem[]
  salesOrderItems Nx08SalesOrderItem[]
  stockBalances Nx09StockBalance[]
  stockTxns Nx09StockTxn[]

  @@index([brandId])
  @@index([isActive])
  @@map("nx00_part")
}

/// =======================================================
/// NX00 - Brand
/// Table: nx00_brand
/// =======================================================
model Nx00Brand {
  id            String  @id @default(dbgenerated("gen_nx00_brand_id()")) @db.VarChar(15)
  code          String  @unique @db.VarChar(30)
  name          String  @db.VarChar(100)
  originCountry String? @map("origin_country") @db.VarChar(50)
  remark        String? @db.VarChar(200)
  isActive      Boolean @default(true) @map("is_active")
  sortNo        Int     @default(0) @map("sort_no")

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  createdByUser Nx00User? @relation("Nx00BrandCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00BrandUpdatedBy", fields: [updatedBy], references: [id])

  parts Nx00Part[]

  @@index([isActive])
  @@map("nx00_brand")
}

/// =======================================================
/// NX00 - Warehouse
/// Table: nx00_warehouse
/// =======================================================
model Nx00Warehouse {
  id       String  @id @default(dbgenerated("gen_nx00_warehouse_id()")) @db.VarChar(15)
  code     String  @unique @db.VarChar(10)
  name     String  @db.VarChar(100)
  remark   String? @db.VarChar(200)
  sortNo   Int     @default(0) @map("sort_no")
  isActive Boolean @default(true) @map("is_active")

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  createdByUser Nx00User? @relation("Nx00WarehouseCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00WarehouseUpdatedBy", fields: [updatedBy], references: [id])

  locations Nx00Location[]
  poItems   Nx01PoItem[]
  salesOrderItems Nx08SalesOrderItem[]
  stockBalances Nx09StockBalance[]
  stockTxns      Nx09StockTxn[]

  @@index([isActive])
  @@map("nx00_warehouse")
}

/// =======================================================
/// NX00 - Location
/// Table: nx00_location
/// =======================================================
model Nx00Location {
  id          String  @id @default(dbgenerated("gen_nx00_location_id()")) @db.VarChar(15)
  warehouseId String  @map("warehouse_id") @db.VarChar(15)
  code        String  @db.VarChar(30)
  name        String? @db.VarChar(100)
  zone        String? @db.VarChar(20)
  rack        String? @db.VarChar(20)
  levelNo     Int?    @map("level_no")
  binNo       String? @map("bin_no") @db.VarChar(20)
  remark      String? @db.VarChar(200)
  sortNo      Int     @default(0) @map("sort_no")
  isActive    Boolean @default(true) @map("is_active")

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  warehouse Nx00Warehouse @relation(fields: [warehouseId], references: [id])

  createdByUser Nx00User? @relation("Nx00LocationCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00LocationUpdatedBy", fields: [updatedBy], references: [id])

  poItems Nx01PoItem[]
  salesOrderItems Nx08SalesOrderItem[]

  @@unique([warehouseId, code])
  @@index([warehouseId])
  @@index([isActive])
  @@map("nx00_location")
}

/// =======================================================
/// NX00 - Partner
/// Table: nx00_partner
/// =======================================================
model Nx00Partner {
  id          String  @id @default(dbgenerated("gen_nx00_partner_id()")) @db.VarChar(15)
  code        String  @unique @db.VarChar(30)
  name        String  @db.VarChar(120)
  partnerType String  @default("BOTH") @map("partner_type") @db.VarChar(10)
  contactName String? @map("contact_name") @db.VarChar(50)
  phone       String? @db.VarChar(30)
  mobile      String? @db.VarChar(30)
  email       String? @db.VarChar(100)
  address     String? @db.VarChar(200)
  remark      String? @db.VarChar(200)
  isActive    Boolean @default(true) @map("is_active")

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  createdByUser Nx00User? @relation("Nx00PartnerCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User? @relation("Nx00PartnerUpdatedBy", fields: [updatedBy], references: [id])

  rfqs Nx01Rfq[]
  pos  Nx01Po[]
  quotes Nx07Quote[]
  salesOrders Nx08SalesOrder[]

  @@index([isActive])
  @@index([partnerType])
  @@map("nx00_partner")
}

/// =======================================================
/// NX00 - AuditLog
/// Table: nx00_audit_log
/// =======================================================
model Nx00AuditLog {
  id          String   @id @default(dbgenerated("gen_nx00_audit_log_id()")) @db.VarChar(15)
  occurredAt  DateTime @default(now()) @map("occurred_at")
  actorUserId String   @map("actor_user_id") @db.VarChar(15)

  moduleCode  String   @map("module_code") @db.VarChar(10)
  action      String   @db.VarChar(20)
  entityTable String   @map("entity_table") @db.VarChar(50)

  entityId    String?  @map("entity_id") @db.VarChar(20)
  entityCode  String?  @map("entity_code") @db.VarChar(50)
  summary     String?  @db.VarChar(200)

  beforeData  Json?    @map("before_data")
  afterData   Json?    @map("after_data")

  ipAddr      String?  @map("ip_addr") @db.VarChar(45)
  userAgent   String?  @map("user_agent") @db.VarChar(200)

  actorUser Nx00User @relation(fields: [actorUserId], references: [id])

  @@index([occurredAt])
  @@index([actorUserId])
  @@index([moduleCode])
  @@index([entityTable])
  @@map("nx00_audit_log")
}

/// =======================================================
/// NX99 - Tenant
/// Table: nx99_tenant
/// =======================================================
model Nx99Tenant {
  /// NX99 租戶主檔 - 每間公司一筆，作為多租戶隔離根節點
  /// 主鍵（系統產生）
  id        String   @id @default(dbgenerated("gen_nx99_tant_id()")) @db.VarChar(15)
  /// 租戶代碼（唯一）
  code      String   @unique @db.VarChar(30)
  /// 租戶名稱
  name      String   @db.VarChar(100)
  /// 狀態（單碼）
  status    String   @db.VarChar(1)
  /// 備註
  remark    String?  @db.VarChar(200)
  /// 排序號
  sortNo    Int      @map("sort_no")
  /// 是否啟用
  isActive  Boolean  @map("is_active")
  /// 建立時間
  createdAt DateTime @default(now()) @map("created_at")
  /// 建立者（使用者 id，可空）
  createdBy String?  @db.VarChar(15) @map("created_by")
  /// 最後更新時間
  updatedAt DateTime @updatedAt      @map("updated_at")
  /// 最後更新者（使用者 id，可空）
  updatedBy String?  @db.VarChar(15) @map("updated_by")

  subscriptions Nx99Subscription[]
  users         Nx00User[]
  rfqs          Nx01Rfq[]
  rfqItems      Nx01RfqItem[]
  pos           Nx01Po[]
  poItems       Nx01PoItem[]
  quotes        Nx07Quote[]
  quoteItems    Nx07QuoteItem[]
  salesOrders   Nx08SalesOrder[]
  salesOrderItems Nx08SalesOrderItem[]
  stockBalances Nx09StockBalance[]
  stockTxns      Nx09StockTxn[]

  /// 部門主檔（NX02）
  nx02Depts     Nx02Dept[]
  /// 員工主檔（NX03）
  nx03Emps      Nx03Emp[]
  /// 單位主檔（NX04）
  nx04Units     Nx04Unit[]
  /// 類別主檔（NX05）
  nx05Categories Nx05Category[]
  /// 產品主檔（NX06）
  nx06Products  Nx06Product[]

  @@map("nx99_tenant")
}

/// =======================================================
/// NX01 - RFQ (Header)
/// Table: nx01_rfq
/// =======================================================
model Nx01Rfq {
  /// NX01 詢價單表頭 - 記錄供應商詢價主檔，支援後續轉進貨單
  id           String    @id @default(dbgenerated("gen_nx01_rfqo_id()")) @db.VarChar(15)
  tenantId     String?   @db.VarChar(15)  @map("tenant_id")
  docNo        String    @unique @db.VarChar(15) @map("doc_no")
  /// 單號規則：[R]+[年月]+[倉別]+[5碼流水號]，EX：R-202602-Z01-00001
  rfqDate      DateTime  @map("rfq_date") @db.Date
  supplierId   String    @db.VarChar(15)  @map("supplier_id")
  contactName  String?   @db.VarChar(50)  @map("contact_name")
  contactPhone String?   @db.VarChar(30)  @map("contact_phone")
  currency     String    @db.VarChar(10)  @default("TWD")
  status       String    @db.VarChar(20)  @default("D")
  /// 單據狀態：(D=DRAFT, S=SENT, R=REPLIED, C=CLOSED, V=VOID)
  remark       String?   @db.VarChar(200)
  createdAt    DateTime  @default(now()) @map("created_at")
  createdBy    String?   @db.VarChar(15) @map("created_by")
  updatedAt    DateTime  @updatedAt      @map("updated_at")
  updatedBy    String?   @db.VarChar(15) @map("updated_by")
  voidedAt     DateTime? @map("voided_at")
  voidedBy     String?   @db.VarChar(15) @map("voided_by")

  tenant   Nx99Tenant? @relation(fields: [tenantId], references: [id])
  supplier Nx00Partner @relation(fields: [supplierId], references: [id])
  items    Nx01RfqItem[]
  quotes   Nx07Quote[]

  @@map("nx01_rfq")
}

/// =======================================================
/// NX01 - RFQ Item
/// Table: nx01_rfq_item
/// =======================================================
model Nx01RfqItem {
  /// NX01 詢價單明細 - 含料號快照與供應商回覆資訊，支援部分或完整轉單
  id           String   @id @default(dbgenerated("gen_nx01_rfit_id()")) @db.VarChar(15)
  tenantId     String?  @db.VarChar(15)   @map("tenant_id")
  rfqId        String   @db.VarChar(15)   @map("rfq_id")
  lineNo       Int      @map("line_no")
  /// 明細排序號（1,2,3...）
  partId       String   @db.VarChar(15)   @map("part_id")
  /// → nx00_part
  partNo       String   @db.VarChar(50)   @map("part_no")
  /// 料號快照
  partName     String   @db.VarChar(200)  @map("part_name")
  /// 零件名稱快照
  qty          Decimal  @db.Decimal(14, 4)
  unitPrice    Decimal? @db.Decimal(14, 4) @map("unit_price")
  /// 供應商回覆單價（未回覆可空）
  currency     String   @db.VarChar(10)   @default("TWD")
  leadTimeDays Int?     @map("lead_time_days")
  /// 交期（天數，可空）
  status       String   @db.VarChar(20)   @default("P")
  /// 狀態：(P=PENDING, R=REPLIED, S=SELECTED, C=REJECTED)
  remark       String?  @db.VarChar(200)
  createdAt    DateTime @default(now()) @map("created_at")
  createdBy    String?  @db.VarChar(15) @map("created_by")
  updatedAt    DateTime @updatedAt      @map("updated_at")
  updatedBy    String?  @db.VarChar(15) @map("updated_by")

  tenant Nx99Tenant? @relation(fields: [tenantId], references: [id])
  rfq    Nx01Rfq     @relation(fields: [rfqId], references: [id])
  part   Nx00Part    @relation(fields: [partId], references: [id])
  quoteItems Nx07QuoteItem[]

  @@map("nx01_rfq_item")
}

/// =======================================================
/// NX01 - PO (Header)
/// Table: nx01_po
/// =======================================================
model Nx01Po {
  /// NX01 進貨單表頭 - 過帳後產生庫存異動與應付帳款
  id           String    @id @default(dbgenerated("gen_nx01_poht_id()")) @db.VarChar(15)
  tenantId     String?   @db.VarChar(15)  @map("tenant_id")
  docNo        String    @unique @db.VarChar(15) @map("doc_no")
  /// 單號規則：[I]+[年月]+[倉別]+[5碼流水號]，EX：I-202602-Z01-00001
  poDate       DateTime  @map("po_date") @db.Date
  supplierId   String    @db.VarChar(15)  @map("supplier_id")
  rfqId        String?   @db.VarChar(15)  @map("rfq_id")
  currency     String    @db.VarChar(10)  @default("TWD")
  subtotal     Decimal   @db.Decimal(14, 2) @default(0)
  taxAmount    Decimal   @db.Decimal(14, 2) @default(0) @map("tax_amount")
  totalAmount  Decimal   @db.Decimal(14, 2) @default(0) @map("total_amount")
  status       String    @db.VarChar(1)   @default("D")
  /// 單據狀態：(D=DRAFT, P=POSTED, C=CANCELED)
  postedAt     DateTime? @map("posted_at")
  postedBy     String?   @db.VarChar(15)  @map("posted_by")
  remark       String?   @db.VarChar(200)
  createdAt    DateTime  @default(now()) @map("created_at")
  createdBy    String?   @db.VarChar(15) @map("created_by")
  updatedAt    DateTime  @updatedAt      @map("updated_at")
  updatedBy    String?   @db.VarChar(15) @map("updated_by")
  voidedAt     DateTime? @map("voided_at")
  voidedBy     String?   @db.VarChar(15) @map("voided_by")

  tenant   Nx99Tenant? @relation(fields: [tenantId], references: [id])
  supplier Nx00Partner @relation(fields: [supplierId], references: [id])
  items    Nx01PoItem[]

  @@map("nx01_po")
}

/// =======================================================
/// NX01 - PO Item
/// Table: nx01_po_item
/// =======================================================
model Nx01PoItem {
  /// NX01 進貨單明細 - 過帳時寫入庫存異動台帳並更新庫存餘額
  id          String   @id @default(dbgenerated("gen_nx01_poit_id()")) @db.VarChar(15)
  tenantId    String?  @db.VarChar(15)   @map("tenant_id")
  poId        String   @db.VarChar(15)   @map("po_id")
  lineNo      Int      @map("line_no")
  /// 明細行號（1,2,3…）
  partId      String   @db.VarChar(15)   @map("part_id")
  /// → nx00_part
  partNo      String   @db.VarChar(50)   @map("part_no")
  /// 料號快照
  partName    String   @db.VarChar(200)  @map("part_name")
  /// 零件名稱快照
  warehouseId String   @db.VarChar(15)   @map("warehouse_id")
  /// → nx00_warehouse
  locationId  String?  @db.VarChar(15)   @map("location_id")
  /// → nx00_location（LITE 先要求必填，此處保留 nullable）
  qty         Decimal  @db.Decimal(14, 4)
  unitCost    Decimal  @db.Decimal(14, 4) @map("unit_cost")
  lineAmount  Decimal  @db.Decimal(14, 4) @map("line_amount")
  /// qty * unit_cost
  remark      String?  @db.VarChar(200)
  createdAt   DateTime @default(now()) @map("created_at")
  createdBy   String?  @db.VarChar(15) @map("created_by")
  updatedAt   DateTime @updatedAt      @map("updated_at")
  updatedBy   String?  @db.VarChar(15) @map("updated_by")

  tenant    Nx99Tenant?   @relation(fields: [tenantId], references: [id])
  po        Nx01Po        @relation(fields: [poId], references: [id])
  part      Nx00Part      @relation(fields: [partId], references: [id])
  warehouse Nx00Warehouse @relation(fields: [warehouseId], references: [id])
  location  Nx00Location? @relation(fields: [locationId], references: [id])

  @@map("nx01_po_item")
}

/// =======================================================
/// NX07 - Quote（對客報價）
/// Table: nx07_quote
/// =======================================================
model Nx07Quote {
  /// 報價單表頭
  id        String   @id @default(dbgenerated("gen_nx07_quote_id()")) @db.VarChar(15)
  tenantId  String   @map("tenant_id") @db.VarChar(15)
  docNo     String   @unique @map("doc_no") @db.VarChar(15)
  quoteDate DateTime @map("quote_date") @db.Date

  customerId String   @map("customer_id") @db.VarChar(15)
  /// 若報價是從某次詢價衍生，可保存 RFQ；實務仍以「明細強連結」為準
  rfqId   String?  @map("rfq_id") @db.VarChar(15)

  currency String   @map("currency") @db.VarChar(10) @default("TWD")
  status   String   @map("status") @db.VarChar(20) @default("D")
  remark   String?  @map("remark") @db.VarChar(200)

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant    Nx99Tenant  @relation(fields: [tenantId], references: [id])
  customer  Nx00Partner @relation(fields: [customerId], references: [id])
  rfq       Nx01Rfq?    @relation(fields: [rfqId], references: [id])
  items     Nx07QuoteItem[]
  salesOrders Nx08SalesOrder[]

  @@map("nx07_quote")
}

/// =======================================================
/// NX07 - Quote Item（報價明細：強連結 RFQ item）
/// Table: nx07_quote_item
/// =======================================================
model Nx07QuoteItem {
  id      String   @id @default(dbgenerated("gen_nx07_quote_item_id()")) @db.VarChar(15)
  tenantId String  @map("tenant_id") @db.VarChar(15)

  quoteId  String @map("quote_id") @db.VarChar(15)
  lineNo   Int    @map("line_no")

  /// 強連結到 RFQ 明細（成本/交期來源）
  rfqItemId String @map("rfq_item_id") @db.VarChar(15)

  /// 料號快照
  partId   String @map("part_id") @db.VarChar(15)
  partNo   String @map("part_no") @db.VarChar(50)
  partName String @map("part_name") @db.VarChar(200)

  qty        Decimal  @db.Decimal(14, 4)
  unitCost   Decimal  @db.Decimal(14, 4) @map("unit_cost_snapshot")
  unitPrice  Decimal  @db.Decimal(14, 4) @map("unit_price_snapshot")

  /// markup 計算快照（MVP 可先存單一欄位；若未來要拆百分比/金額可再擴充）
  markupType  String? @map("markup_type") @db.VarChar(1)
  markupValue Decimal? @map("markup_value") @db.Decimal(14, 4)

  currency String @map("currency") @db.VarChar(10) @default("TWD")
  leadTimeDays Int? @map("lead_time_days")

  remark    String? @map("remark") @db.VarChar(200)

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant   Nx99Tenant  @relation(fields: [tenantId], references: [id])
  quote    Nx07Quote   @relation(fields: [quoteId], references: [id])
  rfqItem  Nx01RfqItem @relation(fields: [rfqItemId], references: [id])
  part     Nx00Part    @relation(fields: [partId], references: [id])
  salesOrderItems Nx08SalesOrderItem[]

  @@map("nx07_quote_item")
}

/// =======================================================
/// NX08 - Sales Order（銷貨單）
/// Table: nx08_sales_order
/// =======================================================
model Nx08SalesOrder {
  id         String   @id @default(dbgenerated("gen_nx08_sales_order_id()")) @db.VarChar(15)
  tenantId   String   @map("tenant_id") @db.VarChar(15)
  docNo      String   @unique @map("doc_no") @db.VarChar(15)
  soDate     DateTime @map("so_date") @db.Date

  customerId String   @map("customer_id") @db.VarChar(15)
  quoteId    String   @map("quote_id") @db.VarChar(15)

  currency String   @map("currency") @db.VarChar(10) @default("TWD")
  status   String   @map("status") @db.VarChar(20) @default("D")
  remark   String?  @map("remark") @db.VarChar(200)

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant   Nx99Tenant  @relation(fields: [tenantId], references: [id])
  customer Nx00Partner @relation(fields: [customerId], references: [id])
  quote    Nx07Quote   @relation(fields: [quoteId], references: [id])
  items    Nx08SalesOrderItem[]

  @@map("nx08_sales_order")
}

/// =======================================================
/// NX08 - Sales Order Item（銷貨明細）
/// Table: nx08_sales_order_item
/// =======================================================
model Nx08SalesOrderItem {
  id       String   @id @default(dbgenerated("gen_nx08_sales_order_item_id()")) @db.VarChar(15)
  tenantId String   @map("tenant_id") @db.VarChar(15)

  salesOrderId String @map("sales_order_id") @db.VarChar(15)
  lineNo     Int    @map("line_no")

  /// 強連結到 Quote item（用於售價/料號快照追溯）
  quoteItemId String @map("quote_item_id") @db.VarChar(15)

  partId   String @map("part_id") @db.VarChar(15)
  partNo   String @map("part_no") @db.VarChar(50)
  partName String @map("part_name") @db.VarChar(200)

  qty       Decimal @db.Decimal(14, 4)
  unitPrice Decimal @db.Decimal(14, 4) @map("unit_price_snapshot")

  /// 出貨配置（MVP：直接從 PO/用戶選擇複製）
  warehouseId String @map("warehouse_id") @db.VarChar(15)
  locationId  String? @map("location_id") @db.VarChar(15)

  remark    String? @map("remark") @db.VarChar(200)

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant   Nx99Tenant  @relation(fields: [tenantId], references: [id])
  salesOrder Nx08SalesOrder @relation(fields: [salesOrderId], references: [id])
  quoteItem  Nx07QuoteItem  @relation(fields: [quoteItemId], references: [id])

  part     Nx00Part      @relation(fields: [partId], references: [id])
  warehouse Nx00Warehouse @relation(fields: [warehouseId], references: [id])
  location  Nx00Location? @relation(fields: [locationId], references: [id])

  @@map("nx08_sales_order_item")
}

/// =======================================================
/// NX09 - Stock Balance（庫存餘額）
/// Table: nx09_stock_balance
/// =======================================================
model Nx09StockBalance {
  id         String   @id @default(dbgenerated("gen_nx09_stock_balance_id()")) @db.VarChar(15)
  tenantId   String   @map("tenant_id") @db.VarChar(15)
  warehouseId String  @map("warehouse_id") @db.VarChar(15)
  partId     String   @map("part_id") @db.VarChar(15)

  qty Decimal @default(0) @db.Decimal(14, 4)

  createdAt DateTime @default(now()) @map("created_at")
  createdBy String?  @map("created_by") @db.VarChar(15)
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant    Nx99Tenant    @relation(fields: [tenantId], references: [id])
  warehouse Nx00Warehouse @relation(fields: [warehouseId], references: [id])
  part      Nx00Part      @relation(fields: [partId], references: [id])

  @@unique([tenantId, warehouseId, partId])
  @@index([warehouseId])
  @@map("nx09_stock_balance")
}

/// =======================================================
/// NX09 - Stock Txn（庫存異動台帳）
/// Table: nx09_stock_txn
/// =======================================================
model Nx09StockTxn {
  id String @id @default(dbgenerated("gen_nx09_stock_txn_id()")) @db.VarChar(15)

  tenantId   String   @map("tenant_id") @db.VarChar(15)
  occurredAt DateTime @default(now()) @map("occurred_at")

  txnType String @map("txn_type") @db.VarChar(1) // I=inbound, O=outbound
  refType String @map("ref_type") @db.VarChar(10) // PO/SO
  refId   String @map("ref_id") @db.VarChar(15)

  partId     String @map("part_id") @db.VarChar(15)
  warehouseId String @map("warehouse_id") @db.VarChar(15)

  qtyDelta  Decimal @db.Decimal(14, 4) @map("qty_delta")
  beforeQty Decimal @db.Decimal(14, 4) @map("before_qty")
  afterQty  Decimal @db.Decimal(14, 4) @map("after_qty")

  remark String? @map("remark") @db.VarChar(200)

  createdBy String? @map("created_by") @db.VarChar(15)
  updatedBy String? @map("updated_by") @db.VarChar(15)

  tenant    Nx99Tenant    @relation(fields: [tenantId], references: [id])
  warehouse Nx00Warehouse @relation(fields: [warehouseId], references: [id])
  part      Nx00Part      @relation(fields: [partId], references: [id])

  @@index([occurredAt])
  @@index([warehouseId])
  @@index([partId])
  @@map("nx09_stock_txn")
}

/// =======================================================
/// NX02 - Dept（部門主檔，對應設計 NX02_Dept）
/// Table: nx02_dept
/// =======================================================
model Nx02Dept {
  /// 主鍵（系統產生）
  id        String   @id @default(dbgenerated("gen_nx02_dept_id()")) @db.VarChar(15)
  /// 租戶／公司（對應設計 CompanyID → nx99_tenant）
  tenantId  String   @map("tenant_id") @db.VarChar(15)
  /// 部門代號（對應設計 DeptID）
  deptCode  String   @map("dept_code") @db.VarChar(20)
  /// 部門名稱
  deptName  String   @map("dept_name") @db.VarChar(50)
  /// 備註
  remark    String?  @db.Text
  /// 建立日期
  createdAt DateTime @default(now()) @map("created_at")
  /// 建立人員（→ nx00_user.id）
  createdBy String?  @map("created_by") @db.VarChar(15)
  /// 修改日期
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  /// 修改人員（→ nx00_user.id）
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant          Nx99Tenant @relation(fields: [tenantId], references: [id])
  employees       Nx03Emp[]
  createdByUser   Nx00User?  @relation("Nx02DeptCreatedBy", fields: [createdBy], references: [id])
  updatedByUser   Nx00User?  @relation("Nx02DeptUpdatedBy", fields: [updatedBy], references: [id])

  @@unique([tenantId, deptCode])
  @@index([tenantId])
  @@map("nx02_dept")
}

/// =======================================================
/// NX03 - Emp（員工主檔，對應設計 NX03_Emp）
/// Table: nx03_emp
/// =======================================================
model Nx03Emp {
  /// 主鍵（系統產生）
  id       String   @id @default(dbgenerated("gen_nx03_emp_id()")) @db.VarChar(15)
  /// 租戶／公司
  tenantId String   @map("tenant_id") @db.VarChar(15)
  /// 員工代號（對應設計 EmpID）
  empCode  String   @map("emp_code") @db.VarChar(20)
  /// 員工姓名
  empName  String   @map("emp_name") @db.VarChar(50)
  /// 部門（→ nx02_dept）
  deptId   String   @map("dept_id") @db.VarChar(15)
  /// 職稱
  title    String?  @db.VarChar(50)
  /// 電話
  phone    String?  @db.VarChar(20)
  /// 電子郵件
  email    String?  @db.VarChar(100)
  /// 備註
  remark   String?  @db.Text
  /// 建立日期
  createdAt DateTime @default(now()) @map("created_at")
  /// 建立人員（→ nx00_user.id）
  createdBy String?  @map("created_by") @db.VarChar(15)
  /// 修改日期
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  /// 修改人員（→ nx00_user.id）
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant        Nx99Tenant @relation(fields: [tenantId], references: [id])
  dept          Nx02Dept   @relation(fields: [deptId], references: [id])
  createdByUser Nx00User?  @relation("Nx03EmpCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User?  @relation("Nx03EmpUpdatedBy", fields: [updatedBy], references: [id])

  @@unique([tenantId, empCode])
  @@index([tenantId])
  @@index([deptId])
  @@map("nx03_emp")
}

/// =======================================================
/// NX04 - Unit（單位主檔，對應設計 NX04_Unit）
/// Table: nx04_unit
/// =======================================================
model Nx04Unit {
  /// 主鍵（系統產生）
  id        String   @id @default(dbgenerated("gen_nx04_unit_id()")) @db.VarChar(15)
  /// 租戶／公司
  tenantId  String   @map("tenant_id") @db.VarChar(15)
  /// 單位代號（對應設計 UnitID）
  unitCode  String   @map("unit_code") @db.VarChar(20)
  /// 單位名稱
  unitName  String   @map("unit_name") @db.VarChar(50)
  /// 備註
  remark    String?  @db.Text
  /// 建立日期
  createdAt DateTime @default(now()) @map("created_at")
  /// 建立人員（→ nx00_user.id）
  createdBy String?  @map("created_by") @db.VarChar(15)
  /// 修改日期
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
  /// 修改人員（→ nx00_user.id）
  updatedBy String?  @map("updated_by") @db.VarChar(15)

  tenant          Nx99Tenant     @relation(fields: [tenantId], references: [id])
  products        Nx06Product[]
  createdByUser   Nx00User?      @relation("Nx04UnitCreatedBy", fields: [createdBy], references: [id])
  updatedByUser   Nx00User?      @relation("Nx04UnitUpdatedBy", fields: [updatedBy], references: [id])

  @@unique([tenantId, unitCode])
  @@index([tenantId])
  @@map("nx04_unit")
}

/// =======================================================
/// NX05 - Category（類別主檔，對應設計 NX05_Category）
/// Table: nx05_category
/// =======================================================
model Nx05Category {
  /// 主鍵（系統產生）
  id            String   @id @default(dbgenerated("gen_nx05_cat_id()")) @db.VarChar(15)
  /// 租戶／公司
  tenantId      String   @map("tenant_id") @db.VarChar(15)
  /// 類別代號（對應設計 CategoryID）
  categoryCode  String   @map("category_code") @db.VarChar(20)
  /// 類別名稱
  categoryName  String   @map("category_name") @db.VarChar(50)
  /// 備註
  remark        String?  @db.Text
  /// 建立日期
  createdAt     DateTime @default(now()) @map("created_at")
  /// 建立人員（→ nx00_user.id）
  createdBy     String?  @map("created_by") @db.VarChar(15)
  /// 修改日期
  updatedAt     DateTime @default(now()) @updatedAt @map("updated_at")
  /// 修改人員（→ nx00_user.id）
  updatedBy     String?  @map("updated_by") @db.VarChar(15)

  tenant          Nx99Tenant    @relation(fields: [tenantId], references: [id])
  products        Nx06Product[]
  createdByUser   Nx00User?     @relation("Nx05CategoryCreatedBy", fields: [createdBy], references: [id])
  updatedByUser   Nx00User?     @relation("Nx05CategoryUpdatedBy", fields: [updatedBy], references: [id])

  @@unique([tenantId, categoryCode])
  @@index([tenantId])
  @@map("nx05_category")
}

/// =======================================================
/// NX06 - Product（產品主檔，對應設計 NX06_Product）
/// Table: nx06_product
/// =======================================================
model Nx06Product {
  /// 主鍵（系統產生）
  id           String   @id @default(dbgenerated("gen_nx06_prod_id()")) @db.VarChar(15)
  /// 租戶／公司
  tenantId     String   @map("tenant_id") @db.VarChar(15)
  /// 產品代號（對應設計 ProductID）
  productCode  String   @map("product_code") @db.VarChar(50)
  /// 產品名稱
  productName  String   @map("product_name") @db.VarChar(100)
  /// 規格
  spec         String?  @db.VarChar(100)
  /// 單位（→ nx04_unit）
  unitId       String   @map("unit_id") @db.VarChar(15)
  /// 類別（→ nx05_category）
  categoryId   String   @map("category_id") @db.VarChar(15)
  /// 單價
  price        Decimal  @default(0) @db.Decimal(18, 4)
  /// 成本
  cost         Decimal  @default(0) @db.Decimal(18, 4)
  /// 備註
  remark       String?  @db.Text
  /// 建立日期
  createdAt    DateTime @default(now()) @map("created_at")
  /// 建立人員（→ nx00_user.id）
  createdBy    String?  @map("created_by") @db.VarChar(15)
  /// 修改日期
  updatedAt    DateTime @default(now()) @updatedAt @map("updated_at")
  /// 修改人員（→ nx00_user.id）
  updatedBy    String?  @map("updated_by") @db.VarChar(15)

  tenant        Nx99Tenant   @relation(fields: [tenantId], references: [id])
  unit          Nx04Unit     @relation(fields: [unitId], references: [id])
  category      Nx05Category @relation(fields: [categoryId], references: [id])
  createdByUser Nx00User?    @relation("Nx06ProductCreatedBy", fields: [createdBy], references: [id])
  updatedByUser Nx00User?    @relation("Nx06ProductUpdatedBy", fields: [updatedBy], references: [id])

  @@unique([tenantId, productCode])
  @@index([tenantId])
  @@index([unitId])
  @@index([categoryId])
  @@map("nx06_product")
}

/// =======================================================
/// NX99 - Plan
/// Table: nx99_plan
/// =======================================================
model Nx99Plan {
  /// NX99 方案主檔 - 定義 LITE / PLUS / PRO 計價規則
  id                String   @id @default(dbgenerated("gen_nx99_plan_id()")) @db.VarChar(15)
  code              String   @unique @db.VarChar(20)
  name              String   @db.VarChar(50)
  levelNo           Int      @map("level_no")
  baseFeeMonth      Int      @map("base_fee_month")
  seatFeeMonth      Int      @map("seat_fee_month")
  minSeats          Int      @map("min_seats")
  maxSeats          Int      @map("max_seats")
  billingDefault    String   @db.VarChar(10) @map("billing_default")
  yearDiscountType  String   @db.VarChar(1)  @map("year_discount_type")
  yearDiscountValue Int      @map("year_discount_value")
  yearPriceOverride Int?     @map("year_price_override")
  remark            String?  @db.VarChar(200)
  sortNo            Int?     @map("sort_no")
  isActive          Boolean  @default(true) @map("is_active")
  createdAt         DateTime @default(now()) @map("created_at")
  createdBy         String?  @db.VarChar(15) @map("created_by")
  updatedAt         DateTime @updatedAt      @map("updated_at")
  updatedBy         String?  @db.VarChar(15) @map("updated_by")

  subscriptions Nx99Subscription[]

  @@map("nx99_plan")
}

/// =======================================================
/// NX99 - Subscription
/// Table: nx99_subscription
/// =======================================================
model Nx99Subscription {
  /// NX99 租戶訂閱資料 - 含費用快照，異動不影響歷史紀錄
  id                     String   @id @default(dbgenerated("gen_nx99_subs_id()")) @db.VarChar(15)
  tenantId               String   @db.VarChar(15) @map("tenant_id")
  planId                 String   @db.VarChar(15) @map("plan_id")
  status                 String   @db.VarChar(1)
  billingCycle           String   @db.VarChar(1)  @map("billing_cycle")
  seats                  Int
  startAt                DateTime @map("start_at")
  endAt                  DateTime @map("end_at")
  autoRenew              Boolean  @map("auto_renew")
  baseFeeSnapshot        Int      @map("base_fee_snapshot")
  seatFeeSnapshot        Int      @map("seat_fee_snapshot")
  discountTypeSnapshot   String   @db.VarChar(1)  @map("discount_type_snapshot")
  discountValueSnapshot  Int      @map("discount_value_snapshot")
  subtotalSnapshot       Int      @map("subtotal_snapshot")
  discountAmountSnapshot Int      @map("discount_amount_snapshot")
  totalSnapshot          Int      @map("total_snapshot")
  currency               String   @db.VarChar(10) @default("TWD")
  remark                 String?  @db.VarChar(200)
  createdAt              DateTime @default(now()) @map("created_at")
  createdBy              String?  @db.VarChar(15) @map("created_by")
  updatedAt              DateTime @updatedAt      @map("updated_at")
  updatedBy              String?  @db.VarChar(15) @map("updated_by")

  tenant Nx99Tenant @relation(fields: [tenantId], references: [id])
  plan   Nx99Plan   @relation(fields: [planId], references: [id])
  items  Nx99SubscriptionItem[]

  @@map("nx99_subscription")
}

/// =======================================================
/// NX99 - SubscriptionItem
/// Table: nx99_subscription_item
/// =======================================================
model Nx99SubscriptionItem {
  /// NX99 訂閱項目明細 - 記錄標配/加購模組與價格快照
  id                    String   @id @default(dbgenerated("gen_nx99_suit_id()")) @db.VarChar(15)
  subscriptionId        String   @db.VarChar(15) @map("subscription_id")
  itemType              String   @db.VarChar(20) @map("item_type")
  refId                 String   @db.VarChar(15) @map("ref_id")
  status                String   @db.VarChar(20)
  isIncluded            Boolean  @map("is_included")
  billingCycle          String   @db.VarChar(1)  @map("billing_cycle")
  priceSnapshot         Int      @map("price_snapshot")
  discountTypeSnapshot  String   @db.VarChar(1)  @map("discount_type_snapshot")
  discountValueSnapshot Int      @map("discount_value_snapshot")
  totalSnapshot         Int      @map("total_snapshot")
  startAt               DateTime @map("start_at")
  endAt                 DateTime @map("end_at")
  createdAt             DateTime @default(now()) @map("created_at")
  createdBy             String?  @db.VarChar(15) @map("created_by")
  updatedAt             DateTime @updatedAt      @map("updated_at")
  updatedBy             String?  @db.VarChar(15) @map("updated_by")

  subscription Nx99Subscription @relation(fields: [subscriptionId], references: [id])

  @@map("nx99_subscription_item")
}

/// =======================================================
/// NX99 - ProductModule
/// Table: nx99_product_module
/// =======================================================
model Nx99ProductModule {
  /// NX99 產品模組主檔 - 定義可銷售的 SKU
  id                 String   @id @default(dbgenerated("gen_nx99_prmo_id()")) @db.VarChar(15)
  code               String   @unique @db.VarChar(30)
  name               String   @db.VarChar(100)
  moduleLevel        String   @db.VarChar(1)  @map("module_level")
  applicablePlanCode String   @db.VarChar(30) @map("applicable_plan_code")
  billingType        String   @db.VarChar(1)  @map("billing_type")
  monthlyFee         Int      @map("monthly_fee")
  isBundleDefault    Boolean  @map("is_bundle_default")
  description        String?
  sortNo             Int      @map("sort_no")
  isActive           Boolean  @default(true) @map("is_active")
  createdAt          DateTime @default(now()) @map("created_at")
  createdBy          String?  @db.VarChar(15) @map("created_by")
  updatedAt          DateTime @updatedAt      @map("updated_at")
  updatedBy          String?  @db.VarChar(15) @map("updated_by")

  moduleMaps Nx99ProductModuleMap[]

  @@map("nx99_product_module")
}

/// =======================================================
/// NX99 - ProductModuleMap
/// Table: nx99_product_module_map
/// =======================================================
model Nx99ProductModuleMap {
  /// NX99 模組對照表 - 將 SKU 對應到程式碼模組 NX00~NX06
  id              String   @id @default(dbgenerated("gen_nx99_prmm_id()")) @db.VarChar(15)
  productModuleId String   @db.VarChar(15) @map("product_module_id")
  appModuleCode   String   @db.VarChar(10) @map("app_module_code")
  isRequired      Boolean  @default(true)  @map("is_required")
  remark          String?  @db.VarChar(200)
  createdAt       DateTime @default(now()) @map("created_at")
  createdBy       String?  @db.VarChar(15) @map("created_by")
  updatedAt       DateTime @updatedAt      @map("updated_at")
  updatedBy       String?  @db.VarChar(15) @map("updated_by")

  productModule Nx99ProductModule @relation(fields: [productModuleId], references: [id])

  @@map("nx99_product_module_map")
}