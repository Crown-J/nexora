# =============================================================================
# db-core / Prisma — Supabase PostgreSQL（雙連線）
# =============================================================================
# 複製為 .env 並將 [password]、[project-ref] 換成專案實際值。
#
# DATABASE_URL — Transaction Pooler（埠 6543）
#   應用程式執行期、連線池、多數查寫使用；經 PgBouncer，適合 serverless／長連線情境。
#
# DIRECT_URL — Direct Connection（埠 5432）
#   Prisma migrate、db push、seed 等需完整 PostgreSQL 連線時使用（繞過 pooler）。
# =============================================================================

# Supabase Transaction Pooler（port 6543）— 應用程式執行期間使用
DATABASE_URL="postgresql://postgres:[password]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres"

# Supabase Direct Connection（port 5432）— prisma migrate / db push / seed 使用
DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

# Prisma ORM 7：`schema.prisma` 的 datasource 僅宣告 provider；CLI 連線字串由 `prisma.config.ts`
# 讀取，並優先使用 **DIRECT_URL**（無則退回 DATABASE_URL）。執行期程式（seed.ts、nx-api 等）
# 仍應使用 **DATABASE_URL**（pooler）搭配 driver adapter。
