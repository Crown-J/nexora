# =============================================================================
# db-core / Prisma — Supabase PostgreSQL（雙連線）
# =============================================================================
# 複製為 .env，將 [project-ref]、[password]、pooler 主機（aws-0 / aws-1 等）換成
# Dashboard → Settings → Database 連線字串中的實際值（建議整段複製再只改密碼）。
#
# 使用者名稱必須為 postgres.[project-ref]（不可只寫 postgres），否則 pooler 常出現
# 「Tenant or user not found」。
#
# DATABASE_URL — Transaction pooler（埠 6543）+ pgbouncer 參數
#   應用程式執行期、nx-api、多數查寫。
#
# DIRECT_URL — 遷移／seed 用（埠 5432，與 pooler 同主機；Dashboard「Configure Prisma」常見格式）
#   若你的專案仍顯示 db.[project-ref].supabase.co:5432，亦可作為 DIRECT_URL。
# =============================================================================

# Supabase：Connection pooling（6543）— 務必帶 ?pgbouncer=true（與官方 Prisma 步驟一致）
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Supabase：Migrations 用（5432，同一 pooler 區域主機；請以 Dashboard 為準，可能是 aws-1-…）
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres"

# 選用：部分專案 Dashboard 仍提供直連主機（與上列擇一即可，勿混用錯密碼）
# DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

# Prisma ORM 7：CLI 連線由 prisma.config.ts 讀取，優先 DIRECT_URL。
# apps/nx-api 僅需 DATABASE_URL（與本檔第一條相同）；見 apps/nx-api/.env.example。
