'use client';

/**
 * [4-2] 2026-06-05：NX-MANUAL-02 v2.0 §④ 對齊。
 *
 * 原為 LITE 方案下採購單區塊靜態示意（含「升級至 PLUS」推銷文案）。
 * 推銷字眼一律不渲染、且 PoLiteAware 已對 LITE 改 redirect、此元件已無實際渲染路徑。
 * 保留 export 殼不破壞 import、內容改 null。
 */
export function PoPlusTeaser() {
  return null;
}
