/**
 * NX03 庫存管理 — 子路由共用 layout（TASK-0420 v2）
 */
export default function Nx03Layout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-0 flex-1 p-4 md:p-6">{children}</div>;
}
