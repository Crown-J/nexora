/**
 * 銷貨中心首頁（路由 v2：`/dashboard/sales`）
 * 版型對齊採購中心 Hub。
 */

import { SalesCenterHub } from '@/features/sales/ui/SalesCenterHub';

export default function SalesHubPage() {
  return <SalesCenterHub />;
}
