# 2026-04-21（週二）— Hank

## TASK-0421 單據公版 Component + DEMO 單據頁面

- 新增 `apps/nx-ui/src/components/document/`：`DocLayout`、`DocListView`、`DocDetailView`、`DocHeader`、`DocItemTable`、`docStatus`，以及 `index.ts` 匯出。
- 明細表：`inputClassName`（報價單價低於最低售價紅字）、`disableRowDelete`（銷貨明細已備貨列不可刪）。
- DEMO 頁：`RfqDocPage`、`PoDocPage`、`RrDocPage`、`QtDocPage`、`SoDocPage`（mock 多狀態）；路由 `/dashboard/purchase/rfq|po|rr`、`/dashboard/sale/qt|so`。
- `next.config.ts`：`/purchase/*`、`/sale/*` 捷徑 redirect 至 dashboard 對應路徑。
- 採購／銷售 Hub 卡片補 `href`；星球選單採購／銷貨／庫存改為子選單（TASK 規格鍵位標籤）。
- 驗收：`pnpm --filter nx-ui build` 通過。
