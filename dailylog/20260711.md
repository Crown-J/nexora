<!-- dailylog/20260711.md -->
# 2026-07-11（週六）— 家用機線：F2 下鑽 + 主檔排版 + 兩本手冊 + F5 轉正 + weimeng-p2 接手 + 直送鏈盤點 + AP 歸戶 + 驗收修正 + 選單鍵盤 + 首頁 V1 + 鍵盤/銷售/財務流情境驗收（同日十五案）

> 本篇＝**家用機線**、承 dailylog/20260710.md 的 F2 改版線；同日十五案：
> 案一 F2 下鑽（`feature/f2-drilldown`、4 commits + 日誌、merge `2b6fc409`）→ 0711-A~D
> 案二 主檔詳細頁排版（`feature/master-detail-layout`、3 commits、merge `88bc63bb`）→ 0711-E
> 案三 F2 操作手冊（`feature/f2-manual`、2 commits、merge `28204de3`）→ 0711-F
> 案四 F5 進銷統計轉正（`feature/f2-monthly-stats`、3 commits、merge `95410b04`）→ 0711-G
> 案五 收尾：死碼清理 + 家用機交接檔（`feature/home-line-closeout`、2 commits）→ 0711-H
> 案六 NX-MANUAL-02 核心主檔手冊逐欄核對 2.8（`feature/manual-02-audit`、6 commits）→ 0711-I
> 案七 weimeng-p2 家機接手 + 三步驗證（`feature/weimeng-p2`、公司機遠端失聯改家機續作）→ 0711-J
> 案八 代購/直送鏈缺口盤點（`feature/directship-gap-survey`、純報告零改動）→ 0711-K
> 案九 AP 帳款歸戶（`feature/ap-bill-to`、盤點缺口②收尾、Crown 授權 Hank 決策+總驗收制）→ 0711-L
> 案十 Crown 總驗收兩發現修正（`feature/acceptance-fixes-0711`、SO 建單 400 + 選單暫緩旗標過時）→ 0711-M
> 案十一 選單重排 + 頂部選單全鍵盤（`feature/menu-keyboard-ia`、三拍板落地）→ 0711-N
> 案十二 首頁改版 V1（`feature/home-v1`、待辦卡/快捷/主管數字/公告）→ 0711-O
> 案十三 G3~G5 鍵盤情境驗收 + Home/End 補洞（`feature/kb-scenarios-g3g5`）→ 0711-P
> 案十四 B 組銷售流情境驗收（`feature/sales-flow-b-verify`、13 項 API E2E 全過）→ 0711-Q
> 案十五 E 組財務流情境驗收（`feature/finance-flow-e-verify`、23 項 + 實體帳 3 核對）→ 0711-R
> 交接依據：docs/_team/f2-redesign-handoff.md §7 缺口、優先序 Crown 指示：銷貨比價 → 進貨比價 → 適用車型/對應料

---

## 0711-A｜開工前拍板（Crown 三題全過建議案）

```
1. 快捷鍵：F8 銷貨比價 / F9 進貨比價 / F10 相關零件（F3~F7 定版不動）
   ⚠️ F10 瀏覽器預設聚焦選單列 → capture preventDefault 攔（同 F5 攔重整手法）、須實測
2. 適用車型：無既有 API（只有反向的車型篩料號）→ 本線加唯讀端點 GET :id/models
   （part-search 自有模組、不動 schema、Step 3 加 isMine/isPrimary 有前例）、併 F10 頁籤
3. 建議售價列成本照露（權限分權題 Crown 先前指示擱置、走現有權限目錄）
```

---

## 0711-B｜四 commits（每步 tsc 綠 + 推）

```
commit 1  b68c5587  F8 銷貨比價
  - quickPanel 系統加 'sales'：lazy load + 換料件清快取（照 F5/F6 既有模式）
  - 建議售價列（成本+A~D）→ 歷史銷貨表 → 歷史報價表（含最低價/成交標記/已轉數量）
  - 比價面板寬殼 960px（meta.wide）；後端各近 50 筆 → 面板標筆數 + 達上限警示
  - 狀態中文對齊 @data/types/nx04 字面、取精簡版局部映射（design 層不 import nx04）

commit 2  2246c58c  F9 進貨比價
  - 歷史進貨表（日期/單號/供應商/數量/單位成本/實際成本/金額/批號/狀態）
  - 實際成本欄橘色強調（比價主看點）

commit 3  f203c7d8  F10 相關零件（對應料）
  - 一表列出、守既拍板 relationType 1~5 不分子類型歸一區

commit 4  83318c5f  適用車型唯讀端點 + F10 兩頁籤
  - 後端 GET /nx01/part-search/:id/models（Nx01PartModel→Nx01Model、
    fitLevel/年份/引擎/品牌、isActive 過濾、sortNo 排序）
  - F10 面板改「對應料 / 適用車型」頁籤、開面板兩支 API 一次載齊
  - 順手清：controller 檔頭 endpoint 清單 drift（原列 7 支、實際 13 支）補齊
```

全程不動 schema；nx04 / shared/doc-shell（公司機戰場）未碰。

---

## 0711-C｜驗證（本機無 SO/RR 歷史單據 → TEMP 假資料範式）

```
環境現實：另一 session 常駐 dev server 佔 3000/3001、Next 16 .next/dev/lock 擋第二實例、
Chrome 擴充未連線 → UI 實走驗不了 → 退 API JWT 端對端驗證（常駐 server watch 熱載新碼、
先以無 token 401 確認新端點已載）。

測試料號 NX01PART0023891（03L 115 561 機油芯：報價 2/對應料 2/車型 1 都有真資料）
TEMP 插入：SO×2 + RR×2（doc_no 'SO/RR-TEMP-F2DRILL-*'、remark 'F2-DRILL-TEMP'）
登入：Y0005 TEMP 換雜湊（bcryptjs、nx-api 沒裝 bcrypt）→ POST /auth/login 取 token

四端點核對全過：
  sales-history    → TEMP SO 2 筆逐欄全對（日期新→舊/客戶/數量/單價/金額/狀態）
                     + 真報價 2 筆 + 建議售價 cost=383.08 / A=1297 / B=908
  purchase-history → TEMP RR 2 筆逐欄全對（含批號、INSPECTING/POSTED 狀態）
  related          → 真資料 2 筆（MAHLE/BOSCH 機油芯、庫存帶出）
  models           → 真資料 1 筆（VW T5 Transporter、1988–2017、fit=1 原廠）

還原全數確認：TEMP 單據刪除歸零；Y0005 雜湊還原＝快照原值逐字比對 t、
must_change_password 還原 true。
```

**驗證中三發現：**
1. ⚠️ **Y0005 雜湊曾被 0710 session 寫壞**：PowerShell 還原時 `$` 被塞反引號（60→63 字）、
   該帳號本機其實一直登不進去。本日修回快照原值（與 dump 逐字比對一致）。
   教訓：**含 $ 值（bcrypt 雜湊）一律走檔案傳遞（[IO.File]→psql stdin）、禁 shell 字串內插**。
2. ⚠️ **schema 觀察（純回報未動、轉 CTO）**：nx04_so / nx02_rr 的 currency_id
   預設值 'TWD' 是幣別代碼、但 FK 指向 nx01_currency.id（NX01CURR...）——
   預設值本身違反 FK、靠應用層每次明填才沒炸。
3. 插測試單據 tenant 別拿 nx99_tenant LIMIT 1（第一列 SYSTEM）；營運租戶=NX99TANT9900004。

---

## 0711-D｜收尾

```
Crown 拍板：feature/f2-drilldown → main merge（no-ff、4 commits + 本篇日誌）
```

**待辦（回公司機）：**
- [ ] F10 鍵瀏覽器攔截實測（capture preventDefault、攔不住回報換鍵）
- [ ] F8/F9/F10 三面板真資料視覺驗證（本機驗的是 API 層）
- [ ] currency_id 預設值 schema 觀察轉 CTO（上面發現 2）
- [ ] 0710 遺留：handoff/ 資料夾補推、員工主要倉 is_primary / 客戶預設出貨倉維護

---

## 0711-E｜案二：主檔詳細頁排版優化（feature/master-detail-layout、3 commits）

Crown 指示：主檔詳細資料排版一刀切不合用、每檔欄位不同要個別調。
拍板：全範圍（28 config 主檔 + 大四 Zoned）、機制先行再逐檔。
⚠️ 本案演進 2026-06-24「detail 統一 5 欄 F 方案」拍板 → 「統一底座 + 每檔個別調」（程式註解已記）。

```
commit 1  a329f5af  排版機制 + 車型打樣
  - EntityFieldDef 加 detailSpan（跨 2/3/full、窄螢幕降級）/ detailGroup（連續段分組、
    分隔線+小標、不重排欄位）/ emphasis（主識別欄瀏覽模式金色加大）
  - EntityMasterPage DetailPane：map 主體抽 renderDetailField、chunk 分組渲染、
    formRef 移外層（Enter 跳格鏈跨組不斷）；FormField 加 emphasis 樣式
  - 打樣：車型三組（識別 / 年式與規格 / 其他）

commit 2  c49bc087  27 個 config 主檔逐檔調整
  - 全檔：主識別欄 emphasis、說明/備註/地址/Logo 跨 2 格
  - 10 檔加分組（往來對象最有感：識別/聯絡/公司與稅務/分級與信用/付款條件/其他 6 組）
  - 唯一欄位序調整：職務 description 移組織後（inList:false、列表不受影響）
  - 沒動：PART_BRAND_MASTER（deprecated）

commit 3  30257402  大四主檔 Zoned 表單對齊基準
  - 零件/往來對象/倉庫：code/name emphasis + 規格/地址/備註跨 2 欄（瀏覽+編輯）
  - 使用者：userAccount/userName emphasis（51KB 大檔僅動瀏覽渲染一處）
  - 分區結構與欄位序不動；編輯模式行為零變化（純排版層）

驗證：tsc 全綠；6 代表頁（車型/往來對象/零件/倉庫/使用者/幣別）dev server
隨選編譯冒煙測試全 200；視覺由 Crown 瀏覽器實看後拍板 merge。
```

---

## 0711-F｜案三：F2 操作手冊（feature/f2-manual、2 commits）

```
31cc2e65  NX-MANUAL-08 F2 即時零件查詢 Ver 1.0
  - 七章：概觀（開關行為）/ 搜尋窗（三查法+鍵盤流）/ 主視窗（三欄、預覽 vs 跳搜）/
    F3~F10 八快捷 / 三入口情境 / 快捷鍵總表 17 條 / 限制與注意
  - 依實作逐欄核對：鍵位/欄位/筆數上限（500/100/50）/狀態三態判定/嵌入點按鈕文案 grep 驗實
  - 外殼樣式沿 NX-MANUAL-04；維護者 Hank

d84284e6  NX-MANUAL-00 總覽 v1.3 同步（8 本→9 本、9 處計數）
  - ⚠️ 編號衝突自查：08 原預留「系統方案售價」（6/5 已刪線暫緩）→ Crown 拍板 08 由
    F2 手冊接用、售價文件未來另行編號；總覽兩處加註、C 切割歷史描述保留（G.4）
```

---

## 0711-G｜案四：F5 進銷統計轉正（feature/f2-monthly-stats、3 commits）

Crown 拍板照提案做：F5 從「近 100 筆前端估算」升級為後端全量聚合 + 12 月進銷長條。
偉盟 11 下鑽對照至此僅剩 kit（需 schema、歸公司機）與相關單據（等 doc-shell 穩定）。

```
commit 1  35e433f0  GET :id/monthly-stats 聚合端點 + 前端 client/型別
  - $queryRaw 全量流水：12 月逐月 進(P 入)/銷(S 出)/其他出入（缺月 service 補 0）
    + 近 30/90 天窗口（全異動口徑、與改版前一致）
commit 2  466aa2e1  F5 面板轉正
  - 六格吃聚合（去「估」字尾/截斷警示/historyFetchedAt）+ 12 月進銷雙長條（進橘/銷綠）
  - F6 照舊吃 stock-history；全零空態訊息
commit 3  4a66427f  手冊 08 → Ver 1.1（F5 段改寫 + 進銷口徑說明）

驗證：TEMP 流水 5 筆（跨 3 月、含 1 筆調撥驗口徑）→ 端點數字逐項手算全對
  （out30=8/out90=16/in90=30/moves=3、12 格補 0、調撥入窗口不入銷長條）→ TEMP 全清
登入新姿勢：JWT_SECRET 簽 token（免動雜湊）；後段 UI 需登入時 TEMP 換雜湊、驗後還原=快照比對 t
⚠️ F5 視覺驗證入回公司機清單（本機 Browser 自動化管道不可用；另一 session server 已停、
   本 session 自起 3000/3001 留跑）
```

---

## 0711-H｜案五：收尾——死碼清理 + 家用機交接檔（feature/home-line-closeout）

```
107dae46  刪 CAR_BRAND_MASTER / PART_BRAND_MASTER 死碼（W6 品牌合併遺留）
  - 查證：redirect 早在 6/18 就加、deprecated 註解「保留避免編譯失敗」理由已消失
  - grep 零引用 + 四頁冒煙 200；§12 同源債三條件清理
bd236b75  docs/_team/home-machine-handoff-2026-07-11.md
  - 給公司機：驗證清單（真資料驗證 4 項 + 瀏覽器實測 2 項）/ 轉 CTO（currency_id）/
    0710 遺留 / 本機環境備忘 / 下一案指引
```

---

## 0711-I｜案六：NX-MANUAL-02 核心主檔手冊逐欄核對（2.7→2.8、feature/manual-02-audit、6 commits）

手冊 6/8 版 vs 一個月主檔變動全面核對、工法照 NX-MANUAL-08（逐欄對程式設定檔）。
開工前兩題 Crown 拍板：①權限章以「權限等級」為主寫（職務=組織標籤、舊頁標過渡）
②車型主檔=LITE 核心（PACKAGE-01 範疇收斂為料件車型適配）。

```
708a9b7d  c1 帳號與權限：權限等級新章 + 使用者補欄 + 職務語意改標籤（6/28 拆分軌）
4735bc8c  c2 產品與料號：零件對齊 6 月底拍板（基準料號可改/廠牌料號必填/刪舊料號/
          三維分類）+ 品牌代碼放寬 + 自訂群組改名 + 通用件刪專屬售價 + 車型新章
4d5acf98  c3 組織架構：五層倉儲 + 區域/貨架新章 + 倉庫（Zoned 欄位）/庫位重寫
aeb6f944  c4 交易對象：出貨→配送、補散客 L、補發票抬頭/每月結帳日（公司機 7/10 新欄）、
          六處字面對齊（僅收現金/總公司/個別毛利率/預設取貨倉/可同行調貨/服務區域）
2c37ace9  c5 系統設定小修 + Ver 2.8 定版（修訂紀錄 + 檔頭維護者 Hank）
a5a678a4  c6 PACKAGE-01 車型範疇收斂（引擎/變速箱/傳動/車體類型四字典 6/26 已取消）
```

定稿檢查：欄位字面逐欄抄自 config/zones、HTML div 配對 1041/1041。
⚠️ 誠實聲明：通用件成員操作細節對設定檔註解、未逐行讀 UniversalGroupPage 全檔。
```

---

## 0711-J｜案七：weimeng-p2 家機接手 + 三步驗證（公司機遠端失聯）

背景：公司機遠端連不上、Crown 指示全部工作移家機。盤點：origin/main 落後本機 2 commit
（公司機晨推 doc-shell-followups、已 ff 同步）；remote 未合併分支 5 條、唯一在飛＝
`feature/weimeng-p2`（3 commits、公司機 05:21 全數已推、無 git 面損失）。
公司機兩段 schema 變更（W5 四表加欄 3ba076a1 + 條碼子表 9ba2172b）均為
「db execute 直接進 DB、不產 migration」範式（migrate dev 壞、Crown 拍板）→ 家機可重建。

```
環境接手（依序全綠）：
  checkout feature/weimeng-p2（基於最新 main、領先 3 落後 0）→ pnpm install →
  本機 Docker DB 補差（scratchpad SQL：W5 四表 source_issue_report_id +
  nx01_part_barcode 全套 seq/fn/table/idx/FK、對齊 t4_part_photo 範式）→
  \d 逐欄核對 → prisma generate → nx-api / nx-ui tsc --noEmit 雙綠 → nx-api 起服
  8 條新路由（barcodes CRUD×4 + resolve + defaults + preview + apply）RouterExplorer 全掛

API 端對端 18 項全過（JWT 簽 token、Y0001=OWNER）：
  條碼 10：第一條自動預設/第二條非預設/跨料重複 409 可讀訊息/resolve 命中+未命中/
    搶預設換手/list 附料號品名快照/defaults 批量（無對照不回列）/另料首條預設/清理歸零
  調價 8：preview 逐欄算值（+10% INT：937→1031/656→722/491→540/29→32）/
    無 filter 無 confirmAll 400 防手滑/調幅 0 400/PCT 600% 超界 400/
    apply affected=2/apply 後現值=preview 承諾值（JS/SQL 口徑一致）/
    AMT -600 GREATEST(0) 負價保護/歸 0 後（未定價語意）不再被調
  稽核＋戳記：nx01_audit_log 3 筆摘要可讀、price_updated_at/by 正確落

UI 冒煙（token 注入 localStorage）：
  批次調價頁：搜「251 405 645B」→ 預覽 3 顆、現→新逐欄與 API 驗證期望一致、
    套用鈕帶顆數；條碼維護頁：表單/標籤列印區/空態提示全渲染
  ⚠️ 本機 Browser 截圖管道卡死（既知限制）、文字層驗證代替；未按套用（apply 已 API 驗）

還原全數確認：價格四價+戳記逐欄還原=備份比對 t、備份表刪、TEMP 條碼 0 殘留、
  測試稽核 3 筆刪；結論：三步 0 bug、一次全過
```

**待辦：**
- [ ] weimeng-p2 → main merge（等 Crown 拍板；push 遠端同）
- [ ] 標籤列印視覺（列印對話框/標籤排版）＝瀏覽器實測、回公司機清單
- [ ] 後天回公司機：`git status` 檢查未 commit 殘留（家機已重做部分直接丟棄、
      勿把公司機舊狀態推上）；公司機本地 DB 與家機同為 db execute 範式、無 migration 漂移
- [ ] 下一案：代購直送鏈盤缺口（P2 決策表「先盤缺口再定」、純盤點報告）
- [ ] ⚠️ 轉 CTO：db execute 範式下 Railway production 的 schema 補套 SQL 需固定流程
      （W5 四表加欄 + 條碼子表兩段、production 部署前必套）


---

## 0711-K｜案八：代購/直送鏈缺口盤點（偉盟檢視 2.7、純報告零改動）

Crown 拍板開盤（P2 決策表「先盤缺口再定」）。產出：
docs/_team/directship-chain-gap-survey-2026-07-11.md（三能力逐件 grep 核實）。

```
① 進貨綁銷貨：✅ 同行鏈全通且明細級（TiItem.sourceSoItemId 必填 + SoItem.tiId +
   Rfq(P).sourceSoItemId + Rr.tiId + Ap.tiId、雙向可追）；🛠 PoItem 綁 SO 無欄（grep 0）
② 帳款對象分離：PO.invoiceToPartnerId 欄在（T7）但「有欄無帳」——AP 產生器 3 支
   （from-po/rr/ti）全掛 supplierId、過帳鏈 0 處引用；銷售側/AR 無對稱設計（轉 CTO、
   與檢視 2.4 請款循環同題域）
③ 直送：PO 指送三欄全套（T7 含「直送不進倉」語意）；🛠 實體流缺整段——
   rr 模組 shipTo grep 0 處（過帳一律入實倉、帳實不符）、TI 無直送欄
   （同行直送客戶=恆迎最高頻情境、目前走不了單）
📕 原檢視 2.7「Rr 無 sourceSoId」表述可訂正（缺口在 PO 層不在 RR 層）

建議優先序：TI 直送+過帳流（中大、價值最高）→ AP 接付款對象（小中、併 NX05 請款）→
PoItem 綁 SO（小、先問頻率）→ SO 指送對象欄（小、低優先）
```

**待辦：**
- [x] 執行長拍板（授權 Hank 決策制）：缺口② → 0711-L 已完成；缺口③/SO 指送暫緩（無配套 UI=死欄）
- [x] 缺口①同行直送 轉 CTO 出規格 → docs/_team/to-cto-directship-spec-request-2026-07-11.md
      （自足文件：現狀事實/五設計題/六約束/期望交付/三附帶事項、執行長轉貼 Alex 用）

---

## 0711-L｜案九：AP 帳款歸戶（盤點缺口②「有欄無帳」收尾）

Crown 授權：「由你來決定、我只做最後的總驗收」。Hank 決策：
做缺口②採購側（欄位/填單 UI 已在、只缺過帳歸戶）；缺口①同行直送、銷售側帳款分離
留 CTO 出規格（庫存過帳語意設計題）；缺口③ PoItem 綁 SO 暫緩（無配套 UI=死欄）。

```
schema：Nx05ApLedger 加 billToPartnerId（可空 FK partner、null=同 supplierId）+
  關聯/reverse 三處；本機 DB db execute + prisma generate
service 三處：
  nx05-create-ap-from-po → AP 帶 po.invoiceToPartnerId（RR/TI 路徑維持 null：
    RR 無付款對象欄、TI 拍板「帳跟貨走」不立帳、語意不變）
  payment.service create → 付款單 partnerId 取 billTo ?? supplier
  ap.service AP_SEL → 加欄回傳
UI 兩處：ApRow 型別 + ApWorkbench 廠商欄「歸戶」副行（金色）、沖銷鈕 partnerId 取歸戶
新增 packages/db-core/prisma/sql/pending-production.sql：
  「db execute 範式」下待上 production 的 DDL 累積檔（W5 四表加欄 + 條碼子表 +
  本案 billTo、全冪等）——production 部署前必整檔跑過、解掉 0711-J 標的流程缺口

驗證：雙 tsc 綠；API E2E 10 項全過（A 組帶歸戶：AP.billTo=歸戶+supplier 不變+
  付款單掛歸戶；B 組對照：billTo=null+付款單掛 supplier；PO 全狀態鏈
  DRAFT→…→CONFIRMED 過）；TEMP 全清（PO/AP/paylog/audit 13 筆、庫表歸零）；
  AP 頁 UI 冒煙渲染無錯 0 console error
留意（未動、記錄語意決策）：dueDate 仍依 supplier 付款條件算（歸戶對象條件不覆寫、
  保守處理）；既有 AP 零筆免 backfill
```

**待辦：**
- [ ] Crown 總驗收本案 + 盤點缺口①（同行直送）轉 CTO 出規格
- [ ] production 部署時跑 pending-production.sql（整檔冪等）

---

## 0711-M｜案十：Crown 總驗收兩發現修正

Crown 實測回報：① SO 建單 400（warehouseId 驗證炸）② 選單只有銷貨模組開著。

```
發現①根因＝雙層 drift：
  - CreateSoDto.warehouseId 必填、但 service（Phase 3 commit 3a）早有
    dto→客戶預設倉 fallback——DTO 漏鬆綁、fallback 永遠跑不到
  - 建單面板文案承諾三層鏈「客戶預設倉→使用者隸屬倉→主倉」、實際只有一層
  修：DTO 加 @IsOptional；service 補齊三層（隸屬倉 isPrimary 優先、單倉隸屬次之、
  最後租戶主倉 isMain）；「都無」給中文可讀 400
  驗證：E2E 4 項全過（原場景不帶倉→fallback Z01 / 明示帶倉尊重指定 / 無效倉 400）、
  TEMP SO 2 張全清；⚠️ 過程發現 C0006 授信=F 凍結、建單被 CreditGuard 擋是正確行為
  （Crown 驗收挑客戶請避開凍結戶）
發現②根因＝選單旗標過時：menu-data.ts 五組 comingSoon 是 6/28 目標 IA 定案時標的
  「暫緩」、實作已跑到前面（單據頁全建、doc-shell 列印十張全通）
  修：開放 採購與進貨/庫存管理/會計財務/報表與分析 四組（子項 href 頁面逐一 glob 核實
  存在、個別未建項維持 pending 灰顯）；簽核作業維持暫緩（8 子項 7 個未建）
  驗證：雙 tsc 綠；首頁快捷卡四組轉開、僅簽核顯「即將推出」
```

⚠️ 工具紀律違規自報：驗證腳本改客戶 ID 用了 PowerShell -replace round-trip、
中文註解編碼壞掉（A046 再犯）——Write tool 重寫。教訓：含中文檔案一律 Edit/Write tool、
包含 scratchpad 暫存腳本。

---

## 0711-N｜案十一：選單重排 + 頂部選單全鍵盤（Crown 三拍板落地）

拍板：①同行調貨=業務動線移入銷售（到貨驗收留庫存）②保固留採購（求償對象=供應商）
③同行詢價面=銷售既有「詢價紀錄」（免搬）；分組原則=「誰在做事」、組內=作業時序。

```
menu-data 全檔重排（歷史註解保留）：頂層 銷售移採購前/簽核殿後；
  銷售=詢價紀錄→報價→報價紀錄→銷貨→同行調貨→銷退→促銷→折扣；
  採購=工作台首位→缺貨簿→詢價→採購單→特殊→進貨→進退→保固；
  基本資料七分類高頻提前（產品/往來對象/據點倉庫）
TopMenuBar 鍵盤軌：Alt+字母/↓ 開組聚焦首項、↑↓ 環狀（跳過建置中）、Home/End、
  → 進子層 ← 退子層還焦點、根層 ←→ 換頂層、Esc 關閉還焦點；
  滑鼠 hover 不搶焦點（e.detail 區分）；群組項補 onClick（原 hover-only=鍵盤斷點修復）
驗證：tsc 綠、頂層順序 DOM 核實；🖱 逐鍵實走待執行長真瀏覽器（情境表 G1/G2）
```

---

## 0711-O｜案十二：首頁改版 V1（Crown 拍板線框落地）

原則：「首頁＝我今天要做什麼」、不再是選單複製品。分期：V1 本案 / V2 角色個人化 / V3 主管儀表。

```
後端：nx08/dashboard/home/summary（新模組、對齊七儀表範式、JwtAuthGuard 全員可呼）
  9 個 count/aggregate Promise.all：未結報價(DRAFT/SENT)/待出貨(CONFIRMED/PICKING)/
  補貨中(transferStatus P,I)/逾期應收/待驗收(INSPECTING)/待撿(PK)/待包(PL)/
  7日內應付/今日銷貨額+單數（主管=OWNER/SYSADMIN 才回、其他 null）
  ⚠️ 庫存警戒卡緩列（part_stock_setting 無營運資料、等安全庫存維護上線）
前端：WorkbenchHome 重寫＝①三組待辦卡（點卡直達、非零橘/紅框）②快捷動作
  （F2 提示 + 數字鍵 1 報價/2 銷貨/3 客戶、輸入框內不攔）③主管今日數字
  ④公告（吃既有 bulletins、置頂標記）＋模組入口降級縮小列
驗證：雙 tsc 綠；端點數字 vs DB 手算全對（未結報價 11/11、其餘 0=快照現實）；
  首頁四區渲染 + 模組入口新順序 + 0 console error
```

---

## 0711-P｜案十三：G3~G5 鍵盤情境驗收 + Home/End 補洞

測法突破：本機 Browser pane 原生輸入打不進 React、改 `el.dispatchEvent` 直發
DOM 事件可完整驅動（記憶備忘已錄範式）。

```
G3 主檔頁熱鍵（幣別頁逐鍵）：Alt+F 搜尋+自動聚焦/Esc 收、Alt+A 新增+焦點落首欄、
  Enter 跳格（代碼→名稱）、Esc 退編輯、Alt+D 停用確認、Alt+1↔2 切分頁——全過 ✅
G4 對話框：焦點自動入框+鍵位提示（Esc/Enter）、開著時 Alt+A 不穿透
  （modal-stack 雙 guard 6/24 方案 A 實測有效）、Esc 關閉不誤動——全過 ✅
  📕 體感備註：頁面導覽浮層也掛 modal stack、首次進頁鍵盤被導覽接管=正確行為非 bug
  （追查過程一度誤判 DocWorkbench 全域鍵盤失效、真因=導覽層在 stack 上鎖背景鍵）
G5 表格導航（報價工作台 DocWorkbench 殼 + 主檔殼）：↑↓/Enter 全過 ✅；
  🛠 補洞：「焦點在列上」路徑 Home/End 缺（MasterTable.handleTableKey 只有 ↑↓/Enter、
  全域 handler 讓位形成死角）+ EntityMasterPage 全域也無 Home/End——兩處補齊、
  雙路徑重驗（全域 End→末列 / 列焦點 Home→首列+焦點跟隨）✅
```

情境表 G3/G4/G5 已勾（docs/_team/acceptance-test-scenarios-2026-07-11.md）。
G 組剩 G1/G2（選單鍵盤）＝執行長已實測過 ✅ → 鍵盤情境全數收官。

---

## 0711-Q｜案十四：B 組銷售流情境驗收（13 項 API E2E 全過、TEMP 全清）

```
B1 報價→銷貨（8 項）：建 2 行報價 → SENT → from-quote 轉單：明細帶行/單價逐欄
  （1000/500）/回連 quoteId/總額含稅 2625 手算對；price-intel 五組帶價
  （suggestedPrice/同客戶報價/同客戶成交/同級報價/同級成交）
B3 缺貨 G 分流（4 項）：addItem transferSourceType=G → transferStatus 自動 P、
  pending-transfer-lines 列出、create-ti 建同行調貨草稿（O0969）、
  SO 行回寫 tiId ——「銷貨缺貨→打給同行」全鏈 API 面通
B4（部分）：業務閘驗過＝SO 須 SHIPPED/INVOICED 才可開銷退（草稿擋、可讀訊息）；
  完整退貨過帳循環（出貨→退→帳）留 E 組財務流同批（需出貨過帳、動 ledger）
業務閘記錄兩條（正確行為、手冊素材）：報價草稿不能轉單須先寄出；草稿銷貨不能退
TEMP 清理：3 報價/2 銷貨/2 調貨（含第一輪中止殘留）+ 明細 + 稽核 9 筆 → 歸零核對
```

---

## 0711-R｜案十五：E 組財務流情境驗收（23 項行為 + 實體帳 3 核對、TEMP 全清全還原）

```
E1 出貨→應收→收款（9 項）：SO 建單/加行（庫存足→補貨 C）→ CONFIRMED→PICKING→
  SHIPPED：庫存 549→547（流水 O 出 2）、自動 AR 210 含稅 → 收款草稿→過帳 →
  AR paid 210/balance 0/PAID
B4-full 銷退全循環（6 項、收掉 B 組尾巴）：對 SHIPPED SO 退 1 顆 → 未標好壞品被正確擋
  （可讀訊息）→ 倉管標 G → 過帳：庫存 +1（流水 I 軌、庫值差=1 顆成本 25.31 分毫不差）、
  自動開折讓 AL-202607 100 元
E2 應付分段沖銷（5 項）：PO 確認→AP 52.5 → 付 30（PARTIAL、balance 22.5）→
  付 22.5（PAID、balance 0）
E3 月關帳（5 項）：關 2026-06（OPEN→CLOSED）→ 期內收款被鎖（訊息含解鎖指引、品質佳）
  → CLOSED→REOPENED → 鎖確實解除

發現四條（零 bug、皆為口徑/一致性題）：
🛠 轉 CTO：①直接付款過帳不寫 settlement 明細列（沖銷歷史頁明細空、總額有數；
  明細現只在票據/多單沖銷流寫）②deliveryType=D 無配送地址不擋（schema 註解必填、
  建單/出貨放行、DN 靜默不建）
⚠️ 觀察：③已付清 AR 可建收款草稿（過帳有把關、UI 提示候選）④折讓額=未稅小計
  （退 1 顆單價 100 → 折讓 100、含稅 105）、退款口徑請 CTO 確認
📕 業務閘記錄：銷退過帳前每行需倉管標好/壞品 G/B

TEMP 清理全還原：SO/SR/PO/AR/AP/收付款 5 筆（含期間鎖測試殘留 2 筆草稿）/折讓+明細/
  關帳單/流水 2 筆 + 稽核 23 筆刪；庫存餘額自備份表還原逐欄比對 t、殘留 0 核對
```
