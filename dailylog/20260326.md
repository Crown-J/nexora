# 2026-03-26 工作日誌

> 以下區塊請依**當日實際工作**填寫；勿將任務說明中的範例文字當成已完成事項。

## 專案資訊
- 專案：NEXORA
- 分支：（當日主要開發分支）
- 開發者：Crown Lin（Hank 執行）

## 今日完成事項
1. **Grid LITE 後端資料庫整備（seed 第一階段）**  
   - 檔案：`packages/db-core/prisma/seed.ts`  
   - 內容：`nx99_plan` 四方案（NEXORA-LITE／PLUS／PRO／ENTERPRISE，欄位對齊商業規格表）；`nx99_product_module` 21 筆 SKU（LITE／PLUS／PRO 各 7，`applicable_plan_code` 與 `plan.code` 同命名空間，`billing_type=F`）；`nx99_product_module_map`（SKU → `NX00`～`NX04` 等）；`DEV-INNOVA` 的 `nx99_subscription`（方案 NEXORA-LITE、含標配明細 + 一筆加購測試）。  
   - 依據計畫：`lite_資料庫整備_cff7feee.plan.md`（試算表 21 筆未在 repo，模組列為可開發用主檔，之後可換成 PM 正式表）。  
   - 驗收：重跑 seed 後以 `admin` 登入，JWT `planCode` 應為 `NEXORA-LITE`。

2. **NX02～NX06 主檔資料表與欄位說明（對應設計 NX02_Dept～NX06_Product）**  
   - 設計對齊：原試算表 `CompanyID` → `tenant_id`（`nx99_tenant`）；建立／修改人員 → `created_by`／`updated_by`（`nx00_user.id`）；主鍵仍為系統產生 `VARCHAR(15)` 與 `gen_nx02_dept_id` 等函式。  
   - Prisma：`packages/db-core/prisma/schema.prisma` 新增 `Nx02Dept`、`Nx03Emp`、`Nx04Unit`、`Nx05Category`、`Nx06Product`，各 scalar 欄位均有 `///` 說明；`Nx99Tenant`、`Nx00User` 已接關聯。  
   - Migration：`20260326203000_add_nx02_nx06_tables`（建表、FK、每欄 `COMMENT ON` 繁中說明）。  
   - 既有表 DB 註解：`20260326204000_add_pg_comments_legacy_tables`（`nx00`／`nx01`／`nx99` 全表欄位補 `COMMENT ON`）。  
   - `schema.prisma` 檔頭與 `Nx99Tenant` 部分欄位補上說明註解。  
   - 本機需：`prisma migrate deploy` 後再 `prisma generate`。

3. **四模組占位頁 + 側選單 + 全表測試資料 seed（已實作）**  
   - 計畫：`模組頁與全表_seed_818f10b0.plan.md`  
   - 前端：`apps/nx-ui/src/app/dashboard/nx01`～`nx04/page.tsx`；共用 `ModulePlaceholderPage`（`features/layout/ui/ModulePlaceholderPage.tsx`）；`menu.nx01.ts`～`menu.nx04.ts` 與 `side-menu.ts` 對應 `nx01`～`nx04`；`modules.ts` 將 nx03 Tab 文案改為「銷售作業」；`dashboard/page.tsx` 檔頭註解改為正確路徑（仍 redirect `/home`）。  
   - 後端：`packages/db-core/prisma/seed.ts` 於 DEV-INNOVA 寫入 `nx04_unit`／`nx05_category`→`nx02_dept`→`nx03_emp`→`nx06_product`；`nx01_rfq` + 兩筆 `nx01_rfq_item`（`doc_no=R-DEV2026-00001`）；`nx01_po` + 一筆 `nx01_po_item`（`doc_no=I-DEV2026-00001`，倉庫 Z01、庫位 A-01-01）。  
   - 驗收：`npx prisma db seed`（`packages/db-core`，需 `DATABASE_URL`）執行成功。

4. **NX00 模組根路徑 404**  
   - 補上 `apps/nx-ui/src/app/dashboard/nx00/page.tsx`（基本資料／主檔說明）；`ModulePlaceholderPage` 支援 `showDevNotice={false}`；`menu.nx00.ts` 新增「模組首頁」連至 `/dashboard/nx00`；`dashboard/page.tsx` 註解與行為區隔（`/home` vs NX00）。

5. **進貨/銷貨流程落地（NX01/NX03 + QUOTE/SO + 庫存 MVP）**  
   - DB/Schema：新增 `nx07_quote`, `nx07_quote_item`、`nx08_sales_order`, `nx08_sales_order_item`；並部署 migration `20260326070110_add_nx07_nx08_quote_sales`。  
   - 狀態機/轉單規則：新增 `apps/nx-api/src/shared/workflows/nx01-nx03-workflow.ts`（RFQ/QUOTE/PO/SO 狀態轉移）。  
   - Backend：  
     - 新增 `nx01` 採購域（`/nx01/rfq`、`/nx01/rfq/:id/to-po`、`/nx01/po/:id/post`）：`apps/nx-api/src/nx01/**`。  
     - 新增 `nx03` 銷售域（`/nx03/quote`、`/nx03/quote/:id/accept`、`/nx03/sales-order/:id/ship`）：`apps/nx-api/src/nx03/**`。  
   - Frontend（流程頁）：  
     - `/dashboard/nx01/stock-replenishment`：RFQ → 產生 PO → PO 過帳。  
     - `/dashboard/nx03/customer-sales`：RFQ → QUOTE（markup 計算）→ accept（自動建 PO+SO）→ PO 過帳 → ship SO。  
   - 庫存 MVP：新增 `nx09_stock_balance`/`nx09_stock_txn`，並將 `PO 過帳` 寫入入庫異動、`SO ship` 寫入出庫異動（不足庫存時阻擋出貨）。

6. **前端流程頁 UI 調整（NX01/NX03）**  
   - 欄位全中文化：`/dashboard/nx01/stock-replenishment` 與 `/dashboard/nx03/customer-sales` 的所有輸入/標籤。  
   - 外鍵改為可關鍵字搜尋的下拉：料號/供應商/客戶/庫位（顯示 `CODE+NAME`，送出仍為內部 `id`）。  
   - 單據編碼規則預設顯示：依 `R/P/Q/S + 年月 + 倉庫代碼 + 5 碼流水號`（倉庫載入後自動帶入前綴組合）。

## 討論與決策（Q&A）
無

## 明日待辦
- [ ] 依產品優先序接續各模組實際業務 UI（詢價／進貨、庫存、銷售、財務）

## 遇到的問題與解法
| 問題 | 解法 |
|------|------|
| 點 Top Tab「基本資料」或開 `/dashboard/nx00` 出現 404：該路徑下僅有 `nx00/user`、`nx00/part` 等子路由，**缺少** `nx00/page.tsx`，App Router 不會為資料夾自動產生索引頁。 | 新增 `dashboard/nx00/page.tsx` 作為 NX00 模組入口；左側選單補「模組首頁」→ `/dashboard/nx00`；`ModulePlaceholderPage` 增加 `showDevNotice`（NX00 關閉「開發中」提示，因主檔子頁已存在）。 |
| 新增 `Nx07Quote/Nx08SalesOrder` 等 Prisma models 後 `npx prisma generate` 失敗：提示各 relation 缺少 opposite relation（例如 `Nx07Quote.tenant`/`Nx00Partner`/`Nx01Rfq` 等）。 | 補齊關聯的 reverse relation 欄位：在 `Nx99Tenant`、`Nx00Partner`、`Nx01Rfq`、`Nx01RfqItem`、`Nx00Part`、`Nx00Warehouse`、`Nx00Location`、`Nx07Quote`、`Nx07QuoteItem`、`Nx08SalesOrder`、`Nx08SalesOrderItem` 間建立對側欄位，並調整成允許互相關聯後再次生成。 |
| `prisma migrate dev --create-only/--shadow` 過程失敗（shadow database replay）：錯誤指向先前 migration 中 `nx02_dept` 的表尚未建立就執行 `ALTER TABLE`。 | 修正既有 quote/so migration 檔內的 `ALTER TABLE nx02_dept/nx03_emp/... updated_at` 段落，避免影響前置 migration replay；並在後續 inventory（nx09）遷移改以手動 migration SQL 方式部署，以避免 shadow DB 機制卡住。 |
