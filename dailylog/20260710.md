<!-- dailylog/20260710.md -->
# 2026-07-10（週五）— 家用機線開張日：新機建置從零到綠 + F2 改版 §11 全五步單日完成

> 本篇＝**家用機線（F2 改版、feature/f2-redesign）**；公司機線同日進度見 `docs/_team/session-handoff-2026-07-10.md`（銷售模組軌、22 commits 未 push）
> 分支：`feature/f2-redesign`（HEAD `2f40dc9f`、7 commits、已推 origin）
> 工作時段：晚間——機器盤點/建置 → 快照還原+手冊第 6 節驗證 → F2 Step 0 回報 → Step 1~5 逐步 commit → F7 拍板收尾
> 六月清理後 dailylog 首篇（資料夾重建於 CLAUDE.md §十四規定位置）

---

## 0710-A｜新開發機建置（照 docs/_team/new-dev-machine-setup.md 走）

```
機器盤點（與手冊假設的落差）：
  - repo 已在 C:\nexora 但落後 origin/main 1413 commits → fast-forward（手冊也是這次才拿到）
  - PostgreSQL 非原生安裝＝Docker 容器 nexora-postgres（postgres:16、port 5433、跑了 2 個月）
    容器內超級使用者是 nexora、沒有 postgres 角色 → 手冊的 restore-dev-snapshot.ps1
    假設原生 psql/pg_restore、本機改用 docker exec 在容器內還原
  - Node v24.13.1 ≠ 手冊要求 v22 → Crown 拍板對齊：提權移除 v24、裝官方 v22.15.0
    （與公司機完全同版）、工具鏈重驗全綠
  - 4 個 .env 有 3 個已在且指向 localhost:5433；缺 apps/nx-ui/.env（無機密）依 example 建
  - pnpm 10.29.3 ✓ / git 身分 ✓ / .env.railway 正確不存在 ✓

快照還原（nexora-dev-snapshot-20260710.dump、12.6MB）：
  - DROP 舊 nexora_core（2 個月前空 schema）→ 容器內 pg_restore → 
    11 萬料號 / 47.7 萬庫存餘額 / 4,058 夥伴 / 162 使用者 / 5 倉 / 租戶 TW-100001

手冊第 6 節驗證清單全過：
  - nx-api / nx-ui 啟動無錯、真實登入流程（含首登強制改密導頁）、
    F2 查詢有料有庫存（0 092 S57 177 → BOSCH 電瓶、總量 74）、tsc 綠
  - 測試帳號密碼文件查不到（恆迎真實員工帳號）→ 備份 Y0005 雜湊暫改驗登入、
    驗完雜湊 + must_change_password 一併還原
  - 發現：login API 對 must_change_password=true 正常發 token、強改密由 dashboard 導頁攔
    （與 login/page.tsx 註解一致、行為正常）

Claude Code 環境：
  - repo 自動生效（CLAUDE.md / hooks）✓；插件用 ~/.claude/settings.json 的
    extraKnownMarketplaces + enabledPlugins 宣告（gsap-skills + official）非互動安裝
```

手冊回饋（依「附：本手冊的維護」回報缺口）：
1. §1 應註明「已有 Docker PG 的機器」路徑（restore 腳本假設原生安裝、埠 5432 vs Docker 5433）
2. §4 快照不含 nx03_stock_ledger／compat group／客戶 default_warehouse_id／is_primary
   —— 影響面比「前次售價」更廣、F2 改版的水位條/本倉 pin/F5F6 本機都是空態
3. §5 插件可用 settings.json 宣告免手動 /plugin
4. 交接 §8 說的 handoff/ 資料夾（design-tokens.md、key-components、截圖）**沒在 repo**、公司機補推

---

## 0710-B｜F2 Step 0：兩項資料依賴 grep 驗證（結論：零 schema 需求）

```
1. 客戶主檔「預設出貨倉」→ 已存在且前後端全接好
   - Nx01Partner.defaultWarehouseId（NX04-IMPL-01 Phase 1 M1 加的、註解明寫 SO 建單自動帶入）
   - partner dto/service 已收、PartnerFormZoned 可維護、
     CustomerPicker / InstantQuoteDialog 已在用
2. 使用者「隸屬倉」→ 2026-05-26 audit 標的缺口已於 6/22 補掉
   - Nx01UserWarehouse 完整（isActive + isPrimary、員工多歸倉+一主要倉）
   - nx01/user-warehouse 後端模組 + 前端 endpoint/UI 都在
```

Crown 確認後開 `feature/f2-redesign` 動工。

---

## 0710-C｜F2 改版 §11 Step 1~5（每步 commit、每步瀏覽器實測）

```
Step 1  80913671  視窗 2 預設頁簡化
  - 中欄「庫存狀態」→「公司總存貨」：大狀態（有貨可出/低於安全/缺貨）+
    總可出/總庫存/在途 + 水位條（安全─現量─最高、各倉 stock_setting min/max 加總、
    未設定退化只畫現量）
  - 各倉分布表移除（§3 拍板）；基本資訊加大（左欄 1fr→1.25fr、縮圖 220→280）
  - 驗證：三狀態實測（暫插安全量 100/最高 200 設定、驗完刪）

Step 2  37033d3c  視窗 1 搜尋入口收斂
  - 五查法 → 料號（預設）/ 品名+車型 / 進階（Alt+1~3）
  - 廠牌/族群/分類/含停用全收進「進階」（AND 組合、能力沒少）；淨 -13 行
  - 驗證：品名「電瓶」200 筆、進階 BOSCH 500 筆上限警示

Step 3  c83f0692  庫存圖像化
  - 「各倉分布」可展開區（預設收合、遵守 §3）：橫向長條 ∝ 現有量、
    可出綠+不可出紅分段、空倉折疊沿用、本倉（isPrimary）pin 頂+徽章、軟上限 8 倉內捲
  - 後端：part-search stock-summary 各倉列 +isMine/isPrimary（F2 自有模組、不動 schema）
  - 驗證：5 倉料號長條比例精確（108→100%…15→14%）、pin 壓過排序

Step 4  58360da1  四快捷鍵框架（取代偉盟 11 彈窗常用集）
  - F3 可替代（只露 料號/廠牌/庫存、§5 拍板）/ F4 報價（既有事件）/
    F5 周轉率（無單料號 API → stock-history 近 100 筆前端估算、標注基礎+截斷警示）/
    F6 出入庫（異動表、docType 中文映射）
  - ⚠️ F3 原綁即時詢價 → 依交接改綁（見 0710-D 拍板）
  - 驗證：暫插 8 筆 ledger（F2-STEP4-TEMP、驗完刪）、F5 統計手算核對全對；
    順手抓到 KpiTile toFixed(0) 把 1.6 吃成 2 → +exact 參數修掉

Step 5  3a7ecca0  三入口情境接線（一個模板、三種入口、銷售為錨）
  - F2EntryContext + openPartQuickSearch({entry})；情境活在 Global 層、退回再進不丟
  - 銷售錨定：報價選客戶 → nx-f2-context-warehouse 事件 → 視窗 2「客戶倉」徽章 +
    各倉自動展開 + 客戶倉 pin 頂（優先於本倉）—— Step 0 驗的 defaultWarehouseId 落地
  - 採購入口：採購需求單工具列「料號查詢 F2」；倉管入口：庫存查詢工具列（自動展開各倉）
  - 驗證：C0004 暫設預設倉 Z02（驗完還原 NULL）、全流程走通：
    選上永汽車 → Z02 pin 頂壓過庫存最多的 Z01 ✓
```

---

## 0710-D｜拍板紀錄 + 收尾

```
2f40dc9f  即時詢價改綁 F7（Crown 拍板）
  - F3=可替代照交接不變；視窗 2 鍵盤面定版：
    F3 可替代 / F4 報價 / F5 周轉 / F6 出入庫 / F7 詢價 + Space 放大 + Esc 退回
```

**待辦（過夜）：**
- [ ] main merge 由 Crown 拍板（feature/f2-redesign 7 commits + 本篇日誌）
- [ ] 最終驗證回公司機：銷貨比價、水位條完整型態、本倉 pin、F5/F6 需真實歷史資料
- [ ] 公司機補推 handoff/ 資料夾（design-tokens.md 等）
- [ ] 公司機使用者維護把員工主要倉（is_primary）設起來、客戶主檔補預設出貨倉——
      不然 Step 3/5 的 pin 效果上線也看不到
- [ ] 手冊缺口 4 項（0710-A 末）補進 new-dev-machine-setup.md

**本機開發資料現況備忘（下次驗證前先看）：**
快照裡 nx03_stock_ledger／compat group／客戶 default_warehouse_id 全空、
nx01_user_warehouse.is_primary 全 false —— 驗相關功能要插標記 TEMP 假資料、驗完還原。
