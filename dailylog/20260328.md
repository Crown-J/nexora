# 2026-03-28 工作日誌

> 以下區塊請依**當日實際工作**填寫；勿將任務說明中的範例文字當成已完成事項。

## 專案資訊
- 專案：NEXORA
- 主線：`main`（本日已併入 `feature/home-modals-settings` 之首頁改版）
- 開發分支：`feature/base-master-hub`（主檔 `/base`、使用者主檔 `/base/users`、`/user` 別名、`HomeLandingChrome` 與 Dock 調整；撰寫日誌時部分檔案仍為工作區未提交狀態，併入 main 前請補 `git add`／`commit`）
- 開發者：Crown Lin（Hank 執行）
- **紀錄約定**：程式或規格有任何更動，須同步寫入 `dailylog/` 當日檔案（例：`dailylog/20260328.md`），避免分支與口頭決策未留痕。

## 今日完成事項

1. **首頁「建立事件」表單調整**（`apps/nx-ui/src/components/home/calendar-details.tsx`）  
   - 移除標題下方說明文字（「事件日期為目前選取之日期：…」）。  
   - 欄位改為：標題、副標題、內容、地點、開始／結束日期時間（`datetime-local`）、**全天**核取方塊，以及類型。  
   - 勾選「全天」時僅顯示開始日期與結束日期（`type="date"`），不顯示時間欄位；事件列表與詳情以 `startAt`／`endAt` 與區間重疊判斷顯示於行事曆當日。  
   - 種子事件補上 `startAt`／`endAt` 以利區間與詳情顯示。

2. **首頁「新增公告」表單調整**（`apps/nx-ui/src/components/home/bulletin-board.tsx`）  
   - 移除標題下方說明「測試用表單，資料僅存在於此瀏覽階段。」  
   - 「內容」文字區加大（`rows={12}`、`min-h-[280px]`）以利長文。

3. **TopBar 通知：查看全部通知**（`apps/nx-ui/src/components/home/top-bar.tsx`）  
   - 點擊下拉選單中的「查看全部通知」開啟**全部通知**視窗（`Dialog` + `ScrollArea`），內容與下拉預覽相同（測試資料）；單筆通知列點擊行為未改，保留後續導頁擴充。

4. **UI 微調（同日後續）**  
   - 深色模式：`globals.css` 為 `select.nx-native-select` 設定 `color-scheme`（非 `html.light` 為 `dark`），修正類型下拉選單選項對比。  
   - 建立事件／新增公告：`Dialog` 加寬、雙欄版面；左欄欄位、右欄大面積「內容」；建立事件之非全天改為「開始／結束日期」+「開始／結束時間」分欄；核取文字改為「整天」。  
   - 全部通知視窗：通知列加上 `hover` 背景過渡。

5. **首頁收尾（同日）**  
   - **對話框寬度**：`DialogContent` 預設 `sm:max-w-lg` 會蓋掉自訂 `max-w-[…]`；改預設為 `sm:max-w-[min(92vw,32rem)]`，建立事件／新增公告改以 `sm:max-w-[min(96vw,80rem)]` 覆寫，大螢幕才真正變寬。  
   - **TopBar 固定**：`home-shell` 的 `overflow-hidden` 使 `sticky` 失效；`HomeTopBar` 改為 `fixed top-0 … z-50`，`home/page.tsx` 的 `<main>` 以 `pt-20`／`lg:pt-[calc(4rem+1.75rem)]` 預留頂欄高度。  
   - **依賴與元件**：`nx-ui` 新增 `@radix-ui/react-dialog`／`tabs`／`label`；新增 `components/ui` 之 `dialog`、`tabs`、`input`、`label`、`textarea`；新增 `hooks/useNxThemeMode.ts`。  
   - **Git**：分支 `feature/home-modals-settings` 已合併至 `main`（首頁本階段功能視為完成，後續再接 API 與通知導頁）。

6. **Landing 外殼共用**（`apps/nx-ui/src/components/home/home-landing-chrome.tsx`）  
   - 抽出星空背景、`Dock`／`MobileDock`、主內容區 `main` 預留與首頁一致之 padding。  
   - `apps/nx-ui/src/app/home/page.tsx` 改為包在 `HomeLandingChrome` 內，降低與其他登入後 landing 頁（如主檔）視覺漂移。

7. **主檔入口 `/base`**（`apps/nx-ui/src/app/base/page.tsx`、`master-cards.ts`）  
   - 八類主檔以 **glass 卡片**＋mock 摘要呈現；卡片 **hover** 縮放／陰影／邊框；倉庫／庫位單卡雙連結。  
   - 與 `/home` 相同 **session**（`useSessionMe`）與 **TopBar／Dock**。  
   - `apps/nx-ui/src/components/home/dock.tsx`：**主檔**導向改為 **`/base`**，並修正 `isDockActive` 涵蓋 `/base` 與子路徑。

8. **主檔子路徑占位**（`apps/nx-ui/src/app/base/[segment]/page.tsx`）  
   - 合法 segment 顯示建置中說明；非法路徑顯示找不到；連回 `/base`；外殼與 hub 一致。

9. **使用者主檔 `/base/users` 與 `/user` 別名**  
   - `apps/nx-ui/src/app/base/users/page.tsx`：Landing 外殼＋返回主檔總覽。  
   - `apps/nx-ui/src/features/base/users/BaseUserMasterView.tsx`：**左欄**上為關鍵字／職務即時篩選（無搜尋鈕），下為列表（新增、批次停用／啟用、表頭排序與欄位篩選）；**右欄**表單明細、檢視／編輯／儲存／取消。  
   - `apps/nx-ui/src/features/base/users/mock-data.ts`：測試用使用者與職務列表（未接 API）。  
   - `apps/nx-ui/src/app/user/page.tsx`：伺服端 **`redirect('/base/users')`**，對應「使用者主檔」捷徑路徑。

10. **使用者主檔：分頁、稽核欄位、mock 擴充與每頁筆數**（`BaseUserMasterView.tsx`、`mock-data.ts`）  
   - 列表工具列除新增／停用／啟用外，新增 **上一頁／下一頁**、**第 x / y 頁**、**共 N 筆 · 本頁 M 筆**；**全選**僅作用於目前頁。  
   - **每頁最多 10 筆**（`PAGE_SIZE = 10`）。  
   - 篩選條件變更時重設為第 1 頁；以 `filterKey`／`safePage` 與 effect 避免分頁狀態競態。  
   - `BaseUserRow` 增 **建立人員／時間、最後修改人員／時間**（`createdByName`、`updatedAt`、`updatedByName`）；mock 使用者擴為 **16 筆**；匯出 **`MOCK_CURRENT_OPERATOR_NAME`** 模擬目前操作者。  
   - 右側明細新增 **「登入與稽核」**：最後登入時間、建立人員、建立時間、最後修改時間、最後修改人員（唯讀）；**編輯時**帳號與上述稽核欄不可改；**儲存**與**批次啟用／停用**會更新 `updatedAt`、`updatedByName`。  
   - 同日補登：**DAILYLOG** 於本檔 `20260328.md` 追記主檔與使用者畫面事項；並訂「**任何更動須記錄 dailylog**」於專案資訊。

11. **品牌與 PWA／分頁圖示（NEXORA GRID）**（`apps/nx-ui/public/` + `manifest.ts`、`layout.tsx`）  
   - 設計匯出並放入 `public/`：`favicon.ico`、`icon-16.png`、`icon-32.png`、`icon-192.png`、`icon-512.png`、`apple-touch-icon.png`（使用者已確認規格無誤）。  
   - `manifest.ts`：`name`／`short_name` 改為 **NEXORA GRID**；`icons` 以 **192／512 PNG**（`any` + `maskable`）為主，並保留 `pwa-icon.svg` 作向量備援。  
   - `layout.tsx`：`metadata.title`、`appleWebApp.title`、`description` 對齊 **NEXORA GRID**；`icons` 串接 favicon、多尺寸 PNG 與 **apple-touch-icon.png**。

12. **Vercel Monorepo 建置**（`vercel.json`）  
   - 於 repo 根目錄新增 `vercel.json`：`buildCommand` 為 `pnpm --filter nx-ui build`、`installCommand` 為 `pnpm install`、`outputDirectory` 為 `apps/nx-ui/.next`、`framework` 為 `nextjs`。  
   - 檢查根目錄 `package.json` 第 2 行：內容為 `  "name": "nexora-monorepo",`，第 9 字元為 `name` 鍵後之 `:`，屬正常 JSON；檔首無 BOM，換行為 CRLF。

## 補充／待辦
- 首頁行事曆事件、公告、通知列表、設定表單仍為**前端測試資料**，未接後端 API。  
- 未完成訂單區塊暫維持 mock，待採購／銷售模組串接。  
- 通知單筆點擊導頁待規格後實作。  
- `/base`、`/base/users` 與占位子頁皆為**前端 mock**；併線前請將 `feature/base-master-hub` 上變更整理提交（`app/base/`、`app/user/`、`features/base/users/`、`home-landing-chrome.tsx`、`home/page.tsx`、`dock.tsx` 等）。

## 討論與決策（Q&A）
無

## 明日待辦
- [ ] 依產品優先序安排後端串接或下一模組。  
- [ ] 提交並（若時程允許）發 PR／合併 `feature/base-master-hub` 至 `main`；續做主檔其他卡片對應畫面。

## 遇到的問題與解法
| 問題 | 解法 |
|------|------|
| TopBar `sticky` 捲動後消失 | 父層 `overflow-hidden` 破壞 sticky；改 TopBar `fixed` + main 上內距預留。 |
| 建立事件 Modal 寬度怎麼調仍像 512px | `DialogContent` 的 `sm:max-w-lg` 覆寫無 `sm:` 前綴的 `max-w`；寬版對話框改加 `sm:max-w-[min(96vw,80rem)]` 並鬆綁預設。 |

