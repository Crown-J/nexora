# 2026-04-02 工作日誌

## 專案資訊
- 專案：NEXORA
- 開發者：Crown Lin（Hank 執行）
- 範圍：`packages/db-core` migration／seed；`apps/nx-ui` 使用者／職務／零件／汽車廠牌／零件廠牌主檔與 shell（含主檔列表勾選／啟用欄樣式）；`apps/nx-api` auth；**本機 Docker DB**；**User API／主檔欄位**

## 今日完成事項

1. **公司環境 DB：`prisma migrate deploy`（對齊目前 `schema.prisma`／migration 目錄）**（`packages/db-core`）  
   - **背景**：昨日已更新 `schema.prisma` 並於家中本機／環境 **`migrate` 成功**；公司機器所連之 **Supabase**（`packages/db-core/.env` 之 **`DIRECT_URL`**，經 **`prisma.config.ts`** 供 CLI 使用）尚未套用後續 migration。  
   - **執行**：於 `packages/db-core` 執行 **`pnpm exec prisma migrate deploy`**（由該目錄 `.env` 載入連線，無需手動覆寫 `DATABASE_URL`）。  
   - **本次新套用**：`20260401120000_add_nx00_data_dict_tables`、`20260402180000_align_nx00_to_nx00_field_csv`、`20260403100000_nx99_tenant_name_en`。  
   - **結果**：**All migrations have been successfully applied**（migration 目錄共 9 筆，其餘 6 筆先前已存在於該庫 `_prisma_migrations`）。  
   - **備註**：若另有本機 **`nexora_core`** 或其他連線，需以**該環境**對應之 `.env` 分別執行 **`migrate deploy`**。

2. **使用者主檔頁面**（`apps/nx-ui`）  
   - **`/base/user`**：移除 **`BaseMasterPageHeader`** 說明文案（mock 提示）；**`BaseMasterPageHeader`** 之 **`description`** 改為**選填**，未傳則不渲染段落。  
   - **`BaseUserMasterView`**：職務篩選由**單選下拉**改為**按鈕＋下拉複選**（**`DropdownMenuCheckboxItem`**）；**清除篩選**還原為顯示全部；**空選＝全部**、有選則 **OR** 比對 **`jobTitle`**。

3. **Seed：重置使用者、CYTIC 租戶與恆迎員工**（`packages/db-core/prisma/seed.ts`）  
   - **`purgeUsersExceptAdmin`**：刪除非 **admin** 之 **`nx00_audit_log`（actor）**、**`nx00_user_role`**、**`nx00_user`**（保留 **`username=admin`**）。  
   - **admin**：密碼改為 **`Nexoragrid2026`**（bcrypt）；租戶仍 **DEV-INNOVA**；綁定 **ADMIN** 角色。  
   - **租戶 `CYTIC`**：**恆迎企業有限公司**／**CYTIC ENTERPRISE**（**`nx99_tenant.code=CYTIC`**）。  
   - **恆迎使用者**：來源 **`prisma/seed-data/cytic-employees.txt`**（Tab；**帳號、姓名、離職**）；**離職=TRUE** → **`is_active=false`**；預設密碼 **changeme**；主職務 **SALES**。（※ 初版曾誤用他圖之 **7001–7162**，已於**項目 6**改為正確 **Y 編號** 帳號。）  
   - **訂閱**：**CYTIC** 另建與 DEV 相同方案之 **`nx99_subscription`**（**seats=200**，明細結構與 DEV **NEXORA-LITE** 一致）。  
   - **移除**：舊 **MOCK 七人**、**`test_user`** 種子；**`SALES`／`WH`** 角色改於檔案前段與 **ADMIN** 一併 **upsert**。

4. **ADMIN 跨租戶（JWT／登入）**（`apps/nx-api`）  
   - **`AuthService.login`**：若使用者具 **ADMIN** 角色，簽發之 JWT **不帶** **`tenantId`／`tenantCode`／`planCode`**（且不查租戶訂閱寫入 **plan**）。  
   - **`JwtStrategy.validate`**：若 DB 角色含 **ADMIN**，**`req.user`** 之 **`tenantId`／`tenantCode`／`planCode`** 強制為 **`null`**（與登入簽發一致，舊 token 重新驗證後亦同）。  
   - **效果**：使用者列表 API 本即**未**依租戶過濾；**建立使用者**時可於 **body** 傳 **`tenantId`**（**JWT 無租戶時為必填**），平台 **admin** 可維護各租戶帳號。

5. **改回本機 Docker PostgreSQL（停用 Supabase 連線設定）**  
   - **`infra/docker/docker-compose.yml`**：維持 **postgres:16**，**`nexora`／`nexora123`／`nexora_core`**，對外 **5433**。  
   - **`.env.example`**：**`packages/db-core`**、**`apps/nx-api`**、**`apps/nx-ui`** 改為以 **`postgresql://nexora:nexora123@localhost:5433/nexora_core?schema=public`** 為預設，並註明啟動指令 **`docker compose -f infra/docker/docker-compose.yml up -d`**。  
   - **實際 `.env`**：**`packages/db-core/.env`**、**`apps/nx-api/.env`** 已對齊上述 URI；**`DIRECT_URL`** 與 **`DATABASE_URL`** 本機設為相同。  
   - **`prisma.config.ts`**、**`schema.prisma`**（datasource 說明）：移除 Supabase／pooler 主敘述，改指向 Docker／`.env.example`。  
   - 執行 **`pnpm exec prisma generate`** 以更新 **`generated/prisma`** 內嵌 schema 註解。

6. **修正恆迎種子帳號（USERNAME＝帳號欄）並補登日誌**  
   - **問題**：`cytic-employees.txt` 曾誤用另一份名冊之 **7001–7162** 作為 **`nx00_user.username`**，與恆迎名冊之 **Y001、Y002…** 帳號欄不符（**帳號應對應 USERNAME**）。  
   - **修正**：依使用者提供之名冊圖重編 **`prisma/seed-data/cytic-employees.txt`**：**162** 筆 **Y001–Y162**，姓名與**離職**（TRUE／FALSE）依圖；檔首註解說明欄位意義；**`#` 行**略過。  
   - **Seed**：**`loadCyticEmployees`** 補文件註解；新增 **`removeExistingCyticUsers`**，每次種子前**刪光 CYTIC 租戶舊使用者**（含 **`nx00_user_role`**、該等帳號之 **`nx00_audit_log`**）再 **upsert**，避免 **7001…** 等殘留。  
   - **後續**：變更名冊後請再執行 **`pnpm exec prisma db seed`**（或整庫重建流程）以寫入 DB。

7. **使用者主檔：啟用／停用篩選與欄位對照**（`apps/nx-ui` + `apps/nx-api`）  
   - **篩選**：在職務複選旁新增 **「狀態」** 下拉：**全部／僅啟用／僅停用**（篩 **`is_active`**）。  
   - **列表欄位**（中文標籤與後端語意）：**帳號**（username）、**姓名**、**信箱**、**電話**、**啟用**（圖示＋寬螢幕文字）、**最後一次登入**、**建立時間**、**建立人員**（`帳號 姓名`）、**修改時間**、**修改人員**；**`createdBy`／`createdByName` 與 `updatedBy`／`updatedByName` 合併為單欄**；**`base.user.listcols` 偏好版本改為 4**（舊版欄位 key 在 **`normalizeColPref`** 映射到合併欄）。  
   - **API**：**`UserDto`** 新增 **`createdByUsername`／`updatedByUsername`**；**`user.service`** 之 **`createdByUser`／`updatedByUser`** 一併 **select `username`**。  
   - **明細**：基本資料 **Email→信箱**；稽核分頁改為 **最後一次登入 → 建立時間 → 建立人員 → 修改時間 → 修改人員**（單一 **建立人員／修改人員** 顯示 **帳號+姓名**）。  
   - **共用**：**`formatAuditPersonLabel`** 置於 **`mock-data.ts`**；**`BaseUserRoleView`** 之 **`pickerUsers`** 對齊 **`BaseUserRow`** 新欄位。

8. **職務主檔（對齊使用者主檔版型與欄位）**（`apps/nx-ui` + `apps/nx-api`）  
   - **`/base/role`**：**`BaseMasterPageHeader`** 僅 **`title`**（與使用者頁一致）。  
   - **`BaseRoleMasterView`**：**置中彈窗明細**、**`glass-card nx-glass-raised`**、列表 **條紋／選取樣式** 與 **單擊選列／雙擊開明細**、**↑↓／Enter／Esc**（明細關閉後 **`/base/...` 則回 `/base`**）、**狀態下拉**、**`RefreshCw`**、**編輯／儲存／取消** 與 **手機浮動編輯鈕**（同使用者主檔互動）。  
   - **列表欄位**（無 **`sort_no`**）：**職務代碼**、**職務名稱**、**職務說明**、**啟用**、**建立時間**、**建立人員**、**修改時間**、**修改人員**（人員為 **`帳號 姓名`**）；**`base.role.listcols` 版本改為 3**（舊 **`sortOrder`／`isSystem`／分拆 audit 欄** 於 **`normalizeColPref`** 映射或剔除）。  
   - **明細**：**順序**（**`sort_no`**）僅於 **基本資料**；**稽核** 為 **建立時間／建立人員／修改時間／修改人員**。  
   - **API**：**`RoleDto`** 新增 **`createdByUsername`／`updatedByUsername`**；**`role.service`** 之 audit user **select `username` + `displayName`**。  
   - **`BaseRoleRow`／`BaseUserRoleView.roleDtoToRow`**：補 **`createdByPerson`／`updatedByPerson`**（**`formatAuditPersonLabel`**）。

9. **使用者隸屬倉庫（先前日誌補登，已由 §12 改為多筆對應）**（`packages/db-core` + `apps/nx-api` + `apps/nx-ui`）  
   - 曾新增 **`nx00_user.warehouse_id`**（migration **`20260404120000_nx00_user_warehouse_id`**）；後續改以 **`nx00_user_warehouse`** 支援多據點，並移除此欄（見 §12）。

10. **Seed：僅保留 admin（先前日誌補登）**（`packages/db-core/prisma/seed.ts`）  
   - **`SEED_RESET_USERS_ADMIN_ONLY=1`**（或 **`true`**）執行 **`prisma db seed`** 時：僅 **upsert 租戶 DEV-INNOVA、ADMIN 角色、admin 與綁定** 後 **`purgeUsersExceptAdmin`**，**不**再匯入 CYTIC 員工名冊，供手動匯入使用者前清空。

11. **使用者職務設定 `/base/user-role`**（`apps/nx-ui`）  
   - **`page.tsx`**：**`BaseMasterPageHeader`** 移除長段 **description**。  
   - **`BaseUserRoleView`**：左側標題固定 **「職務列表」**（移除 **「（已選：…）」**）；成員 **PRIMARY／設為主要** 與 **`setUserRolePrimary`** 移除；**`assignUserRole`** 一律 **`isPrimary: false`**。  
   - **`LookupAutocomplete`**（共用元件）：**`inputRef`**、**`panelArrowNavigation`**（下拉 **↑↓** 高亮、**Enter** 選取）、**`escapePanelOnlyFirst`／`onEscapePanelAlreadyClosed`**、**`onEnterWhenEmptyQuery`**；**Esc** 先關面板並 **stopPropagation** 避免誤觸發外層。  
   - **鍵盤流程**：左側列表區 **↑↓** 換職務、**Enter** 焦點至右側使用者搜尋；搜尋有字時下拉 **↑↓／Enter** 加入；**無關鍵字 Enter** 焦點至成員區；成員區 **↑↓** 選標籤、**Delete** 移除、**↑**（在第一筆時）回到搜尋框；右側 **Esc**（面板已關）回到左側職務列表；左側職務列表 **Esc**→**`/base`**（主檔匯總）。  
   - **成員名單**：表格式清單、依帳號排序（後續小調）。

12. **使用者據點對應表與主檔頁**（`packages/db-core` + `apps/nx-api` + `apps/nx-ui` + `docs/nx00_field.csv`）  
   - **資料表**：**`nx00_user_warehouse`**（**`id`** 預設 **`gen_nx00_user_warehouse_id()`**＝**`NX00USWA`+7 碼**；**`user_id`／`warehouse_id`／`assigned_at`／`assigned_by`／`revoked_at`／`is_active`**；**`@@unique([userId, warehouseId])`**）。Migration **`20260405120000_nx00_user_warehouse`**：建表、自 **`nx00_user.warehouse_id`** 匯入一筆對應後 **DROP** 該欄與 FK／索引。  
   - **`id_generators.sql`**：補 **`seq_nx00_user_warehouse_id`／`gen_nx00_user_warehouse_id`**。  
   - **Prisma**：**`Nx00User`** 移除 **`warehouseId`**；**`Nx00Warehouse`** 改掛 **`userWarehouses`**。  
   - **API**：**`GET/POST /user-warehouse`**、**`PATCH /user-warehouse/:id/revoke`**（**`UserWarehouseModule`**）；**`UserDto`** 改 **`warehouseSummary`**（作用中據點彙整），**`CreateUser`／`UpdateUser`** 不再收 **`warehouseId`**。  
   - **UI**：**`/base/user-warehouse`**（**`BaseUserWarehouseView`**，版型對齊 **`BaseUserRoleView`**）；**`/base` hub** 卡片與 **`nx00_view` seed**（**`NX00_USER_WAREHOUSE`**，**sort_no** 緊接 **`NX00_USER_ROLE`**，其後畫面序號遞延）。  
   - **使用者主檔**：**隸屬倉庫**僅讀 **`warehouseSummary`**，並連結至 **使用者據點設定**；**`base.user.listcols` 版本 6**。  
   - **Seed**：刪除 CYTIC／purge 使用者前清 **`nx00_user_warehouse`**；**`SEED_RESET_USERS_ADMIN_ONLY`** 之 admin **update** 不再寫 **`warehouseId`**。

13. **零件主檔後續：清空資料、滾軸／淺色表格、主檔版型模組化**（`packages/db-core` + `apps/nx-ui`）  
   - **資料未清空之說明**：若 DB **尚未套用** migration **`20260406120000_part_drop_car_brand_unique_code`**，表內仍會殘留舊種子三筆。請於 **`packages/db-core`** 執行 **`pnpm exec prisma migrate deploy`**（或 dev 用 **`migrate dev`**）。  
   - **僅清空零件表（不跑完整 seed）**：設 **`SEED_TRUNCATE_NX00_PART_ONLY=1`**（或 **`true`**）後執行 **`pnpm exec prisma db seed`**，會 **`TRUNCATE "nx00_part" CASCADE`** 並結束。亦可手動執行 **`prisma/scripts/truncate-nx00-part.sql`**（`psql`／GUI）。  
   - **UI**：**`globals.css`** 新增 **`--nx-scroll-*`** token、**`.nx-master-scroll`**／**`.nx-master-modal-scroll`**（細捲軸、淺／深共用）；表頭／資料列改 **`nx-master-thead-row`**、**`nx-master-tbody-row`**，**`html.light`** 下表頭改淺灰底，不再寫死 **`bg-black`**。  
   - **模組化**：新增 **`BaseMasterModalFrame`**（遮罩＋置中 aside＋標題列與上一筆／下一筆／全螢幕／關閉）、**`MasterListScrollRegion`**（列表 **`nx-master-scroll`**）、**`isMasterListKeyboardBlocked`**（**`baseMasterListKeyboard.ts`**）；**`BaseUserMasterView`／`BaseRoleMasterView`／`BasePartMasterView`** 已改為引用上述元件，後續調整主檔互動可集中改 shell。

14. **主檔列表：淺色勾選框、啟用欄標籤（含深色可讀性）**（`apps/nx-ui`）  
   - **`globals.css`**：**`html.light .nx-master-row-checkbox`** 以 **`appearance: none`** 自繪淺色方塊；**`:checked`／`:indeterminate`** 搭配 SVG 勾／橫線，避免原生塊過深。  
   - **`MasterActiveListCell`**（USER／ROLE／PART 共用）：淺色維持柔和圓角標籤；**深色**改 **emerald-950／rose-950 高不透明度膠囊底**、**亮邊框（emerald-400／rose-400）** 與 **外緣微光 `box-shadow`**，圖示區 **emerald-900／rose-900** 加深對比。  
   - **Tailwind `dark:` 與主題切換對齊**：**`useNxThemeMode`** 僅在淺色時為 **`<html>`** 加 **`.light`**，深色為「未加 `.light`」而非 **`.dark`**；原先 **`@custom-variant dark (&:is(.dark *))`** 使全站 **`dark:`** 在預設深色下**從未生效**，元件若同時寫了淺色用 **`text-emerald-900`** 等，在深色畫面上會變成**暗綠字貼暗底**（例如啟用欄）。已改為 **`@custom-variant dark (&:where(html:not(.light) *, .dark *));`**，並保留 **`.dark`** 子樹以利日後巢狀深色區塊。

15. **汽車廠牌／零件廠牌主檔（零件主檔版型）**（`apps/nx-ui` + `apps/nx-api` + `packages/db-core`）  
   - **路由**：**`/base/car-brand`**（**`/car-brand`**）、**`/base/part-brand`**（**`/brand`**）；**`/base/brand`** 改為兩入口之**匯總頁**；**`/base/brands`** 轉址 **→ `/base/part-brand`**。  
   - **UI**：**`BaseBrandLikeMasterView`**（**`BaseCarBrandMasterView`／`BasePartBrandMasterView`**）— 列表欄：**廠牌代碼、廠牌名稱、廠牌國家、備註、啟用、建立／修改時間與人員（帳號＋姓名）**；**排序（`sort_no`）僅明細「基本資料」**；國家下拉 **GET /country**；批次啟用／停用、欄位偏好、鍵盤與 **`MasterActiveListCell`** 與零件主檔一致。  
   - **API**：**`CarBrandDto`／`BrandDto`** 增 **`countryCode`／`countryName`、`createdByUsername`／`updatedByUsername`**（稽核 user 改 **select `username`+`displayName`**）。  
   - **前端**：**`listBrand`** 可帶 **`isActive`**；**`car-brand/api`** 新增；**`car-brand/types`** 改 re-export **`BrandDto`** 別名以相容舊 **`BaseBrandMasterView`**。  
   - **Hub／選單／seed**：**`master-cards`** 拆為汽車／零件兩卡；**`menu.nx00`** 兩項；**`nx00_view`** 保留 **`NX00_BRAND`→`/base/brand`** 並新增 **`NX00_CAR_BRAND`／`NX00_PART_BRAND`**（共 **14** 筆畫面）。

16. **主檔 hub：廠牌捷徑合併、摘要與廠牌頁說明**（`apps/nx-ui`）  
   - **`master-cards`**：汽車廠牌／零件廠牌改為單卡 **`brand-masters`**（**`links`**：汽車／零件兩入口）；**`/base` hub** 摘要改 **`listCarBrand` + `listBrand`**，顯示 **`汽 X 筆 · 零 Y 筆`**。  
   - **`BaseMasterQuickNav`**：有 **`links`** 的卡片改為**單一按鈕＋下拉**選子主檔；在 **`/base/car-brand`** 或 **`/base/part-brand`** 時維持 **active**。  
   - **`/base/car-brand`、`/base/part-brand`**：**`BaseMasterPageHeader`** 移除長段 **description**（僅 **title**）。

17. **國家／幣別主檔：modal 版型與稽核帳號**（`apps/nx-ui` + `apps/nx-api`）  
   - **UI**：新增 **`BaseNx00ModalCodeMasterView`**（**`country`／`currency`**）— 與汽車廠牌相同 **LIST + 置中彈窗**、分頁、欄位偏好、批次啟用／停用、關鍵字與狀態篩選；列表欄位中文標籤（**國家代碼／名稱**、**幣別代碼／名稱／符號／小數位數**等）；**`sort_no` 僅「基本資料」**；**建立／修改人員** 為 **`formatAuditPersonLabel`（帳號＋姓名）**。  
   - **頁面**：**`/base/country`、`/base/currency`** 外殼對齊 **`/base/car-brand`**（**`HomeLandingChrome` + `HomeTopBar` + `BaseMasterPageHeader`**）。  
   - **API**：**`CountryDto`／`CurrencyDto`** 新增 **`createdByUsername`／`updatedByUsername`**；**`country.service`／`currency.service`** 之 **`createdByUser`／`updatedByUser`** 改 **select `username` + `displayName`**。  
   - **轉匯**：**`BaseCountryMasterView`／`BaseCurrencyMasterView`** 改指向 modal 實作；列表欄位偏好鍵 **`base.country.modal.listcols`／`base.currency.modal.listcols`**。

18. **零件族群／廠牌料號規則：modal 版型與稽核帳號**（`apps/nx-ui` + `apps/nx-api`）  
   - **零件族群**：**`BaseNx00ModalCodeMasterView`** 新增 **`partGroup`** 變體（欄位：**族群代碼、族群名稱**；**排序僅明細**）；**族群代碼不強制大寫**；**`BasePartGroupApiMasterView`** 轉匯 **`BasePartGroupModalMasterView`**；**`/base/part-group`** 頁面外殼同上。  
   - **廠牌料號規則**：新增 **`BaseNx00ModalBrandCodeRoleMasterView`** — 列表：**零件品牌、第1～5段字數限制、組合格式、排序順序、啟用、稽核**；明細：**零件品牌**（新增可選 **`/brand`** 下拉，儲存後唯讀）、**seg／組合格式／排序順序／啟用**；**`BaseBrandCodeRoleMasterView`** 轉匯該元件；**`/base/brand-code-role`** 標題 **廠牌料號規則**。  
   - **API**：**`PartGroupDto`／`BrandCodeRoleDto`** 新增 **`createdByUsername`／`updatedByUsername`**；對應 **service `include`** 補 **username**。  
   - **欄位偏好**：**`base.partGroup.modal.listcols`／`base.brandCodeRole.modal.listcols`**。

19. **主檔總覽 `/base` 與頂部捷徑：依業務分區排列**（`apps/nx-ui`）  
   - **`master-cards.ts`**：**`MasterHubCard`** 新增 **`section`**（**`MasterHubSectionId`**：`account`／`product`／`geo`／`warehouse`／`partner`）；**`MASTER_HUB_CARDS`** 依分區重排：**帳號與權限**（使用者、職務、使用者職務、據點、權限）→ **產品與料號**（零件、廠牌、族群、品牌料號規則、零件關聯）→ **國家與幣別** → **倉儲** → **往來對象**（**客戶主檔** 卡片標題／說明與 **`/base/partner`** 一致）。  
   - **`getMasterHubSections()`**：回傳各區標題與卡片陣列，供 hub 與 QuickNav 共用。  
   - **`/base` `page.tsx`**：每區 **區塊標題**＋**卡片網格**（**`space-y-10`**）；說明文案略述「依業務分區」。  
   - **`BaseMasterQuickNav`**：依 **`getMasterHubSections()`** 渲染，組與組之間 **sm 以上** 顯示 **直向分隔線**；下拉選單 **Label** 帶 **分區 · 卡片名** 以利辨識。

## 討論與決策（Q&A）
- 無。

## 明日待辦
- [ ] **將後端（`apps/nx-api`）部署到 Railway 試跑**：建立專案／服務、設定環境變數（**`DATABASE_URL`**、JWT／CORS 若需、與前端 **`nx-ui`** 的 API 基底網址對齊）、建置與啟動指令（**`pnpm`** monorepo 路徑或 Docker 若已有）、連線雲端或 Railway **PostgreSQL** 後執行 **`prisma migrate deploy`**。  
- [ ] 依實際開發進度延續主檔／API；若換機或換庫，記得確認 **`migrate deploy`**／**`prisma db seed`** 狀態。  
- [ ] 恆迎帳號若需改預設密碼／職務分批指派，可再調整 **`cytic-employees.txt`**（**Y 編號＝username**）或 seed 邏輯。

## 今日收工
- 程式與日誌已整理；**Git** 已提交並推送 **`main`**（與遠端同步）。

## 遇到的問題與解法

| 問題 | 解法 |
|------|------|
| 深色主題下 **`dark:`** 樣式未套用，啟用欄等仍像暗字貼暗底 | **`globals.css`**：**`@custom-variant dark`** 改為 **`html:not(.light)`** 後代（並保留 **`.dark *`**），與 **`useNxThemeMode`** 僅切換 **`.light`** 的行為一致。 |
