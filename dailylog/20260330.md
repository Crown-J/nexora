# 2026-03-30 工作日誌

> 補登：以下為 **NX03 銷貨即時報價工作台** 相關實作與修正（含已推上 `main` 之提交），先前未寫入 `dailylog` 者一併記錄於此。

## 專案資訊
- 專案：NEXORA
- 開發者：Crown Lin（Hank 執行）

## 今日完成事項

1. **NX03「實際銷貨操作」改為四滿版垂直區塊（單頁捲動）**（`b33f529`）  
   - **`SalesOperationWorkspace`**：四區 **① 搜尋料號 → ② 庫存 → ③ 價格與紀錄 → ④ 報價**，以 **`scrollIntoView`** 對應快捷鍵，取代先前以多 Tab／窄版面為主的配置（與 `docs/flows/10-sales-quote-workbench-ui.md` 對齊）。  
   - **`SalesWorkflowPage`**：傳入 **`searchFocusNonce`**，**`F2`** 時捲至第一區並聚焦料號輸入（`nx03-part-search-input`）。  
   - **第二區**：左側通用件列表可 **↑↓**；**倉別／儲位** 面板 **一律展開**（不收合）。  
   - **第三區**：**`Alt+2`** 捲至本區並聚焦 **客戶編號**（與既有 **`input` + `datalist`** 行為一致）；**Enter** 篩選紀錄並可將焦點移至下方紀錄格。  
   - **第四區報價**：**`Alt+A`** 捲至本區並 **`syncQuoteFromBlock3`**（帶入第三區客戶與選中紀錄單價）；依 **客戶編號／單價是否空白** 決定起始焦點（**Enter** 鏈：**客戶編號 → 單價 → 數量 → 確認窗**；客戶名稱欄 **Enter** 亦進入單價）。**`useLayoutEffect`** 僅依 **`focusBlock4Nonce`** 對焦，避免輸入時被搶焦點。  
   - **Mock**：新增 **`workbench.mock.ts`**（本倉、倉別彙總、儲位、牌價 **A～D**、比價列 **僅銷貨／報價**、無訂單比價列示意）；**`operation.mock.ts`** 等沿用關鍵字 **`searchPartFamilies`**。  
   - **文件**：新增／更新 **`docs/flows/10-sales-quote-workbench-ui.md`**、**`docs/flows/README.md`** 索引。

2. **第一區料號搜尋：與「客戶編號」相同之 `datalist` 體驗**（`24499f4`）  
   - 以 **`list` + `<datalist>`** 呈現候選料號（**`value` = 料號**、**option 內文 = 品名**），對齊第三區客戶欄互動。  
   - **`families`** 改為 **`useMemo(searchPartFamilies(query))`**；新增 **`findExactVariantByPartNo`**，當輸入值與某筆 **完整料號** 相同（含從下拉選取）時鎖定 **`familyId`／`variantId`**。  
   - **版面**：搜尋輸入外層移除 **`flex-1`**（避免在 **`min-h` + `flex-col`** 下把下方內容擠到區塊底部）；第一區改用 **`blockClassSearch`**（**不**套用與他區相同之 **`min-h-[min(70vh,720px)]`**），減少無謂留白。  
   - 移除獨立 **`<select>`「快速選擇料號」** 與搜尋框左側放大鏡圖示；**Enter／查詢** 仍 **`runSearch`** 並捲至第二區、聚焦對應零件列。

3. **快捷鍵摘要（實作與說明文字一致）**  
   - **`F2`**：第一區（捲動 + 聚焦料號）。  
   - **`Alt+1`**：第二區庫存。  
   - **`Alt+2`**：第三區並聚焦客戶框。  
   - **`Alt+A`**：第四區報價（含帶入與焦點邏輯）。

## Git
- **`main`** 已包含：**`b33f529`**（四區塊與第四區 Alt+A／Enter 鍵流等）、**`24499f4`**（料號 `datalist` 與第一區留白修正）。

## 明日待辦
- [ ] 依實際進度延續功能或後端串接。

## 遇到的問題與解法
| 問題 | 解法 |
|------|------|
| 第一區「快速選擇」出現在區塊底部、中間大片黑空 | 搜尋列誤加 **`flex-1`** 於 **`flex` + `min-h`** 區塊內會撐滿高度；移除 **`flex-1`**，第一區改 **`blockClassSearch`** 不強制 **`70vh`**。 |
| 料號欄要與客戶欄一致的下拉體驗 | 改用 **`input` + `datalist`**，與第三區 **`nx03-wb-cust-dl`** 同一 HTML 模式。 |
