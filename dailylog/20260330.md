# 2026-03-30 工作日誌

> 補登：以下為 **NX03 銷貨即時報價工作台** 相關實作與修正（含已推上 `main` 之提交），先前未寫入 `dailylog` 者一併記錄於此。

## 專案資訊
- 專案：NEXORA
- 開發者：Crown Lin（Hank 執行）

## 今日完成事項

1. **NX03「實際銷貨操作」改為四滿版垂直區塊（單頁捲動）**（`b33f529`）  
   - **`SalesOperationWorkspace`**：四區 **① 搜尋料號 → ② 庫存 → ③ 價格與紀錄 → ④ 報價**，以 **`scrollIntoView`** 對應快捷鍵，取代先前以多 Tab／窄版面為主的配置（與 `docs/flows/10-sales-quote-workbench-ui.md` 對齊）。  
   - **`SalesWorkflowPage`**：傳入 **`searchFocusNonce`**，**`F2`** 時捲至第一區並聚焦料號輸入（`nx03-part-search-input`）。  
   - **第二區**：左側通用件列表可 **↑↓**；**倉別／儲位** 面板 **一律展開**（不收合）。  
   - **第三區**：**`Alt+2`** 捲至本區並聚焦 **客戶編號**（與既有 **`input` + `datalist`** 行為一致）；**Enter** 篩選紀錄並可將焦點移至下方紀錄格。  
   - **第四區報價**：**`Alt+A`** 捲至本區並 **`syncQuoteFromBlock3`**（帶入第三區客戶與選中紀錄單價）；依 **客戶編號／單價是否空白** 決定起始焦點（**Enter** 鏈：**客戶編號 → 單價 → 數量 → 確認窗**；客戶名稱欄 **Enter** 亦進入單價）。**`useLayoutEffect`** 僅依 **`focusBlock4Nonce`** 對焦，避免輸入時被搶焦點。  
   - **Mock**：新增 **`workbench.mock.ts`**（本倉、倉別彙總、儲位、牌價 **A～D**、比價列 **僅銷貨／報價**、無訂單比價列示意）；**`operation.mock.ts`** 等沿用關鍵字 **`searchPartFamilies`**。  
   - **文件**：新增／更新 **`docs/flows/10-sales-quote-workbench-ui.md`**、**`docs/flows/README.md`** 索引。

2. **第一區料號搜尋：與「客戶編號」相同之 `datalist` 體驗**（`24499f4`）  
   - 以 **`list` + `<datalist>`** 呈現候選料號（**`value` = 料號**、**option 內文 = 品名**），對齊第三區客戶欄互動。  
   - **`families`** 改為 **`useMemo(searchPartFamilies(query))`**；新增 **`findExactVariantByPartNo`**，當輸入值與某筆 **完整料號** 相同（含從下拉選取）時鎖定 **`familyId`／`variantId`**。  
   - **版面**：搜尋輸入外層移除 **`flex-1`**（避免在 **`min-h` + `flex-col`** 下把下方內容擠到區塊底部）；第一區改用 **`blockClassSearch`**（**不**套用與他區相同之 **`min-h-[min(70vh,720px)]`**），減少無謂留白。  
   - 移除獨立 **`<select>`「快速選擇料號」** 與搜尋框左側放大鏡圖示；**Enter／查詢** 仍 **`runSearch`** 並捲至第二區、聚焦對應零件列。

3. **快捷鍵摘要（實作與說明文字一致）**  
   - **`F2`**：第一區（捲動 + 聚焦料號）。  
   - **`Alt+1`**：第二區庫存。  
   - **`Alt+2`**：第三區並聚焦客戶框。  
   - **`Alt+A`**：第四區報價（含帶入與焦點邏輯）。

4. **頁首改為流程圖式導覽與「建立銷貨單」階段（mock）**  
   - **`SalesWorkflowPage`**：以 **「建立報價單 → 建立銷貨單」** 取代原「實際銷貨操作」分頁標籤；**建立銷貨單** 需先有 **`QuoteSnapshot`**（報價完成後寫入）。  
   - **`types.ts`**：新增 **`QuoteSnapshot`**（客戶、料號、數量、單價、備註）。  
   - **`SalesOperationWorkspace`**：報價確認窗改為三按鈕 **取消／繼續報價／報價完成**；**報價完成** 觸發 **`onQuoteComplete`**，父層切至 **`SalesOrderWorkspace`**。  
   - **`SalesOrderWorkspace`**（新檔）：三區塊 **確認客戶 → 商品明細 → 出貨方式**（自取／送貨／寄貨），Enter 鍵流與完成對話框（mock）。

5. **第二區庫存：簡化右側與倉別行為**  
   - 右側改為固定 **Z00 總倉／Z01 台北倉／Z02 新莊倉／Z04 林口倉／調貨**，數量由 mock **`rollup`** 列依序對應；移除本倉三卡、倉別大表、儲位明細。  
   - 左側選件 **Enter** → 右側選倉；再 **Enter** → 第三區。  
   - 選 **庫存 0** 之倉：toast **「目前此倉庫沒有貨」**；備註欄自動加上 **「由{倉編}{倉名}出貨」**（調貨為「由調貨出貨」），並會替換先前同類前綴。

6. **第三區價格與紀錄：精簡為三列**  
   - 已輸入客戶並 **Enter 篩選** 後，紀錄表僅顯示 **最近一次報價**、**最近一次成交（銷貨）**、**自訂報價**（↑↓ 選列）；移除銷貨／報價勾選與長列表。  
   - **`syncQuoteFromBlock3`**：選 **自訂報價** 時 **清空第四區單價**（自行輸入）；選前兩列則帶入對應單價。  
   - 紀錄表 **Enter**（或 **Alt+A**）進入第四區，**永遠先聚焦客戶編號**。

7. **第四區報價：雙欄試算與確認窗**  
   - **客戶編號／客戶名稱** 同一列左右並排；**Enter** 鏈：**編號 → 名稱 → 單價（含稅）→ 數量 → 備註 → 開啟確認**。  
   - 單價列右欄：**未稅總價**（試算：含稅單價×數量÷1.05）；數量列右欄：**含稅總價（5%）**（試算：含稅單價×數量）；右欄唯讀。  
   - 備註為 **textarea**（**Enter** 開確認、**Shift+Enter** 換行）。

8. **`DialogContent` 可拖曳（`components/ui/dialog.tsx`）**  
   - 新增 **`draggable`**：頂部拖曳列，可 **左右／上下** 平移視窗（避免與中央定位 transform 衝突時改以 inline **`translate`**）。  
   - **確認報價**、**快捷鍵** 等對話框已啟用 **`draggable`**。

## Git
- **`main`** 已包含：**`b33f529`**（四區塊與第四區 Alt+A／Enter 鍵流等）、**`24499f4`**（料號 `datalist` 與第一區留白修正）。  
- 上述第 4～8 點之流程圖、**`SalesOrderWorkspace`**、精簡紀錄、倉別／備註、雙欄試算、**`draggable`** 等，請以實際 **`git log`**／尚未推送之提交為準補登 commit。

9. **側邊 DOCK：Alt 展開與 Alt+1～6 導覽**（已於下方第 10 點改版，此條為歷史描述。）

10. **側邊 DOCK：Alt+X 與字母鍵導覽**（`apps/nx-ui/src/components/home/dock.tsx`）  
   - **Alt+X**：切換 DOCK 開／關（不需長按 Alt）。  
   - DOCK 開啟時：**H** 首頁、**B** 主檔、**P** 採購、**S** 銷貨、**W** 庫存、**M** 財務、**R** 報表；**Esc** 關閉；**Tab** 保留在面板內移焦點；其餘單鍵關閉 DOCK（可編輯欄位內不攔截）。  

11. **NX03 銷貨流程總覽**（`apps/nx-ui/src/features/nx03/sales/SalesFlowHub.tsx` 等）  
   - 流程六階段：**查詢 → 報價 → 訂單 → 備貨 → 出貨 → 完成**。  
   - **`Alt+1～6`**（僅路徑 `/dashboard/nx03`）：切換對應階段。  
   - **`Alt+A`**：僅在「報價」「訂單」階段生效，分別導向 **`/dashboard/nx03/workbench?phase=quote`**、**`?phase=salesOrder`**；**`SalesWorkflowPage`** 讀取 **`phase`** 切換建立報價／建立銷貨單；**workbench** 頁面以 **`Suspense`** 包住 **`useSearchParams`**。  
   - **查詢**階段：左欄上搜尋、下通用零件列表；右欄各倉數量與 **調貨** 按鈕（mock）。

12. **DOCK 顯示快捷鍵字母；銷貨查詢／報價流程再強化**（`dock.tsx`、`SalesFlowHub.tsx`）  
   - **DOCK**：圖示右側固定顯示 **H／B／P／S／W／M／R**（與 **Alt+X** 後單鍵導覽一致）。  
   - **查詢**：**Enter** 進入左下清單、清單內 **Enter** 多選；**Alt+S** 或 **「前往報價」** 開倉別指派對話框（**↑↓←→**、**Enter** 選倉；**Alt+S** 先驗證再進確認；確認後 **Alt+S** 或 **Enter** 進入報價階段）；防呆：**不可選無庫存倉**（**調貨**除外）。右欄若四倉皆無庫存則**僅顯示一列「調貨」**；移除 **+ 進階搜尋**。  
   - **報價**：取消捷徑／按鈕直開工作台建立報價單；**建立報價**僅能由查詢流程完成指派後進入。報價畫面下方：**建立報價單**（可收合 mock，由查詢進入時展開）＋**今日報價單列表**（mock）。  
   - **訂單**階段仍可用 **`Alt+A`** 開 **`workbench?phase=salesOrder`**。

13. **報價建立區改為非比價、B 價預設與 A～D 參考**（`SalesFlowHub.tsx`）  
   - 左欄：查詢帶入之料號，可逐筆輸入報價；**預設帶入 B 價**（依 **`MOCK_COST_BY_PART`** 與毛利公式，公司預設層級 **`DEFAULT_QUOTE_TIER`** 預留後端設定）。  
   - 右欄：**A～D 價**附 **三成／兩成五／一成五／一成** 毛利說明；依目前反白列顯示 **較 B 價 ±%** 提示。  
   - 下方 **textarea**：可複製給客戶之摘要；**Alt+S** 寫入今日報價列表（mock）。

## 明日待辦
- [ ] 依實際進度延續功能或後端串接。

## 遇到的問題與解法
| 問題 | 解法 |
|------|------|
| 第一區「快速選擇」出現在區塊底部、中間大片黑空 | 搜尋列誤加 **`flex-1`** 於 **`flex` + `min-h`** 區塊內會撐滿高度；移除 **`flex-1`**，第一區改 **`blockClassSearch`** 不強制 **`70vh`**。 |
| 料號欄要與客戶欄一致的下拉體驗 | 改用 **`input` + `datalist`**，與第三區 **`nx03-wb-cust-dl`** 同一 HTML 模式。 |
