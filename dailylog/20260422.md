<!-- dailylog/20260422.md -->
# 2026-04-22（週三）— BOSCH 春酒 48hr demo 搶救戰 + PROJECT_CONTEXT

> 本日開工：早上 PROJECT_CONTEXT，隨後全日 DEMO-EMERGENCY R1~R5 phase 1。
> 分支：`feature/demo-emergency`（origin 已 push 到 `1df3253`）。
> 週五春酒前剩 1 天（明天 2026-04-23）。

---

## 0422-A｜早上：建立 docs/PROJECT_CONTEXT.md

**TASK-PROJECT-CONTEXT-UPDATE-01 v2**（v1 假設 PROJECT_CONTEXT.md 已存在，實際需新建）

- 新建 [docs/PROJECT_CONTEXT.md](../docs/PROJECT_CONTEXT.md)（578 行，13 章節）
- 順手新建 [README.md](../README.md)（31 行 minimal：專案名 + 文件導航 + 本機開發指令）
- 新建兩檔第一行加路徑註解 `<!-- ... -->`（feedback 規範）
- 校正事實：role 7→8（含 HR_ADMIN）、medal 20→16、role_view 826 筆 per tenant
- Alex spec 本身有兩處數字不一致（第三章 128 張表、第十三章 124 張）原文複製
- Commit：`0b9112c docs: add docs/PROJECT_CONTEXT.md as project knowledge snapshot`（已 push `main`）

---

## 0422-B｜DEMO-EMERGENCY 分支策略（Step 0）

- 從 `main`（`0b9112c`）checkout `feature/demo-emergency` 並 push 到 origin
- `pnpm prisma migrate status`：17 migrations applied，DB @ localhost:5433，schema up to date
- `pnpm --filter nx-ui dev`：✓ Ready in 1455ms（Turbopack）
- 發現：**Next.js 實際 16.1.6**（CLAUDE.md / PROJECT_CONTEXT.md 寫 15）→ 登記 A003
- 發現：Next.js 16 警告 `middleware` convention deprecated，建議改用 `proxy` → 登記 A004
- 全 repo 83 個 page.tsx / 0 個 `/m/*`（手機版路由完全不存在）
- A 級接 API ~23 頁、B 級 mock UI ~42 頁、C 級占位 ~17 頁

---

## 0422-C｜Round 1：NX02 庫存模組手機版（7 個 View）

Alex spec：5 頁手機化（P1 balance + P2 stock-take/transfer + P3 shortage/ledger）。`/dashboard` 首頁已 responsive 判斷不動。

統一 pattern：
- `<table>` 包進 `hidden lg:block` + 新增 `lg:hidden` 手機卡片列表
- Filter bar：`flex flex-col sm:flex-row sm:flex-wrap`
- Input/Select：`h-11 mobile → h-9 lg:`（≥44px tap target）
- Button：`min-h-[44px]` mobile tap target

Commits：
- `cf84e66` BalanceView：desktop table + mobile card（庫存一覽主打）
- `e020c9c` StockTakeListView + TransferListView：list + mobile cards
- `c41d6c6` StockTakeDetailView + TransferFormView：detail/form + 手機卡片（含 PartLookupAutocomplete 嵌入）
- `875acd9` ShortageView + LedgerView：P3 list + mobile cards（Shortage 含 checkbox 選取、Ledger 14 欄壓進 2×2 grid）

**發現**：真正視覺元件在 `features/*/ui/*View.tsx`，page.tsx 都是薄殼。`DashboardShell` 架構是「HomeTopBar + DashboardSubNav 橫向次選單」，沒有傳統 Sidebar，Alex spec 的 Pattern 4/5（Sidebar drawer）不適用。

---

## 0422-D｜Round 2：登入頁滿版 + TopBar 精簡 + DOCK 去子選單

Commits：
- `5a12c31` 登入頁 `h-dvh` 滿版：三段 `shrink-0 / flex-1 min-h-0 / shrink-0`、刪 `lg:hidden fixed bottom` 版本列、orbit max-w 280→200
- `99c364d` TopBarPlanToggles env-gated：`process.env.NEXT_PUBLIC_SHOW_VERSION_SWITCHER !== 'true'` → return null（hook 先呼叫保持順序穩定）
- `9fa2740` NavPlanetMenu 手機 Direct-link：採購/銷貨/庫存手機不展開子選單（用 `display: contents` CSS 切換），桌面保留 `DropdownMenuSub` 行為

**釐清**：Alex spec 說「DOCK」實際指 `NavPlanetMenu`（頂欄星球按鈕 dropdown），不是 `DashboardSubNav`（橫向次選單）也不是 `MobileDock`（底部 bar）。

---

## 0422-E｜Round 3：主檔中心 5-Tab + 卡片響應式

Commits：
- `7ed9b40` master-cards 5-group refactor（方案 C）：
  - `warehouse → organization`（組織架構）
  - `geo` 合併到 `system`（系統設定，新增）
  - `往來對象 → 交易對象`
  - `bulletin` 從 `account` 移至 `system`
- `7d6da44` master hub mobile bottom tabs + responsive cards：
  - 新建 `features/base/ui/MobileSectionTabs.tsx`（R3 版本，base-specific）
  - `BaseDashboardPage` 拆 desktop/mobile 兩個 render path
  - `MobileDock` pathname 判斷 `/dashboard/base` 時 return null
  - 卡片 `!w-full lg:!w-[220px]` 響應式寬度

---

## 0422-F｜Round 4：首頁 tier 差異化 + 主檔子頁 + 3 中心套 Tab

### R4-A 首頁 tier（初版）
- `4a32096` `SysDashboardPage` 改用 `useSessionMe().planCode` + `deriveTierFromPlanCode()`，廢棄 `useDashboardHomePlan` consume（保留給 TopBarPlanToggles env-switcher）

### R4-B 主檔子頁簡化
- `ddd6146`
  - `DashboardSubNav` 在 `/dashboard/base/*` 子頁加 `hidden lg:block`
  - `MobileDock` `HIDE_MOBILE_DOCK_PATHS` → `HIDE_MOBILE_DOCK_PREFIXES`（startsWith 匹配，整個 base/* 路徑族群）

### R4-C 三中心套 R3 模式
- `31b738c` hub infra：
  - 新建 generic `features/layout/ui/module-hub/MobileHubSectionTabs.tsx`（從 R3 抽象出來，`{ tabs, activeId, onChange, ariaLabel }` props）
  - `ModuleHubSection` 響應式 `grid grid-cols-1 sm:grid-cols-2 lg:flex lg:flex-wrap`
  - `HubLinkCard` 加 `!w-full lg:!w-[220px]`
  - dock 區分 `HIDE_EXACT`（中心首頁）vs `HIDE_PREFIXES`（主檔含子頁）
- `2a40f32` purchase / sale / inventory 三 hub pages：各自定義 tabs（3/2/4 個）、抽出 `<MasterSection />` 等子元件給 desktop+mobile 共用、`pb-16 lg:pb-0` 預留 Tab bar 空間

**三中心 tabs**：
- Purchase（3 tabs）：主檔管理 / 國內採購 / 特殊採購（Briefcase/Globe2/Zap）
- Sale（2 tabs）：客戶主檔 / 國內銷售（Handshake/ShoppingCart）
- Inventory（4 tabs）：主檔管理 / 出貨 / 進貨 / 盤點（Settings/PackageOpen/PackageCheck/ScanLine）

---

## 0422-G｜R4A-DEBUG + R4A-FIX：demo session 動態 tier

### 驗收失敗
Crown 清 cookie 重登 TEST-PLUS，仍看到 ProHomeBody（PRO 版首頁）。

### 根因（純靜態分析）
- `.env.local` 設 `NEXT_PUBLIC_DEMO_MODE=true` + `NEXT_PUBLIC_DEMO_PLAN_CODE=PRO`
- `useSessionMe.ts:143` 第一檢查 `isNextPublicDemoMode()` → 走 `DEMO_USER` 路徑，**完全不打 `/auth/me`**
- `DEMO_USER.planCode` 來自 env var 寫死 `'PRO'`
- R4-A 的 `deriveTierFromPlanCode('PRO') === 'PRO'` 邏輯正確但來源被污染
- 另一處 `buildDemoMeFromStorage` 也寫死 `plan_code: 'PRO'`

### 修復（`b6da920`）
選項 B：保留 demo 離線能力，依 tenantCode 動態 tier。

4 個檔案：
1. **`features/auth/demo-session.ts`**
   - 新增 sessionStorage key `nexora_demo_tenant_code`
   - `setDemoSessionTenantCode()` / `readDemoSessionTenantCode()` / `deriveDemoPlanCodeFromSession()`
   - `TEST-LITE → NEXORA-LITE-M` / `TEST-PLUS → NEXORA-PLUS-L` / `TEST-PRO → NEXORA-PRO-XL`
   - `buildDemoMeFromStorage` 改用動態 plan code
   - `clearDemoSessionUsername` 連帶清 tenantCode（logout reset）
2. **`hooks/useDemoSession.ts`**
   - 新增 `getDemoUser()` 動態 getter（每次呼叫讀 sessionStorage 再 collapse 成 LITE/PLUS/PRO）
   - `DEMO_USER` const 保留 backward compat（模組載入時快照）
3. **`features/auth/api/login.ts`**（`callLoginApi`）
   - 兩條 path 都寫入 tenantCode：demo 短路 + 真實 `/auth/login` 成功後
4. **`features/auth/hooks/useSessionMe.ts`** line 146 `DEMO_USER → getDemoUser()`

**關鍵釐清**：`.env.local` `NEXT_PUBLIC_DEMO_MODE=true`（`isNextPublicDemoMode`）與 `.env` `NEXT_PUBLIC_NEXORA_RUN_MODE=development`（`isNexoraDemoMode`）是**兩個獨立 env 變數走不同 path**，名字近似極易混淆 → 登記 A005。

---

## 0422-H｜Round 5 Phase 1：國內進貨 SOP 手機工作台（BOSCH demo 核心作品）

### Crown 揭示產品哲學
> 「材料行老員工都是經驗累積，新世代不願花時間學、老員工也不會教。NEXORA 主打『SOP 內建』，系統規劃出正確流程、有程序跟防呆，新人也能上手。」

### 任務本質
不是做採購功能，是做**春酒 demo 示範**：對方看完 5 分鐘會覺得「這跟一般 ERP 真的不同」、「我們公司的新人需要這個」。

### R5 範圍
- **Core**（6-8h）：國內進貨 SOP 9 步 STEP 1~9-A
- **Extra**（2-3h）：國外流程 STEP 7-B ~ 11-B
- **不做**：桌面版、真實 API、退貨子流程

### Phase 1 完成內容（`1df3253`，11 檔 +1224 行）

**路由**：`/dashboard/purchase/sop-demo`（新）
**入口**：採購中心 > 國內採購 tab 首張「SOP 精品示範 🔥」卡（`Sparkles` icon），同時改 mobile 預設 tab `master → domestic` 讓 demo 一進採購中心就看到

**檔案**：
```
apps/nx-ui/src/features/purchase/ui/sop-workspace/
├─ types.ts                              StepNumber / RequirementItem / Vendor / ScenarioState / ScenarioAction
├─ mock-data/scenario.ts                 VAG 料號（VW/Audi/Skoda/Porsche）+ A/B/C 級廠商 + 含稅 runtime 計算 + STEPS_META
├─ MobilePurchaseSopPage.tsx             主容器 useReducer + step switch
└─ components/
   ├─ ProgressHeader.tsx                 sticky 頂部 9 節點捷運圖（綠/金/灰三態）
   ├─ StepWrapper.tsx                    共用殼 + fixed 底部 CTA 列（48px tap target）
   ├─ Step1Requirements.tsx              3 分類勾選（庫存不足/客戶下單/廠商特價）+ 群組全選
   ├─ Step2Inquiry.tsx                   3 家廠商比價卡（A 級金邊推薦 + 贈品 / 最低價 badge / 交期）
   └─ StepPlaceholder.tsx                STEP 3~9 占位（允許前進預覽流程動畫、STEP 9 變「再來一次」RESET）

apps/nx-ui/src/app/dashboard/purchase/sop-demo/page.tsx  薄殼
```

**Dock 改動**：`HIDE_MOBILE_DOCK_PREFIXES` 加 `/dashboard/purchase/sop-demo`（SOP 頁自己有 fixed 底部 CTA，避免雙 bar 衝突）

**Mock 資料真實感**：
- 料號：剎車片 VW Golf MK7 / 機油濾心 Audi A4 / 引擎蓋密封條 Skoda Octavia / 輪胎 Porsche Cayenne 21" / 火星塞 VW Passat / 空氣濾心 Skoda Superb
- 廠商：德國汽車配件（A 級，月結 30 天，單價高但贈輪胎）、台灣汽材貿易（B 級，月結 15 天，中價交期快）、東亞零件商（C 級，貨到付款，低價交期長）
- 金額格式：有零有整（如 24,800 / 485 / 1,280）非整齊數字

**SOP 內建展示亮點已埋入**：
- STEP 1：系統自動盤點三類需求（hint badge 「建議新人先從這三類開始」）
- STEP 2：系統已向推薦廠商發詢價（hint badge）+ 額外推薦（廠商順便推銷情境）+ 最低價 badge

---

## 0422-I｜Git commits 總表（feature/demo-emergency）

| Hash | Task | 摘要 |
|------|------|------|
| `0b9112c` | CONTEXT v2 | docs/PROJECT_CONTEXT.md + README.md 新建（已 merge 在 main） |
| — | Step 0 | `git checkout -b feature/demo-emergency` + push |
| `cf84e66` | R1 | nx02 balance card list |
| `e020c9c` | R1 | nx02 stock-take + transfer list |
| `c41d6c6` | R1 | nx02 stock-take detail + transfer form |
| `875acd9` | R1-P3 | nx02 shortage + ledger |
| `5a12c31` | R2-1 | login `h-dvh` 滿版 |
| `99c364d` | R2-2 | TopBar plan toggles env-gated |
| `9fa2740` | R2-3 | NavPlanetMenu 手機去子選單 |
| `7ed9b40` | R3-1 | master-cards 5-group refactor |
| `7d6da44` | R3-2 | master hub mobile tabs + responsive cards |
| `4a32096` | R4-A | home tier from useSessionMe |
| `ddd6146` | R4-B | master sub-pages hide subnav + dock |
| `31b738c` | R4-C infra | generic MobileHubSectionTabs + responsive hub-primitives |
| `2a40f32` | R4-C pages | purchase / sale / inventory hubs tabs |
| `b6da920` | R4A-FIX | demo session tenantCode 動態 tier |
| `1df3253` | R5 phase 1 | domestic SOP mobile workspace（STEP 1-2 + infra） |

**HEAD**：`1df3253`｜origin/feature/demo-emergency 同步｜main 在 `0b9112c`

---

## 0422-J｜架構債登記（A 系列延續）

| 編號 | 描述 | 狀態 | 處理時機 |
|------|------|------|---------|
| A003 | 文件 drift：CLAUDE.md / PROJECT_CONTEXT.md 寫 Next.js 15，實際 16.1.6 | 🟡 待修 | 春酒後順手 |
| A004 | Next.js 16 `middleware` convention deprecated，建議改用 `proxy` | 🟡 待修 | 單獨任務 |
| A005 | `isNextPublicDemoMode`（`NEXT_PUBLIC_DEMO_MODE`）vs `isNexoraDemoMode`（`NEXT_PUBLIC_NEXORA_RUN_MODE`）兩個 env 變數語意重疊易混淆 | 🟡 待修 | 春酒後整併為單一 run mode |
| A006 | `features/base/ui/MobileSectionTabs.tsx` 與通用版 `MobileHubSectionTabs.tsx` 並存（R4-C 時為求穩未刪 R3 base 版） | 🟢 順手清 | 春酒後 |

A001/A002 昨天已修。今天新增 A003~A006。

---

## 0422-K｜明日（2026-04-23，週四）續做

### R5 Phase 2（上午 9:00~12:00，預估 3h）
- **STEP 3 建立採購單**：廠商資訊卡 + 採購明細（runtime 計算 + 稅金）+ **防呆提醒**「金額超過 NT$ 100,000 需主管審核」
- **STEP 4 主管審核**：等待中畫面（送出時間、審核人林主管、通知已送達動畫）+ 模擬「主管已核准」按鈕 + 已核准畫面（審核備註「請確認交期」）
- **STEP 5 寄給廠商**：Email 範本預覽 + 附件清單（PDF/規格書）+ 一鍵寄送動畫
- **STEP 6 廠商回覆**：等待畫面 + 模擬「廠商已回覆」按鈕 + 回覆明細（`buildVendorReplies` 已寫好：全可出/部分缺貨/延後三種戲劇性場景）

### R5 Phase 3（下午 14:00~17:00，預估 3h）
- **STEP 7 建立進貨單**：「✨ 系統已自動生成」展示卡（PO → RR 帶入）+ 為什麼自動的理由列表
- **STEP 8 驗收入庫**：逐項「應收 vs 實收」輸入 + 差異自動提示（例如「差 1 個 → [聯繫廠商補貨] [先入庫]」）+ 外觀 ok/defect 切換
- **STEP 9 完成總結**：本次採購摘要（PO 編號/廠商/項目/金額/耗時）+ 系統自動處理清單（庫存/AP/評分/歸檔）+ 給新人的提醒區塊

### R5 Extra（晚上 20:00~22:00，可選）
- 國外進貨 SOP 差異化（STEP 7-B 等備貨 / 8-B 通知付款 / 9-B 船號飛機號 / 10-B 報關 / 11-B 驗收）
- 新路由 `/dashboard/purchase/sop-demo-import`

### 部署（晚上 22:00~23:00）
- Push Railway preview 準備春酒（需確認：Vercel 前端 auto deploy 是否已 on、Railway 後端是否在跑；前端只需 Vercel 沒問題；SOP 全 mock 不打 API，後端可無）

---

## 0422-L｜給回家電腦的 pull 指令

```bash
git fetch origin
git checkout feature/demo-emergency
git pull origin feature/demo-emergency
# HEAD 應為 1df3253
pnpm install            # 本日無 package.json 變動，可跳過
pnpm --filter nx-ui dev
# iPhone 14 Pro DevTools → 登入任一 TEST 帳號 →
# /dashboard/purchase → 「國內採購」tab → 「SOP 精品示範 🔥」卡 →
# /dashboard/purchase/sop-demo
```

**Crown 驗收 Phase 1 的重點**：
1. STEP 1 勾選互動（預設 3 項、全選群組、最少 1 項防呆）
2. STEP 2 廠商比價視覺（A 級金邊 + 推薦 badge + 贈品提示、最低價 badge、選中態）
3. 9 節點流程條動畫（點 STEP 3~9 的 Placeholder「下一步」預覽綠/金/灰圈過渡）
4. STEP 9 → 「再來一次」→ RESET
5. 頂部返回（STEP 1 回採購中心；其他步驟回上一步）

驗收完若要調視覺細節，回報後再開 Phase 2。

---

## 0422-M｜今日里程碑

- 📄 PROJECT_CONTEXT 知識庫完整建立（未來新對話 Alex/Hank 無縫接手）
- 📱 83 個頁面中 NX02 庫存 / login / 主檔中心 / 三中心 hubs 共 ~15 個頁面有手機版
- 🔧 demo session tier bug 根因定位 + 修復（tenantCode 動態推導）
- 🎯 R5 核心作品 SOP 工作台 infra 完成，STEP 1-2 可展示，明日接續 phase 2/3

明天春酒前最後衝刺。🫡
