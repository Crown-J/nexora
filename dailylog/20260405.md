# 2026-04-05 工作日誌

## 專案資訊
- 專案：NEXORA
- 分支：**`main`**

---

## 今日完成事項（依實際修改順序）

以下為 **NX01-T2（採購模組）**、**MIG006**、**追加驗收修正** 與 **NX-INFRA-T1（全域 ValidationPipe）** 的合併時間線；與舊版日誌中「NX01 階段三儀表板／PO LITE／RFQ 新增倉庫」等條目不重複贅述，僅在本日補齊**後續**實作與修正。

### 1｜資料庫與 Prisma（MIG006）
- **Migration**：`packages/db-core/prisma/migrations/20260407120000_MIG006-nx01-rr-rfq-item-link/migration.sql`  
  - `nx01_rr_item.rfq_item_id`（可空）、索引、FK → `nx01_rfq_item`。  
- **`schema.prisma`**：`Nx01RrItem` ↔ `Nx01RfqItem` 關聯；`pnpm exec prisma generate` 更新 client。  
- **部署**：各環境需執行 `pnpm exec prisma migrate deploy`（與 `docs/flows/11-railway-db-and-tenant-prep.md` 流程一致）。

### 2｜nx-api — NX01 單號、RFQ 回覆、RR／PO 連動
- **`nx01/utils/nx01-doc-no.ts`**：產號前強制倉別碼（`warehouseCode` 空則丟錯），避免 16 碼規則被破壞。  
- **RFQ**  
  - `POST` 建立：`warehouseId` 空值 → **400**（服務層）。  
  - **`PATCH /nx01/rfq/:id/reply`**：已發出（`S`）填寫回覆；明細 `R`／`C`；全數終態後表頭轉 **`R`**。  
  - 路由順序：`PATCH :id/reply` 須在 **`PATCH :id`** 之前。  
  - **`patchReply` 行為**：`C` 清空單價／交期；`R` 時 `lead_time_days: null` 可清空交期，`undefined` 則沿用原明細交期。  
  - 轉單：`toRr`／`toPo` 僅允許 RFQ 狀態 **`R`**。  
- **PO**：建立時可綁 `rfqId`／明細 `rfqItemId`；與 RR 過帳的收貨數量銜接（避免在錯誤階段累加 `received_qty`，改由 RR 過帳處理）。  
- **RR**  
  - DTO／建立流程支援 **`rfqItemId`**（與 RFQ 明細對應）。  
  - **過帳**：可更新 `po_item.received_qty`、條件關閉 PO；自 RFQ 來源且帶 `rfqItemId` 者，可將對應 **`nx01_rfq_item.status` 標為 `S`（SELECTED）**。

### 3｜nx-ui — NX01 前端（TASK 1～5 對應）
- **`RfqDetailView.tsx`**  
  - `D`：草稿編輯、儲存、發出（發出前先成功儲存）。  
  - `S`：料號／數量唯讀；`unit_price`／`lead_time_days` 數字輸入；明細狀態 `R`／`C`；**儲存回覆** → `PATCH …/reply`。  
  - `R`：唯讀；**轉進貨（RR）**；PLUS 時 **轉採購（PO）**。  
  - `C`／`V`：唯讀、無操作。  
  - 追加 UX：改為「不採用」時清空單價／交期；不採用時交期欄 disabled。  
- **`RrNewForm.tsx`**（取代 `RrNewView`）  
  - 來源：**直接進貨**／**從詢價單（`status=R`）**／**從採購單（PLUS，`status=S`）**。  
  - 支援 `?rfq=`、`?po=` 帶入；`createRr` 送 `rfqId`／`poId` 與明細 `rfqItemId`／`poItemId`。  
  - 切換來源時清空明細與關聯 id，避免殘列。  
- **`PoNewForm.tsx`**（取代 `PoNewView`）  
  - **直接採購**／**從詢價單**；`?rfq=`；`createPo` 含 `rfqId`、明細 `rfqItemId`。  
  - `po/new/page.tsx`：`PoLiteAware` + `Suspense`。  
- **`PrNewForm.tsx`**（取代 `PrNewView`）  
  - 必須選已過帳 RR（`GET …/rr?status=P`）；明細勾選與退貨數量；**數量 ≤ 原進貨量**；**單價（原進貨）唯讀**；`?rr=`。  
- **`api/rfq.ts`**：`patchRfq`、`patchRfqReply` 等。  
- **`api/rr.ts`**、**`types.ts`**：RR 明細 `rfqItemId`。  
- **路由**：`rr/new`、`pr/new` 使用 `Suspense` + 新 Form 元件。

### 4｜本日追加（驗收／測試順序相關）
- 已於本機（或目標 DB）執行 **`migrate deploy`**，確認 **MIG006** 套用成功後再測 RFQ→RR 明細 FK。  
- RFQ 回覆與 RR「從詢價單帶入」路徑對齊：**先有 `status=R` 的 RFQ**，再測 LITE 進貨帶入。

### 5｜NX-INFRA-T1 — nx-api 全域 `ValidationPipe`
- 依賴：`class-validator`、`class-transformer`。  
- **`main.ts`**：`whitelist: true`、`forbidNonWhitelisted: false`、`transform: true`。  
- **RFQ 示範 DTO**（class）：  
  - `create-rfq.dto.ts`：`CreateRfqDto`／`CreateRfqItemDto`（含 `contactName`／`contactPhone`、明細 `unitPrice`／`leadTimeDays` 等與現行 API 一致）。  
  - `patch-reply.dto.ts`：`PatchReplyDto`／`PatchReplyItemDto`（JSON 欄位 **`unit_price`／`lead_time_days`** 與前端一致）。  
  - `rfq.dto.ts`：re-export 與 `CreateRfqBodyDto`／`PatchRfqReplyBodyDto` 型別別名，服務層少改動。  
- **whitelist 相容**：補上 **`LoginDto`**、**`UpsertRoleViewDto`／`UpsertRoleViewItemDto`** 之 class-validator 裝飾器，避免登入與 role-view upsert 欄位被靜默剝除。

### 6｜建置驗證
- `pnpm --filter nx-api build`、`pnpm --filter nx-ui build` 於變更後通過。

---

## 與前版日誌的整理說明

- **合併重疊**：先前「今日事項」分兩段（NX01 儀表板／PO LITE 與 NX02 第三批）易與本日 **NX01-T2 + MIG006 + ValidationPipe** 混淆；本檔將 **2026-04-05 當日新增／修正的實作** 集中為上一節之**單一時間線**。  
- **名稱更新**：PR／PO／RR 新增頁若仍寫 `PrNewView`／`PoNewView`／`RrNewView`，請改為 **`PrNewForm`／`PoNewForm`／`RrNewForm`**（與程式一致）。

---

## 延續自前日／背景參考（非本日重複實作清單）

- **NX01 階段三（儀表板、PO LITE 包裝、RFQ 新增倉庫必填、側選單與 list/new/[id] 路由）**：見前一日誌或 Git 歷史；本日在此不重列細節。  
- **NX02 第三批（調撥、缺貨簿、自動補貨、PLUS Guard、planCode 正規化等）**：仍為待驗收／維運注意事項時，請參考下列追蹤段落。

---

## 問題追蹤：DEMO-PLUS 卻顯示「需要 PLUS 方案」

- **現象**：已登入 DEMO-PLUS，PLUS 頁面仍顯示升級提示。  
- **原因**：JWT payload 使用 **`planCode`（camelCase）**；前端 `useSessionMe` 僅讀 **`me.plan_code`（snake_case）** 時會變成 `undefined`，誤判為非 PLUS／PRO。  
- **處理**：於 `useSessionMe` 在寫入 `me` 前呼叫 `normalizeMeDto()`，將 `plan_code` 與 `planCode` 合併為同一值並回寫兩欄位；hook 另回傳 **`planCode`**（與 `me.plan_code` 同步）供頁面使用。  
- **驗證建議**：DevTools → Application → Local Storage 取 JWT，jwt.io 解碼確認 `planCode`；Network → `/auth/me` 確認回應是否為 snake 或 camel（若有 API 閘道改寫鍵名，正規化後應一致）。

### 追加根因（仍顯示需要 PLUS）

- **頂欄「NEXORA Demo PLUS」** 來自租戶 **`name_en`**（例：`seed` 中 `DEMO-PLUS` 租戶），**不是**訂閱方案的技術代碼。  
- **`nx99_plan.code` 實際為 `NEXORA-PLUS` / `NEXORA-LITE` / `NEXORA-PRO`**，JWT 與 `/auth/me` 的 `plan_code` 亦為此格式。  
- 前端與 `PlusPlanGuard` 原先只接受字串 **`PLUS`／`PRO`**，故 **`NEXORA-PLUS` 永遠被判成非 PLUS**。  
- **修正**：共用 `planSupportsNx02PlusFeatures()`，同時接受 `NEXORA-PLUS`、`NEXORA-PRO`、`NEXORA-ENTERPRISE` 與簡寫（nx-api guard + nx-ui 各一檔，邏輯對齊）。

### 仍 403「此功能需 PLUS 或 PRO」（API）

- **現象**：前端已能進列表 UI，但 `GET /nx02/transfer` 回 403，錯誤與 Guard 訊息相同。  
- **原因**：`JwtStrategy.validate()` 原先僅在 **`planCode` 為空** 時才查 `nx99_subscription`；若 JWT 仍帶**舊的** `planCode`（例如曾為 LITE、或格式與 DB 不一致），會**永遠不刷新**，`PlusPlanGuard` 讀到的仍是錯的方案。  
- **修正**：只要有 `tenantId`，**一律先讀 DB 有效訂閱（`status: 'A'`）的 `plan.code`，有則採用，否則才退回 JWT payload**。

---

## 缺貨紀錄未出現／調撥過帳與 Hank 確認事項

### 程式面（已實作）

- **開帳存過帳後**：每筆明細在 `nx02_stock_balance` upsert 之後會呼叫 `ShortageService.detect()`（`apps/nx-api/src/nx02/init/init.service.ts` 約在 balance `update` 區塊末尾）。  
- **調撥過帳後**：每筆明細對**來源倉、目標倉**各呼叫一次 `detect()`（`apps/nx-api/src/nx02/transfer/transfer.service.ts`，過帳迴圈內）。  
- **缺貨偵測條件**（`shortage.service.ts`）：須存在**啟用**的 `nx02_part_stock_setting` 且 **`min_qty > 0`**，且 **`on_hand_qty < min_qty`** 才會建立／更新 OPEN 缺貨紀錄。僅有庫存、無庫存設定，**不會**出現缺貨簿列項。

### 資料面（過去 seed 缺口）

- 先前 **`prisma/seed.ts` 未寫入** DEMO-PLUS 的 `nx02_part_stock_setting`，故即使用開帳存把庫存壓在 100 以下，**仍不會觸發缺貨列**。  
- **已補**：`seedDemoPlusNx02StockSettingForShortage` — DEMO-PLUS、倉別 **Z01（台北主倉）**、料號對應 seed 之 **「機油濾芯 03N115561」**（`nx00_part.name` 含 `03N115561`）、**`min_qty = 100`**。重跑 seed 後新環境會自動有設定；**已存在的雲端 DB** 請 Hank 用下列 SQL 或 UI「庫存設定」補一筆同等條件。

### 請 Hank 協助確認

1. **庫存設定是否存在**（租戶 `code = 'DEMO-PLUS'`、倉 `code = 'Z01'`、零件為展示料 **VAG-03N 115 561**／或 `name` 含 `03N115561`）：

```sql
SELECT ps.id, t.code AS tenant_code, w.code AS wh_code, p.code AS part_code, p.name AS part_name,
       ps.min_qty, ps.is_active
FROM nx02_part_stock_setting ps
JOIN nx99_tenant t ON t.id = ps.tenant_id
JOIN nx00_warehouse w ON w.id = ps.warehouse_id
JOIN nx00_part p ON p.id = ps.part_id
WHERE t.code = 'DEMO-PLUS' AND w.code = 'Z01'
  AND (p.code ILIKE '%03N%' OR p.name LIKE '%03N115561%');
```

預期：至少一筆、`min_qty = 100`、`is_active = true`。

2. **`detect()` 是否在開帳存過帳路徑被呼叫**：以程式為準（見上 `init.service.ts`）；若仍無列項，優先查 **(1) 無 stock_setting** 或 **(2) 過帳後 on_hand 仍 ≥ min_qty**。

### 一鍵轉 RFQ 500（`nxui_nx02_shortage_torfq_001`）

- **推論**：後端寫入 **`nx01_rfq`／`nx01_rfq_item`** 失敗；常見為 Railway **未套用 MIG004**，表不存在。  
- **Hank／維運**：DBeaver 執行下列查詢；若無列則在 `packages/db-core` 設好 **`DATABASE_URL`**（必要時 **`DIRECT_URL` 直連**，見 `prisma.config.ts`）後執行 **`pnpm exec prisma migrate deploy`**，再重試 UI。細節已併入 **`docs/flows/11-railway-db-and-tenant-prep.md`**「NX02 PLUS：缺貨『一鍵轉 RFQ』與 MIG004」小節。

```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
  AND table_name IN ('nx01_rfq', 'nx01_rfq_item');
```

**補充（本日）**：若 RR 需連結 RFQ 明細，另需 **MIG006**（`nx01_rr_item.rfq_item_id`）；請一併確認 migration 已全部套用。

### 調撥單過帳截圖

- 調撥過帳流程已在 API 實作；**我無法代為操作正式站並產出截圖**。請於 **DEMO-PLUS** 建立草稿 → **過帳** 後自行截圖（列表狀態「已過帳」或明細頁）；若需缺貨連動，目標倉需有該料之 **min_qty** 且過帳後 **on_hand < min_qty**。
