# 2026-04-05 工作日誌

## 專案資訊
- 專案：NEXORA
- 分支：**`main`**

## 今日事項（NX02 第三批 — 已實作，待 Alex 驗收）

1. **NX02 第三批（PLUS）— 調撥單 + 缺貨簿 + 自動補貨設定**  
   - **規格**：`docs/NEXORA_SRS-03_NX02_v1.0.0.md`、`docs/nx02_field.csv`  
   - **PLUS 管控**：`/nx02/transfer`、`/nx02/shortage`、`/nx02/auto-replenish` 後端 LITE → 403 `PLAN_NOT_SUPPORTED`；前端 LITE → 升級提示、不顯示功能。  
   - **後端**：`transfer`／`shortage`／`auto-replenish` 模組；調撥過帳於 transaction 內寫 ledger O/X、I/X；`ShortageService.detect()` 於開帳存／盤點／調撥過帳後呼叫；缺貨轉 RFQ 需 **nx01_rfq**（MW2 簡化表 + migration）。  
   - **前端**：調撥列表／新增／明細；缺貨簿查詢＋批次轉 RFQ／忽略；自動補貨左清單＋右表單（風格同庫存設定）。  
   - **驗收**：見任務清單（調撥過帳與均價、缺貨偵測與關閉、自動建調撥草稿等）。

## 延續自前日
- NX02 第二批（開帳存、庫存設定、盤點單）已驗收通過。

## 問題追蹤：DEMO-PLUS 卻顯示「需要 PLUS 方案」

- **現象**：已登入 DEMO-PLUS，PLUS 頁面仍顯示升級提示。  
- **原因**：JWT payload 使用 **`planCode`（camelCase）**；前端 `useSessionMe` 僅讀 **`me.plan_code`（snake_case）** 時會變成 `undefined`，誤判為非 PLUS／PRO。  
- **處理**：於 `useSessionMe` 在寫入 `me` 前呼叫 `normalizeMeDto()`，將 `plan_code` 與 `planCode` 合併為同一值並回寫兩欄位；hook 另回傳 **`planCode`**（與 `me.plan_code` 同步）供頁面使用。  
- **驗證建議**：DevTools → Application → Local Storage 取 JWT，jwt.io 解碼確認 `planCode`；Network → `/auth/me` 確認回應是否為 snake 或 camel（若有 API 閘道改寫鍵名，正規化後應一致）。

### 追加根因（仍顯示需要 PLUS）

- **頂欄「NEXORA Demo PLUS」** 來自租戶 **`name_en`**（例：`seed` 中 `DEMO-PLUS` 租戶），**不是**訂閱方案的技術代碼。  
- **`nx99_plan.code` 實際為 `NEXORA-PLUS` / `NEXORA-LITE` / `NEXORA-PRO`**，JWT 與 `/auth/me` 的 `plan_code` 亦為此格式。  
- 前端與 `PlusPlanGuard` 原先只接受字串 **`PLUS`／`PRO`**，故 **`NEXORA-PLUS` 永遠被判成非 PLUS**。  
- **修正**：共用 `planSupportsNx02PlusFeatures()`，同時接受 `NEXORA-PLUS`、`NEXORA-PRO`、`NEXORA-ENTERPRISE` 與簡寫（nx-api guard + nx-ui 各一檔，邏輯對齊）。

### 仍 403「此功能需 PLUS 或 PRO」（API）

- **現象**：前端已能進列表 UI，但 `GET /nx02/transfer` 回 403，錯誤與 Guard 訊息相同。  
- **原因**：`JwtStrategy.validate()` 原先僅在 **`planCode` 為空** 時才查 `nx99_subscription`；若 JWT 仍帶**舊的** `planCode`（例如曾為 LITE、或格式與 DB 不一致），會**永遠不刷新**，`PlusPlanGuard` 讀到的仍是錯的方案。  
- **修正**：只要有 `tenantId`，**一律先讀 DB 有效訂閱（`status: 'A'`）的 `plan.code`，有則採用，否則才退回 JWT payload**。
