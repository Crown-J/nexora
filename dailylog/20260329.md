# 2026-03-29 工作日誌

> 以下區塊請依**當日實際工作**填寫；勿將任務說明中的範例文字當成已完成事項。

## 專案資訊
- 專案：NEXORA
- 開發者：Crown Lin（Hank 執行）
- **紀錄約定**：程式或規格有任何更動，須同步寫入 `dailylog/` 當日檔案，避免分支與口頭決策未留痕。

## 今日完成事項

1. **Dashboard 殼層對齊新風格（Dock / TopBar / 背景）**  
   - **`DashboardShell`** 改為使用 **`HomeLandingChrome`**（與 `/home`、`/base` 相同：星空 **`ParticleField`**、極光、`bg-background`、左側 **`Dock`** + **`MobileDock`**）。  
   - 頂欄改用 **`HomeTopBar`**，並新增 **`centerContent`** 置中放入 **`TopModuleTabs`**；點商標改為 **`/home`**。  
   - **`TopModuleTabs`** 改 **語意色票**（`primary` / `border` / `secondary`），**HOME** 導向 **`/home`**（`/dashboard` 亦視為 HOME 高亮）。  
   - 舊版 **`SideMenu`**（340px 白邊玻璃欄）改為 **`DashboardSubNav`**：同源 `resolveSideMenuGroups`，**橫向**次功能連結。  
   - 內容框改 **`border-border` / `bg-card`**；**`useSessionMe`** 載入／錯誤與 `/home` 一致。

2. **NX01 採購流程式首頁（分支 `feature/nx01-procurement-flow-hub`）**  
   - 將 **`/dashboard/nx01`** 由占位改為 **「採購與進貨作業」** 流程總覽：橫向 **六階段**（詢價→庫存覆核→採購→確認訂單→待驗收→驗收單）、階段 **mock 待辦**、**常用功能** 捷徑。  
   - **視覺改為與 `/base` 主檔 Hub 一致的新系統語意色票**（`foreground` / `muted-foreground` / `primary` / `border` / `card`），**避免**舊版「主檔網格式」四格大卡片與厚重 `glass-card` stepper；改為 **輕量流程軸節點＋連接線**、待辦 **列表區塊**、底部 **橫向文字工具列**（非 grid 卡片）。  
   - **「+ 新增詢價」** 連結 **`/dashboard/nx01/stock-replenishment`**；其餘階段 CTA 暫 **disabled**（待後端）。  
   - **供應商搜尋** 連結 **`/base/partner`**；匯入／匯出／歷史為占位。  
   - 新增 **`apps/nx-ui/src/features/nx01/procurement/ProcurementFlowHub.tsx`**；側邊選單「模組首頁」改為 **「採購作業總覽」**（`menu.nx01.ts`）。

3. **`nx-ui` 本機建置失敗排查**（`pnpm --filter nx-ui build`）  
   - 現象：Turbopack 回報 `Module not found`，無法解析 `@radix-ui/react-dialog`、`@radix-ui/react-label`、`@radix-ui/react-tabs`。  
   - 原因：`package.json` 與根目錄 `pnpm-lock.yaml` 已宣告上述套件，但 `apps/nx-ui/node_modules/@radix-ui` 實際缺少對應目錄（`node_modules` 與 lockfile **不同步**）。  
   - 處理：於 **monorepo 根目錄**執行 `pnpm install`，補齊連結後建置通過。  
   - 備註：Windows PowerShell 不支援 `&&` 串指令時，可改 `Set-Location <repo>; pnpm --filter nx-ui build`。

4. **Vercel 部署「建置成功但首頁 404」說明與設定修正**  
   - 原因摘要：Next.js 的 `.next` 為**框架內部輸出**，不應在 Vercel 當成靜態站的 **`outputDirectory`**；先前日誌（`20260328.md` 第 12 點）將 `outputDirectory` 設為 `apps/nx-ui/.next` 的作法，易導致部署狀態 Ready、實際路由卻 404。另 **`distDir: ".next"`** 本為 Next 預設，無法單靠它修正 Vercel 行為。  
   - 程式／設定變更：  
     - **`apps/nx-ui/next.config.ts`**：已移除僅等於預設值的 `distDir: ".next"` 及相關註解（先前提交）。  
     - **不在 repo 內放 `apps/nx-ui/vercel.json`**（見下方第 5 點）：改由 Vercel 專案設定處理 monorepo 安裝指令。  
   - Vercel 後台建議再確認：**Root Directory** = `apps/nx-ui`、**Include files outside the root directory** 開啟；**勿**自訂 **Output Directory**；**勿**保留舊版 Production 覆寫（`Framework: Other`、`Output Directory: apps/nx-ui/.next` 等）；若仍異常可 **Redeploy（不使用 cache）**。

5. **Vercel `JSON5: invalid character 'h' at 2:9`（精簡 `vercel.json` 後仍失敗）**  
   - 現象：CLI 一載入專案設定即失敗，未進入 `pnpm install`。  
   - 釐清：精簡後之 **`apps/nx-ui/vercel.json` 第 2 行為 `"framework": "nextjs"`，第 9 欄為字母 `w` 而非 `h`**，故錯誤較可能來自 **Vercel 將「儀表板 Production 覆寫／舊部署設定」與檔案合併後的內容**，而非單一檔案字面。先前推測僅因 `$schema` 的 `https://` 觸發註解誤判，**無法解釋**拿掉 `$schema` 後仍同錯誤，故予以修正敘述。  
   - 處理：**刪除 repo 內 `apps/nx-ui/vercel.json`**，避免與後台設定重複合併；請在 Vercel **Project → Settings → Build & Deployment** 手動設定 **Install Command**：`pnpm --dir ../.. install --frozen-lockfile`（Vercel 仍會先 `cd` 至 Root Directory，再執行此指令）。並 **關閉／清除** Build Command、Output Directory、Framework 等覆寫，改回 **Framework Preset：Next.js**、預設 build（`next build`）。  

6. **Monorepo 單一 pnpm lockfile**  
   - **刪除** `apps/nx-ui/pnpm-lock.yaml`、`apps/nx-ui/pnpm-workspace.yaml`（避免子目錄自成一套 lock／workspace，與根目錄 `pnpm-lock.yaml` 脫節）。  
   - 將原 nx-ui 內 **`ignoredBuiltDependencies`（sharp、unrs-resolver）** 併入 **根目錄 `pnpm-workspace.yaml`**，行為與本機一致。

7. **`JSON5: invalid character 'h' at 2:9` 追蹤**  
   - **`fac54dc`**（無 repo `vercel.json`）仍錯：指向 **Vercel 後台持久化／合併設定**，非僅 Git 檔案。  
   - **`697c5f0`**（**單行** `vercel.json` 僅 `installCommand`、新專案部署）**仍**同一錯誤：檔案內容僅一行 JSON、無 `https`／`//`，**第 2 行不應出現字元 `h`**，故判定為 **只要存在 `apps/nx-ui/vercel.json`，CLI 在解析／合併階段即觸發 bug 或壞合併**。  
   - **處理**：**刪除 `apps/nx-ui/vercel.json`**。本機已驗證在 **`apps/nx-ui` 目錄執行** `pnpm install --frozen-lockfile` 會向上找到 monorepo 根並 **Scope: all 4 workspace projects**，故 Vercel 預設 **Root Directory = `apps/nx-ui` + 預設 `pnpm install`** 即可，**不必**在檔內寫 `installCommand`。後台 **Install Command 覆寫請維持關閉**；其餘覆寫仍應清空（見第 5 點）。

8. **Vercel `JSON5: invalid character 'h' at 2:9` 真正根因**（`36f2c49` 仍錯之後釐清）  
   - 錯誤**與** `apps/nx-ui/vercel.json` **無關**；來自 **repo 根目錄 `turbo.json` 第 2 行**：鍵名誤為 `\"`（應為 **`$schema`**），導致 JSON5 解析在 **`https://…` 的 `h`（第 2 行第 9 欄）** 報錯。Monorepo 啟用「根目錄外檔案」時，Vercel 會讀到根目錄 `turbo.json`。  
   - 已改為：`"$schema": "https://turbo.build/schema.json"`。

9. **Vercel：Turbo 建置**（`0039ab9` 之後）  
   - 現象：`turbo run build` 報 **`Missing packageManager field in package.json`**。  
   - 處理：根目錄 **`package.json`** 新增 **`packageManager": "pnpm@10.29.3"`**；新增 **`turbo`** 為 root **`devDependency`**（與 lockfile 一致），避免僅用全域 Turbo。  
   - **`turbo.json`**：`pipeline` 改為 Turbo 2.x 的 **`tasks`**；`build` 的 **`outputs`** 補 **`!.next/cache/**`**（與 Vercel KB 建議一致）；`dev` 加 **`persistent": true`**。

10. **`nx-ui`：development / demo 雙模式（登入與 session）**  
   - **目的**：開發時接真後端與 PostgreSQL；外出對客戶手機展示時可不依賴 API／DB。  
   - **環境變數**：`NEXT_PUBLIC_NEXORA_RUN_MODE`  
     - 未設定或 `development`：**真實** `POST /auth/login`、`GET /auth/me`，需 `NEXT_PUBLIC_API_URL` 指向可連線的 `nx-api` 與資料庫。  
     - `demo`：**短路**登入（不發 HTTP）、以固定 token + `sessionStorage` 暫存輸入之帳號字串組假 `me`；不呼叫 `/auth/me`。  
   - **程式變更摘要**：  
     - 新增 `apps/nx-ui/src/features/auth/run-mode.ts`（讀取 `NEXT_PUBLIC_NEXORA_RUN_MODE`）。  
     - 新增 `apps/nx-ui/src/features/auth/demo-session.ts`（展示用帳號 key、`buildDemoMeFromStorage`）。  
     - `apps/nx-ui/src/features/auth/constants.ts`：`NEXORA_DEMO_ACCESS_TOKEN`、`NEXORA_DEMO_USERNAME_SESSION_KEY`。  
     - `apps/nx-ui/src/features/auth/types.ts`：抽出共用 **`MeDto`**。  
     - `apps/nx-ui/src/features/auth/api/login.ts`：demo 時回傳假 token 並寫入 demo session。  
     - `apps/nx-ui/src/features/auth/hooks/useSessionMe.ts`：demo token 時略過 `apiFetch('/auth/me')`；`logout` 清 demo session。  
     - `apps/nx-ui/src/shared/api/client.ts`：**development** 仍強制 `NEXT_PUBLIC_API_URL`；**demo** 未設 URL 時 fallback `http://127.0.0.1:9`（避免 import 階段拋錯；其餘 API 呼叫仍會失敗，屬預期）。  
     - `apps/nx-ui/src/app/login/page.tsx`：demo 時顯示琥珀色「展示模式」提示。  
     - `apps/nx-ui/.env.example`：補上模式說明與 demo 可不設 API URL 的註解。  
   - **操作說明**（見下方「討論與決策」新增段落）。

11. **登入後首屏與 `/dashboard` 根路徑分工**  
   - **首頁（新 Landing）**：`/home` — `HomeLandingChrome` + Dock + 行事曆／公告等。  
   - **主檔總覽**：`/base` — `MASTER_HUB_CARDS` 卡片；子頁 `/base/[segment]`、`/base/user`（舊 `/base/users` 轉址）、`/base/role`、`/base/role-view` 等。  
   - **作業模組**：`/dashboard/nx01`～`nx04` — `DashboardShell`（與 `/home` 相同 **Dock + HomeTopBar + 星空背景**）；**主檔不再使用 NX00 儀表板內頁**（見下方「討論與決策」第 14 點）。  
   - 先前將 `router.replace('/dashboard')` 搭配 `redirect('/dashboard/nx00')`，登入後**直接進 Shell**，看不到 `/home` 與 `/base`，故使用者回報「新首頁／主檔不見」。  
   - 變更：  
     - `apps/nx-ui/src/app/login/page.tsx`：登入成功改為 **`router.replace('/home')`**。  
     - `apps/nx-ui/src/app/dashboard/page.tsx`：改為 **`redirect('/dashboard/nx01')`**（主檔改走 `/base`）。

12. **Demo 模式固定帳密**  
   - `NEXT_PUBLIC_NEXORA_RUN_MODE=demo` 時，登入改為僅接受帳號 **`demo`**、密碼 **`nexora2026`**（定義於 `apps/nx-ui/src/features/auth/constants.ts`）；錯誤時顯示與正式類似的「帳號或密碼錯誤」。  
   - 登入頁展示模式提示改為：「有需要請洽相關人員。」（仍屬前端比對，僅擋一般路人。）

13. **Git：分支 `feature/nx01-procurement-flow-hub` 合併至 `main` 並推送**  
   - 合併內容摘要：**NX01 採購流程式首頁**（`ProcurementFlowHub`）、採購頁語意色票調整、**Dashboard 殼層**改 **`HomeLandingChrome` + `HomeTopBar` + `DashboardSubNav`** 等。  
   - 相關提交（由舊到新）：**`8dff5d2`** → **`6704ccf`** → **`3f7363e`**；合併後 **`main`** 已 **`git push origin main`**。

## 補充／待辦
- 已推送 **`main`** 者可再觸發 Vercel 部署驗證；後台覆寫維持 Next.js 預設即可。

## 討論與決策（Q&A）

### `NEXT_PUBLIC_NEXORA_RUN_MODE`：怎麼用

| 模式 | `.env.local`（本機）或 Vercel Environment Variables | 行為 |
|------|-----------------------------------------------------|------|
| **development** | `NEXT_PUBLIC_NEXORA_RUN_MODE=development` 或**整行省略** | 與先前相同：需 `NEXT_PUBLIC_API_URL`（如 `http://localhost:3001`）、本機跑 `nx-api` + PostgreSQL + migrate/seed，用真帳密登入。 |
| **demo** | `NEXT_PUBLIC_NEXORA_RUN_MODE=demo` | 登入頁**任意帳號＋密碼**（欄位仍須非空）即可進 `/dashboard`；**不**打後端。其他仍呼叫 API 的頁面可能顯示錯誤，可搭配既有 mock 資料頁面展示。 |

**注意**：`NEXT_PUBLIC_*` 在 Next 為**建置時**嵌入；改變數後需 **`pnpm dev` 重啟** 或 **Vercel Redeploy**。正式產品網域請勿設 `demo`。

**Vercel 建議**：可開**第二個專案**或 Preview 分支專用 demo：`NEXT_PUBLIC_NEXORA_RUN_MODE=demo`；開發用專案維持 `development` 或不設。

**登出**：會清除 access token 與 demo 用的 `sessionStorage` 鍵，回到登入頁。

12. **主檔：職務 `/base/role`**（分支 `feature/base-role-page`）  
    - 左側職務列表含搜尋與**新增**；右側上為詳細資訊、下為匯入使用者（mock）；風格與使用者主檔一致（`glass-card`、語意色票）。  
    - Hub 職務卡片改連結 **`/base/role`**；`BASE_SEGMENT_TITLES` 補 `role`。

13. **主檔路由調整：`/base/user` 與權限頁 `/base/role-view`**  
    - **使用者主檔**正式路徑改為 **`/base/user`**（`apps/nx-ui/src/app/base/user/page.tsx`）；**`/base/users`** 保留為 **`redirect('/base/user')`**，避免舊書籤失效。  
    - **`/user`** 捷徑改為轉向 **`/base/user`**。  
    - Hub「使用者」卡片：`id` → `user`、`href` → `/base/user`；`BASE_SEGMENT_TITLES` 補 **`user`**（並保留 `users` 占位標題）。  
    - **權限設定**：新頁 **`/base/role-view`**（`BaseRoleViewSplitView` + 既有 `useRoleView`／API）；Hub「權限設定」卡片改 **`href: '/base/role-view'`**、`id: 'role-view'`；`BASE_SEGMENT_TITLES` 補 **`role-view`**（保留 `permissions` 占位）。  
    - **`RoleViewMatrix`**：新增 **`appearance: 'dashboard' | 'base'`** 與 **`showMatrixHeader`**，`base` 時採主檔語意色票；Dashboard NX00 仍用預設 `dashboard`。

14. **NX00 儀表板主檔廢止，統一走 `/base`**  
    - 決策：**`/dashboard/nx00` 及子路徑僅作轉址**，實際畫面以主檔 **`/base/*`** 為準；`nx00_view` 的 `path` 與 seed 同步改為 `/base/...`。  
    - **`apps/nx-ui/src/app/dashboard/nx00/**`**：各頁改 **`redirect`** 至對應 **`/base/user`**、`/base/role`、`/base/role-view`、`/base/part`**（等）、`/base/location`、`/base/user-role`…；模組根 **`/dashboard/nx00`** → **`/base`**。（後續見第 16 點路徑精簡。）  
    - **`apps/nx-ui/src/app/dashboard/page.tsx`**：預設改 **`redirect('/dashboard/nx01')`**。  
    - **Top 模組 Tab**：原「基本資料」改標 **`主檔`**、連結 **`/base`**；在 **`/base`…** 路徑時高亮該 Tab（`TopModuleTabs`）。  
    - **`menu.nx00.ts`**：連結改為 **`/base/*`**（備用；多數情境會先被轉址帶離 nx00）。  
    - **`packages/db-core/prisma/seed.ts`**：`nx00_view` 路徑更新；新增 migration **`20260329120000_nx00_view_paths_to_base`** 更新既有 DB。  
    - **`BASE_SEGMENT_TITLES`**：補 **`user-role`**（占位頁標題）。

15. **主檔 hub 與占位外殼（迭代前）**  
    - 曾將廠牌／往來卡片做成雙 pill；**第 16 點**改為單一路徑並在頁內 **Tab** 切換，Hub 改 **單一 `href`**。  
    - **`BaseMasterSubPageLayout`**、**`BaseMasterPlaceholderSection`**、**`/base/[segment]`** 標頭與使用者頁對齊仍沿用。

16. **主檔實作頁（mock）**  
    - **`/base/part`**：與使用者相同版型（左上搜尋、左列表、右明細）。  
    - **`/base/brand`**：同上 + **Tab（汽車廠牌／零件廠牌）**。  
    - **`/base/part-group`**：族群主檔同上。  
    - **`/base/location`**：比照職務頁——左倉庫列表、右上倉庫明細、右下**新增庫位**（mock）。  
    - **`/base/partner`**：同上使用者版型 + **Tab（廠商／客戶）**。  
    - Hub 與 **`nx00_view` seed** 路徑改為 **`/base/part`**、`/base/brand`、`/base/part-group`（僅 hub，無獨立 view code）、`/base/location`、`/base/partner`；舊路徑 **`/base/parts`** 等保留 **redirect**。  
    - Migration **`20260329203000_nx00_view_paths_master_pages`**。

17. **主檔子頁標題列：快速切換圖示（免回 `/base` 總覽）**  
    - 需求：在「MASTER DATA」標題列右側以小圖示捷徑切換各主檔，與 hub 卡片同源、減少往返。  
    - 新增 **`apps/nx-ui/src/features/base/shell/BaseMasterQuickNav.tsx`**：依 `MASTER_HUB_CARDS` 渲染 `Link`＋ Lucide 圖示，`usePathname()` 比對 `href` 標示目前頁（`aria-current`、樣式）。  
    - 新增 **`BaseMasterPageHeader.tsx`**：整合「返回主檔」、標題說明與右側 `BaseMasterQuickNav`（大螢幕與標題同列右對齊）。  
    - **`BaseMasterSubPageLayout`** 改使用 `BaseMasterPageHeader`；**`/base/user`**、**`/base/role`**、**`/base/role-view`** 自訂外殼頁同步改用同一標頭元件。

18. **`/base/part-group` 前端對齊表 nx00_part_group**（僅 mock UI，未接 API）  
    - 移除舊欄位：族群代碼、上層代碼；改為 **name**、**car_brand_id**（UI：`carBrandId`，下拉取自 **`MOCK_BASE_BRANDS` 之 vehicle** 模擬 **nx00_car_brand**）、**seg1～seg5**、**sort_no**、**is_active**，以及唯讀稽核 **created_at / created_by / updated_at / updated_by**。  
    - **`id`** 仍僅內部使用，畫面不顯示。  
    - 篩選：關鍵字 + 汽車廠牌；列表列示匹配條件摘要。  
    - 檔案：**`features/base/part-group/mock-data.ts`**、**`BasePartGroupMasterView.tsx`**；**`app/base/part-group/page.tsx`** 與 **`master-cards`** 說明文字同步。

## 明日待辦
- [ ] 依實際進度延續功能或後端串接。

## 遇到的問題與解法
| 問題 | 解法 |
|------|------|
| `pnpm --filter nx-ui build` 找不到 `@radix-ui/*` | 在 repo 根執行 `pnpm install`，使 `apps/nx-ui/node_modules` 與 lockfile 一致。 |
| Vercel 建置成功、線上卻 404 | 勿把 `.next` 當 `outputDirectory`；用 Next 預設部署流程；Root Directory = `apps/nx-ui`，Install 自 monorepo 根安裝（見下格）。 |
| Vercel `JSON5: invalid character 'h' at 2:9` | 檢查 **根目錄 `turbo.json`**：`$schema` 鍵名不可壞成 `\"`；並確認其他 JSON 合法。 |
