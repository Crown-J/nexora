# 2026-04-15 工作日誌

## 時間未記錄｜[NX01-DASH] 雙入口卡圖示尺寸對齊一般 Hub（32px 框／16px icon）

- **完成內容**：`DualEntryHubCard` 頂部兩入口由 `flex-1`+`py-2`+`h-5` 改為與單卡相同 **`h-8 w-8` `rounded-lg` `bg-secondary/50`**，內圖 **`h-4 w-4`**；保留橘金邊框 hover。
- **本機驗證**：檔案小改，未重跑 build。

## 時間未記錄｜[NX01-DASH] 雙入口主檔卡改頂部圖示（汽車／零件廠牌、倉庫／庫位）

- **完成內容**：`master-cards` 新增 `MasterHubCardLink.entryIcon`；廠牌卡為 **CarFront**／**Tags**，倉庫卡為 **Warehouse**／**MapPin**。`DualEntryHubCard` 頂部改為並排兩顆橘金邊圖示 `Link`（無文字），其下標題＋簡述；底層全卡仍連第一順位。移除雙入口卡左上角模組小圖（避免與雙圖示重複）。
- **本機驗證**：`pnpm --filter nx-ui exec tsc --noEmit` 通過

## 時間未記錄｜[NX01-DASH] 主檔中心：恢復說明、雙入口橘金按鈕、側欄更名

- **完成內容**：`/dashboard/base` 標題改**主檔中心**；一般卡恢復一行說明（`line-clamp-2`）。「汽車／零件廠牌」「倉庫及庫位」：圖示下為兩顆**全寬圓角橘金邊按鈕**（採購中心色系），點按鈕進子頁；全卡底層 `Link` 覆蓋 `z-0` 指向 **links 第一順位**（汽車廠牌／倉庫主檔），其餘區域點擊進預設主檔。`modules.ts` 側欄模組標籤同步為「主檔中心」。
- **本機驗證**：`pnpm --filter nx-ui exec tsc --noEmit` 通過

## 時間未記錄｜[NX01-DASH] 主檔首頁卡片精簡（移除說明與統計）

- **完成內容**：`/dashboard/base` Hub 卡固定 220×160 後版面過擠；移除卡片內**說明文字**與**底部統計區**（含 API 載入摘要）；單入口卡僅保留圖示＋標題＋hover 箭頭；汽車／零件廠牌、倉庫及庫位保留底部兩顆入口連結。頁首副標改為不再提及「摘要數字」。
- **本機驗證**：`pnpm --filter nx-ui exec tsc --noEmit` 通過

## 時間未記錄｜[NX02-DASH] Hub 卡片統一 220×160 與退貨／保固雙層虛線迴圈

- **完成內容**：新增 `shared/lib/hubCardDimensions.ts`（`HUB_CARD_WIDTH`/`HEIGHT` 220／160、`hubCardShellBaseClass`：`p-5`、`rounded-xl`）；主檔中心 `dashboard/base/page.tsx` 與採購中心卡片皆套用；主檔網格改 `flex flex-wrap gap-6` 固定寬卡。採購虛線改為自進貨頂中央 **上 20px** 水平至退貨頂再垂入（`strokeDasharray: 6 4`）；國內另 **上 40px** 一條至保固；終點 **↓ 向小三角**（polygon）；主線改 **→→**；`ResizeObserver` 監聽 root／進貨／退貨／保固（國內）。流程區 `pt-12` 預留弧線上方空間。
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 `/dashboard/base`、`/dashboard/purchase` 擷圖確認。

## 時間未記錄｜[NX02-DASH] 採購中心版面（橫向主流程＋進貨頂部虛線繞接）

- **完成內容**：`PurchaseCenterHub` 依 Crown 規格重排：無 GROUP 標題；群組間 `space-y-12`（48px）、群組內卡片 `gap-6`（24px）；管理區靠左兩卡無連線；國內／特殊皆單列橫排（`md:flex-row`＋必要時橫向捲動）。主線詢價→進貨→驗收為實線橘金 `#E8A020`＋`→`；退貨／保固支線為 `rgba(232,160,32,0.4)` 虛線，`useLayoutEffect`＋`ResizeObserver` 自「進貨」卡頂量測 SVG 路徑（經上方水平再垂入終點卡頂）。卡片 hover 加橘金邊框光暈；右下 badge 同色；子選單文案改為規格列表（無 emoji）。外層 padding 仍由 `DashboardShell` 與主檔中心一致。
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機開 `/dashboard/purchase` 全寬擷圖（含三區與虛線弧）確認。

## 時間未記錄｜[NX02-DASH] 採購中心首頁（流程卡片＋Dropdown）

- **完成內容**：`/dashboard/purchase` 改為 `PurchaseCenterHub`；標題 PURCHASE CENTER／採購中心；主檔風格 `glass-card`（圖示左上、粗體名、灰說明、右下數量 badge）；`mockPurchaseCounts`；GROUP 管理／國內／特殊；主線 `rgba(232,160,32,0.4)` 虛線＋→／支線↓；點卡片開 Radix `DropdownMenu` 子功能連結（nx01 RFQ/PO/RR/PR、base parts/partners 等）；保固先連 `?focus=warranty`。（後續已由上一則條目改版為橫向流程與實線／虛線分離。）
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 `/dashboard/purchase` 自行擷圖。

## 時間未記錄｜[SYS-DASH-UI] PRO 事件簿外層 scrollbar-gutter 與行事曆寬差

- **完成內容**：PRO 行事曆／事件簿外層容器誤用 `nx-master-scroll`（`scrollbar-gutter: stable`），事件簿整卡被「預留捲軸槽」擠窄；改外層僅 `overflow-hidden`，捲動仍由 `EventBookCard` 內 `ul` 的 `nx-master-scroll` 負責。月曆標題列加 `min-w-0`／`+新增` 區 `shrink-0`。
- **本機驗證**：`pnpm --filter nx-ui build` 通過

## 時間未記錄｜[SYS-DASH-UI] 行事曆可縮欄寬、與事件簿同寬＋PRO 區塊拉高

- **完成內容**：月曆 7 欄改 `repeat(7,minmax(0,1fr))`、日期格 `min-w-0`，避免內容最小寬度撐破欄寬；卡片 `max-w-full`、與事件簿同 `p-4`；日期格 `h-10`；PRO 左欄行事曆＋事件簿外層 `h-full`，行事曆 `flex-[1.12]`＋`fillContainerHeight` 多佔垂直空間。
- **本機驗證**：`pnpm --filter nx-ui build` 通過

## 時間未記錄｜[SYS-DASH-UI] 行事曆與事件簿寬度對齊

- **完成內容**：`CalendarCard`／`EventBookCard` 改為 `w-full min-w-0`，移除卡片上 `max-w-[600px]`、`mr-auto`（與 `fillContainerHeight` 分支不一致導致寬度不一）；LITE／PLUS 仍由外層 `max-w-[600px]` 限制欄寬；PRO 左欄堆疊加 `w-full` 包裝。
- **本機驗證**：`pnpm --filter nx-ui build` 通過

## 時間未記錄｜[SYS-DASH-UI] CalendarCard 高度縮短

- **完成內容**：日期格 `h-11`→`h-9`、格線 `gap-0.5`、卡片與標題區 padding／margin 略縮；事件點改小，與事件簿區塊視覺更平衡。
- **本機驗證**：`pnpm --filter nx-ui build` 通過

## 時間未記錄｜[SYS-DASH-PRO] 圓形 KPI、今日上班、右欄 Grid 對齊

- **完成內容**：
  - KPI：`mockProMonthlyKpi` 增 `formula`／`reverse`；`getKpiColor`、`getProKpiDisplayPercent`（`features/sys-dashboard/lib/proKpiColor.ts`）；`ProKpiCircularRow`（約 56px SVG 弧線＋中央百分比）；反向指標顯示% = 100 − 扣分占比
  - 區塊四：`ProTodayAttendancePanel`＋`mockProAttendance`；行事曆＋事件簿與今日上班 **3fr／2fr** 並列；頭像 `title` 顯示姓名＋狀態
  - 區塊五：`ProHomeBody` 改 **65fr／35fr** 雙欄，右欄 **第 1 列快捷、第 2 列任務** 同欄寬、移除 `max-w`／左邊框留白
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 PRO `/dashboard` 自行擷圖。

## 時間未記錄｜[SYS-DASH-UI] LITE／PLUS 快捷列高度修正

- **完成內容**：`DashboardQuickShortcuts` 移除預設 `h-full`（在 LITE／PLUS 的 `flex-col` 內會撐滿主區導致行事曆／任務被擠沒）；改僅在 PRO `ProHomeBody` grid 上列傳入 `h-full` 與經驗條對齊。
- **本機驗證**：`pnpm --filter nx-ui build` 通過

## 時間未記錄｜[SYS-DASH-PRO] 簽到單鍵、本月 KPI、快捷圖示、Grid 對齊

- **完成時間**：時間未記錄
- **完成內容**：
  - `ProNx10LeftPanel`：未簽到僅「簽到」橘金按鈕；簽到後顯示綠點＋計時＋簽到時間，且僅顯示「填寫工作日誌」或「✓ 今日日誌已完成」（兩階段按鈕不並列）；「今日目標」改為「本月目標」＋副標 `yearMonth`、五項 KPI（Badge＋名稱、分數、進度條；達標綠／距目標 10% 內橘金漸層／落後紅）；`失誤` 附「反向：越高越好」
  - Mock：新增 `mockProMonthlyKpi`（避免與既有 `mockMonthlyKpi` 公司營收結構同名衝突）；`mockNx10` 移除今日目標 XP 欄位
  - `DashboardQuickShortcuts`：僅 24px 圖示＋下方 `text-sm` 橘金字母；`title` 為功能名稱；LITE／PLUS／PRO 共用
  - PRO 大螢幕：`grid-cols` 13/7、`rows` auto／1fr 統一區塊一～五高度；左下列再 7/13；外層 `lg:flex` 讓主內容撐滿；`ProExpRankBar`／快捷列 `h-full` 等高
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 `/dashboard`（PRO）自行擷圖確認。

## 時間未記錄｜[SYS-DASH-PRO] PRO 首頁 NX10 版面（經驗條＋左欄簽到／目標）

- **完成時間**：時間未記錄
- **完成內容**：
  - `mockNx10` 與型別：`mocks/dashboard.ts`；今日目標列含 `targetRoute` 供點擊導頁
  - `ProExpRankBar`：圓頭像＋左下勳章角標、稱號／等級、金色漸層經驗條（`#E8A020`→`#F5C842`）、「排位」開 `MedalModal`
  - `ProNx10LeftPanel`：簽到（計時）、工作日誌按鈕（導 `/dashboard/report/daily`）、今日目標（最多 4 筆、列可導頁）
  - `SysDashboardPage`：`showNx10 = planCode === 'PRO'`；PRO 大螢幕 65/35 上列（經驗｜快捷）、下列左內 35/65（NX10｜行事曆＋事件簿）＋右欄任務清單；LITE／PLUS 維持 `LitePlusHomeBody`
  - 新增占位頁 `app/dashboard/report/daily/page.tsx`
  - 移除舊 PRO 三欄（`LeftColumnCards`／`MiddleStack`／頂部綠色 `ExpBar`）、PRO 出勤卡（此版 spec 未含）
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 `/dashboard` 將方案切為 **PRO** 後自行擷圖確認；此環境未附二進位截圖檔。

## 時間未記錄｜[SYS-DASH-UI] LITE 首頁儀表板 UI 調整

- **完成時間**：時間未記錄
- **完成內容**：
  - `CalendarCard`：`max-w-[600px]`、`mr-auto` 靠左；日期格固定 `h-11` 等高
  - `TodayEventCard`：標題改為選中日 `yyyy-MM-dd` + 兩空格 + 中文星期；卡片寬與月曆對齊（同 `max-w-[600px]`）；事件列改兩行（標題＋類型 Badge、時間範圍＋建立人）；Badge 色碼 MEETING `#378ADD`、EVENT `#E8A020`、LEAVE `#1D9E75`、DEADLINE `#E24B4A`
  - Mock：`MockCalendarEvent.creatorName`、`MockTask.status`（pending / in_progress / done）；mock 事件補建立人
  - `TodayTaskList`：移除可勾選 checkbox；改狀態圓點／半圓／綠勾；副標「系統自動偵測各模組待辦」；整列仍可導頁；`onTasksChange` 改為選用
  - `SysDashboardPage`：任務改 `useMemo` 自 `mockTasks`，不再用 state 切換完成
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：本機若已有 `next dev`（例：port 3000），請 Crown 於 `/dashboard` 將 TopBar 方案切為 **LITE** 後自行擷圖確認版面；此提交未附二進位截圖檔。

## 時間未記錄｜[SYS-DASH-UI] 快捷鍵預設與行事曆／事件列等高

- **完成時間**：時間未記錄
- **完成內容**：
  - 新增 `dashboardQuickShortcuts.ts`：Q/W/E/R/T 對應即時查詢、即時詢價、即時報價、建立銷貨單、調撥申請（路由含 `?mode=`）；`DashboardQuickShortcuts` 左欄 Lucide 圖示（Search／ClipboardList／Tag／FileText／ArrowLeftRight）
  - `/dashboard` 單鍵 Q/W/E/R/T（非輸入框內）呼叫 `router.push`；`/` 聚焦搜尋仍僅在 `/dashboard` 觸發
  - LITE／PLUS 大螢幕：左快捷列＋`CalendarCard`＋`TodayEventCard` 同一外框高 `408px`；事件列表 `overflow-y-auto`；中欄底部以 flex 留白對齊右欄高度
  - `CalendarCard`／`TodayEventCard` 支援 `fillContainerHeight`；手機版快捷改橫向 `justify-evenly` 列於行事曆上方
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 `/dashboard`（LITE）自行擷圖確認三欄對齊。

## 時間未記錄｜[SYS-DASH-UI] LITE 三區塊重構、事件簿／任務清單元件更名

- **完成時間**：時間未記錄
- **完成內容**：
  - LITE／PLUS：`DashboardQuickShortcuts` 全寬橫列（輕量邊框＋半透明底）→ 下為兩欄 grid（左：行事曆＋`EventBookCard`；右：`TaskListCard` 填滿高度、內部捲動）；移除垂直快捷列與 `dashboardLiteHeights.ts`
  - `TodayEventCard.tsx` → `EventBookCard.tsx`（標題「事件簿」＋選中日副標）；`TodayTaskList.tsx` → `TaskListCard.tsx`（標題「任務清單」、底連「查看所有任務」）
  - PRO：`MiddleStack` 改引用 `EventBookCard`；右欄仍 `TaskListCard`
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機 `/dashboard`（LITE）確認。

## 時間未記錄｜[SYS-DASH-UI] 事件簿寬對齊行事曆、快捷鍵字母可讀性

- **完成時間**：時間未記錄
- **完成內容**：
  - LITE／PLUS 主列改 `lg:flex-row`：左欄 `max-w-[600px] shrink-0` 垂直堆疊行事曆＋事件簿（同寬）；右欄任務 `flex-1 min-w-0`
  - `EventBookCard` 在填滿高度模式加 `max-w-[600px]` 與行事曆一致
  - `DashboardQuickShortcuts`：字母改 `text-sm`、`#E8A020`；圖示同色；`gap-1`；按鈕 `hover:bg-[#E8A020]/14`
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機確認。

## 時間未記錄｜[SYS-DASH-UI] 快捷純圖示、全日事件、版面方案建議（待 Crown）

- **完成時間**：時間未記錄
- **完成內容**：
  - `DashboardQuickShortcuts`：24px 圖示、下方小字 Q–T、`title` 僅功能名稱作 hover tooltip；移除長標籤；直向欄寬 `w-14`
  - `MockCalendarEvent`：`isAllDay`、`startTime`／`endTime`（可 null）；`formatEventTimeRange`：`isAllDay` 或 `time === '全天'` 或（雙欄 null 且 `time` 空）→「全天」；有 `startTime`+`endTime` →「HH:MM - HH:MM」；其餘沿用 `time` 字串解析
  - Mock filler 排假：`isAllDay: true`
- **版面空白（未實作，待 Crown 拍板）**：
  - **方案 A**：大螢幕改「上段：快捷＋行事曆＋今日事件」「下段：今日工作全寬」。改動集中在 `SysDashboardPage`（grid／flex 重排），可一併拿掉中欄刻意留的伸縮留白，資訊層次清楚。
  - **方案 B**：維持「中欄｜右欄今日工作」兩欄。若僅把右欄工作清單 `h-full`，**無法**消除中欄內 408px 列下方的空白（該段為中欄在 grid 等高拉伸多出來的區域）；要消空白仍須改中欄結構或接受工作區隨列高變矮（一般不建議）。
  - **建議**：以「改動可預期且能消除空洞」為優先，建議 Crown **採方案 A**；若 Crown 堅持右欄工作，再討論上段比例或下段分割變體。
- **本機驗證**：`pnpm --filter nx-ui build` 通過
- **截圖**：請 Crown 本機確認；此輪未附圖檔。

## 時間未記錄｜[SYS-DASH-UI] 快捷鍵 T 依方案：LITE 進貨單／PLUS+ 調撥

- **完成內容**：`getDashboardQuickShortcuts(planCode)`；LITE 的 T → `建立進貨單` `?mode=po`；PLUS／PRO 的 T → `調撥申請` `?mode=transfer`；QWER 不變。`DashboardQuickShortcuts` 與 `SysDashboardPage` 鍵盤監聽改依 `planCode`。
- **本機驗證**：`pnpm --filter nx-ui build` 通過
