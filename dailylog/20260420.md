# 2026-04-20（週一）— TASK-0420 路由清理與 v2 骨架、TASK-0421 單據公版

## 0420-A｜刪除的目錄

> 註：0420-D 已於同路徑重建 **語意化 Hub**（`/dashboard/purchase`、`inventory`、`sale`、`finance`、`report`），與本段當初刪除的舊實作目錄不同。

以下自 `apps/nx-ui/src/app/` 整包移除：

- `home/`
- `user/`
- `base/`（舊轉址層；`master-cards` 已改存 `features/base/config/master-cards.ts`）
- `dashboard/purchase/`
- `dashboard/inventory/`
- `dashboard/sales/`
- `dashboard/finance/`
- `dashboard/report/`
- `dashboard/bulletin/`
- `dashboard/nx00/`
- `dashboard/nx03/workbench/`
- `dashboard/nx03/customer-sales/`

## 0420-B｜新建的 page / layout

- `dashboard/nx02/layout.tsx`
- `dashboard/nx02/domestic|import|special|product|vendor/page.tsx`（各為佔位）
- `dashboard/nx03/layout.tsx`、`page.tsx`（轉址至 workspace）、`workspace/page.tsx`、`warehouse-setting/page.tsx`
- `dashboard/nx04/layout.tsx`、`domestic|export|customer/page.tsx`（**無**根層 `page.tsx`，避免與舊書籤 redirect 衝突）
- `dashboard/nx05~nx10/workspace/page.tsx`（佔位）
- 共用元件 `features/layout/ui/NxWorkspacePlaceholder.tsx`

## 0420-C｜redirect

- `apps/nx-ui/next.config.ts` 的 `redirects()` 共 **35** 條（含 `/base`、`/user`、舊六大中心、舊 nx00、舊 nx03 workbench、財務／報表子路徑、舊 nx04 根路徑等）。
- 已移除舊 `rewrites()`（`/dashboard/base` → `/base`）及舊 `/dashboard/nx02` → inventory 的 redirect。

## Build

- `cd apps/nx-ui; pnpm build`：**成功**（Next 16.1.6 / Turbopack）。

## 其他調整摘要

- Top 模組 Tab、Dock、側邊選單設定、`menu.nx01~04`、`nx01` 根轉址、各 Hub／主檔內連結、`/base` 路徑判斷等，已改為 v2（nx02 採購工作台、nx03 庫存、nx04 銷售、nx05 財務、nx08 報表占位等）。

## 0420-E｜採購產品管理頁（mock）

- **路由**：`/dashboard/purchase/product`（`NX02-PROD-UI-001-F01`）
- **實作**：`features/purchase/product/mock-data.ts`（`MOCK_PRODUCTS`、`CODE_RULES` 動態 SEG、`COUNTRY_OPTIONS`、R/S/C/B 關聯 mock）、`PurchaseProductManagementView.tsx`
- **詳細**：料號建立（廠牌前綴提示、編碼規則獨立下拉、SEG 依規則顯示／停用段、產地下拉、即時料號預覽）、產品資料（副廠料號、零件類型 A~D、規格、單位、OEM／副廠、車型 tag）、品質政策（保固、退貨 F/S/R/N/W）；**有交易記錄**時 SEG 鎖定並提示改號；瀏覽列顯示規則／產地／退貨政策等。
- **關聯**：四 Tab — **可替代 (R)**、**改號紀錄 (S)**、**改版換周邊 (C)**、**組合／拆解 (B/F)**（對應 `relation_type` 說明）；各 Tab 可展開 DEMO 新增區塊。
- **快捷鍵**：`Alt+A` 新增產品（展開關聯新增時不觸發）、`Alt+E` 詳細編輯、`Shift+Alt+E` 編輯定價（與 `Alt+E` 分離）、`Alt+S` 儲存、`Esc` 取消；清單 `↑↓` `Enter`；SEG **Backspace** 於空欄回上一段。
- **採購中心**：「產品管理」卡片連結此路由。
- **Build**：`pnpm build` 成功。

## 0420-D｜五大中心卡片 Hub（D1~D5）

- **共用**：`features/layout/ui/module-hub/hub-primitives.tsx`（`ModuleHubSection`、`HubLinkCard`、`HubStepBadge` 整數／半步、`HubProBadge`）、`/coming-soon` 占位頁。
- **路由與頁面**
  - `/dashboard/purchase` — `NX02-HUB-UI-001-F01`
  - `/dashboard/sale` — `NX04-HUB-UI-001-F01`（`/dashboard/sales` 301 → `sale`）
  - `/dashboard/inventory` — `NX03-HUB-UI-001-F01`（`/dashboard/inventory/workspace` 等仍 redirect 至 nx03）
  - `/dashboard/finance` — `NX05-HUB-UI-001-F01`（`/dashboard/finance/*` 子路徑仍 redirect 至 nx05）
  - `/dashboard/report` — `NX08-HUB-UI-001-F01`（全卡 PRO 鎖＋點擊 alert；子路徑仍 redirect 至 nx08）
- **導覽**：`modules.ts`、Dock、`TopModuleTabs`、`side-menu`（五中心與主檔／採購 hub 一樣不顯示 SubNav）、`ModuleMenuOverlay`（P 採購改指 `/dashboard/purchase`）。
- **Git**：`[TASK-0420-D1]` … `[TASK-0420-D5]` 五筆 commit。
- **Build**：`cd apps/nx-ui; pnpm build` 成功（Next 16.1.6）。

## 0420-F｜供應商管理頁（mock）

- **路由**：`/dashboard/purchase/vendor`（`NX02-VEND-UI-001-F01`）
- **實作**：`features/purchase/vendor/mock-data.ts`、`PurchaseVendorManagementView.tsx`（左欄 320px 清單＋篩選；右欄詳細／採購記錄／評鑑與談判 PRO 鎖）
- **採購中心**：「供應商管理」卡片連結此路由。
- **Build**：`cd apps/nx-ui; pnpm build` 成功（Next 16.1.6）。

## 0420-G｜國內採購工作台（mock）

- **路由**：`/dashboard/purchase/domestic`（`NX02-PO-UI-001-F01`）
- **實作**：`features/purchase/domestic/mock-data.ts`（12 筆 DR mock、`buildRfqSplitPreview`）、`PurchaseDomesticWorkbenchView.tsx`（左 160px 流程節點／中 flex 需求清單／右 300px 待詢價；詢價確認後切節點占位）
- **採購中心**：國內採購「需求 STEP 1」卡片連結此路由。
- **Build**：`cd apps/nx-ui; pnpm build` 成功（Next 16.1.6）。
- **版型對齊規格**：左欄 **160px** 垂直流程（金／綠／灰圓點＋連線＋橘 badge）；中欄 **篩選四鈕＋搜尋**、**每頁 5～6 筆（可調）**、**底部分頁**；需求卡 **四欄 grid**（勾選視覺｜料號資訊｜庫存條＋缺口｜需求量）、**整卡點擊**勾選、**緊急**左紅邊＋紅「緊急」badge；右欄 **300px**、待詢價 **× 數量**（可編輯詢價量、重設補滿）。
- **可讀性**：字級／按鈕／庫存條加粗加高；列表 **每頁 5 筆**＋中欄列表可 **overflow 捲動**；右欄約 **19～22rem** 寬。

## 0420-H｜採購需求節點＋詢價節點（TASK-0420-H）

- **路由**：`/dashboard/purchase/domestic`（`NX02-PO-UI-001-F01`）
- **H1 需求節點**：卡片中段 **周轉月數** mock（`turnoverMonths`／`turnoverMonthsLabel`／`turnoverMonthsTone`）；右欄改為 **共 N 筆待處理**＋**前往詢價節點**（切 `rfq`，等同 `Alt+2`）；卡片 **移除勾選**與金色勾選高亮，僅檢視與鍵盤焦點。
- **H2 詢價節點**：`PurchaseDomesticRfqFormView.tsx` — 表頭（廠商 `MOCK_VENDORS`、代理廠牌、回覆日、備註）、主檔式明細表（工具列帶入 Modal、手動新增、列 checkbox、鍵盤 `↑↓`／`Ctrl+↑↓`／`Enter`／`Tab`／`Esc`／`Delete`／`Alt+A`／`Alt+S`）、底部固定按鈕（取消確認、儲存草稿顯示「草稿」標、送出條件）。
- **Build**：`pnpm --filter nx-ui build` **成功**（Next 16.1.6）。

## 0420-I｜採購工作台三項（TASK-0420-I）

- **路由**：`/dashboard/purchase/domestic`（`NX02-PO-UI-001-F01`）
- **I1 需求節點**：移除右欄，主區全寬；標題列「採購需求　篩選 N 筆」+「前往詢價節點 →」；需求改 **單列 grid**（`1fr 60 60 60 220 80 80`）+ 業務列第二行展開；周轉／現存色碼 **#1D9E75 / #E8A020 / #E24B4A**；緊急 **2px 紅左框**；分頁 **12 筆/頁**。
- **I2 詢價節點**：`PurchaseDomesticRfqNodeView.tsx` — 預設 **詢價單列表**（`MOCK_RFQS_INITIAL` + 篩選／搜尋／主檔式表格／過期紅字／狀態 badge）；`[+ 新增詢價單]`、`Alt+A`（僅列表）進入表單；點列進 **詳情占位**；左欄流程 **詢價 badge** 與列表筆數同步。
- **I3 新增表單**：`PurchaseDomesticRfqFormView.tsx` — 唯讀系統列；表頭對應 **supplier / contact / rfq_date / valid_until（詢價日+5 可改）/ warehouse / currency / remark**；廠商帶入聯絡人電話與代理廠牌；明細 **項次、單價、交期、行狀態、詢價原因**（需求帶入列鎖原因）；底部摘要與送出條件；送出寫入列表並 **高亮** 新單。
- **mock-data**：`MockVendor` 聯絡人欄、`MOCK_WAREHOUSES`、`MOCK_RFQ_CURRENCIES`、`MOCK_RFQS_INITIAL`、`nextRfqDocNo`、`MOCK_RFQ_CREATOR_NAME`、`demandGapToSafety`、`turnoverMonthsColorHex`、`turnoverMonthsShortText`。
- **Workbench**：`Alt+A` 僅在 **需求節點** 開「手動新增需求」；詢價節點不佔右欄。
- **Build**：`pnpm --filter nx-ui build` **成功**。

---

## TASK-0421｜單據公版 Component + DEMO 五單據（同日完成，日誌歸本日）

### 摘要

- **單據公版**：`apps/nx-ui/src/components/document/` 共用元件（列表／明細切換、表頭、明細表鍵盤、狀態 badge）。
- **DEMO**：詢價 RF、採購 PO、進貨 RR、報價 QT、銷貨 SO 五頁（mock 多狀態）+ 語意化路由。
- **導覽**：採購／銷售 Hub 補連結；星球 Dock 採購／銷貨／庫存改 **子選單**；`next.config` 短網址 redirect。
- **Git**：`12748d0` — `[TASK-0421] Add document shell components, five demo doc pages, dock submenus, and purchase/sale shortcuts`
- **Build**：`pnpm --filter nx-ui build` **成功**（Next 16.1.6 / Turbopack）。

### 0421-A｜單據公版 Component（`NX00-DOC-UI-001-F01`）

| 檔案 | 說明 |
|------|------|
| `DocLayout.tsx` | 標題、`docCode`、**Alt+1／Alt+2**、工具列（**Alt+A／E／S／D／P**、**Esc**）、刪除確認 Dialog；可受控 `view` |
| `DocListView.tsx` | 列表區外框（`nx-master-scroll`，對齊主檔表格） |
| `DocDetailView.tsx` | `header` + `itemToolbar` + 主內容 + `footer` |
| `DocHeader.tsx` | 系統列 + 兩欄表頭 grid |
| `DocItemTable.tsx` | 明細鍵盤與列選取樣式；`renderCell`、`inputClassName`、`disableRowDelete` |
| `docStatus.tsx` | 狀態 badge（**`.tsx`**，含 JSX） |
| `index.ts` | barrel 匯出 |

### 0421-B～F｜五張 DEMO 單據

**共用 mock**：`features/document-demo/mockData.ts`（`MOCK_RFQ_LIST`～`MOCK_SO_LIST`、`MOCK_CUSTOMERS`、`MOCK_LOCATIONS` 等）。

| 單據 | FUNCTION_CODE | 路由 |
|------|-----------------|------|
| RF | `NX02-RFQ-UI-001-F01` | `/dashboard/purchase/rfq` |
| PO | `NX02-PO-UI-002-F01` | `/dashboard/purchase/po` |
| RR | `NX02-RR-UI-001-F01` | `/dashboard/purchase/rr` |
| QT | `NX04-QT-UI-001-F01` | `/dashboard/sale/qt` |
| SO | `NX04-SO-UI-001-F01` | `/dashboard/sale/so` |

**行為**：PO 小計／稅／總額即時算；RR 草稿可過帳後鎖定；QT 低於最低售價紅字、已發出顯示轉銷貨占位；SO 配送地址必填、已備貨列不可刪。

### 0421-G｜路由與 Hub

- **redirect**：`/purchase/rfq|po|rr`、`/sale/qt|so` → 對應 `dashboard` 路徑（`next.config.ts`）。
- **Hub**：`purchase/page.tsx`（詢價／採購／進貨）；`sale/page.tsx`（報價／銷貨／客戶 `nx04/customer`）。

### 0421-H｜DOCK 子選單（`dock.tsx`）

採購／銷貨／庫存改 `DropdownMenuSub`（`DockGridLink`、`DockSubLink`）；底欄說明含子選單提示。

---

## TASK-0422｜NX01 主檔與 Prisma／nx-api／nx-ui 對照與補齊

### 摘要

- **規格矩陣**：新增 `docs/spec/nx01_master_field_matrix.md`（NX01 模型 ↔ DTO／service ↔ 前端型別／表單；刻意排除欄位；後續主檔如 DiscountCode／Department／Team 等標註延後）。
- **Part**：擴充 `part.dto`／`part.service` 與前端 `Part` 型別、`BasePartMasterView`、mock（含 segs、partType、returnPolicy、warrantyMonths、priceA～D、價格異動稽核欄位等）；清單欄位 pref 版本上調。
- **Partner**：擴充 `partner.dto`／`partner.service`（creditLimit、paymentTermImport、incoterm、customerGrade join；重複 code 以 **tenant** 範圍檢查）；前端 `BasePartnerMasterView`、`PartnerFormPanel`（PATCH 時剝除 `code`）。
- **新 API**：`GET /nx01/customer-grades`、`GET /nx01/warehouse-types`（controller／service，`nx01.module` 註冊）；前端 `customer-grade.ts`、partner 型別與 API 對齊。
- **導覽與主檔 Hub**：`menu.nx00.ts`、`master-cards.ts` 與五大中心一致；公告改掛 **`/dashboard/base/bulletins`**（`bulletin.ts` API client）。
- **前端 API 路徑**：base／nx00 相關 client 統一 **`/nx01/*`**、查詢參數 **`search`**、回應 **`rows` → `items`**、更新 **`PATCH`**（users、roles、warehouses、partners、parts、part-brands 等）。
- **UI 規格文件**：`docs/ui/*_WORKSPACE.md`、`SYS_DASHBOARD.md`、`SYS_LAYOUT.md` 等路由敘述改為 **`/dashboard/purchase/*`** 等 v2 語意化路徑，並更新「最後更新」日期。

### Build／型別

- `pnpm --filter nx-api exec tsc --noEmit`：**通過**。
- `pnpm --filter nx-ui exec tsc --noEmit`：曾因 **`.next/types` 舊路由** 造成誤報；需清 `.next` 後再驗證實際 `src` 錯誤。

### Git

- 本節對應變更將以 **`[TASK-0422]`** 單筆 commit 推送至 `origin/main`（含上列檔案與日誌）。

---

## 問題／後續

- 舊「採購／銷貨中心」完整 Hub（`ProcurementFlowHub`、`SalesFlowHub` 等）路由已刪，實作需之後掛回 `nx02/domestic`、`nx04/domestic`。
- `next.config` 中 `/dashboard/nx04` → `nx05/workspace`（`permanent: false`）為舊書籤過渡；正式銷售作業仍為 `/dashboard/nx04/domestic`，模組 Tab 預設進 `/dashboard/sale` 卡片中心。
- **TASK-0421**：舊路徑 **`/dashboard/nx01/rfq`** 等若需統一轉到 **`/dashboard/purchase/rfq`**，可另開 task 在 `next.config` 補 redirect；五單據仍為 mock，接 API 後可沿用公版。
