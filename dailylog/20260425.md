<!-- dailylog/20260425.md -->
# 2026-04-25（週六）— Phase 0 schema + translator + 多項 schema patch 一日推完

> 分支：`main` （HEAD `8b83a2d`，含早上 v1.1 plan）+ `feature/wp-phase0-schema`（HEAD `c80dee2`，後端開發主軸）
> 跨分支推進：`75a7bc1` → `c80dee2` 共 **19 個 commit**（main 5 + feature 14）
> 工作時段：早上 ~ 傍晚連續推 Phase 0 後端架構

---

## 0425-A｜今日工作六段式

```
段 1（早上）：plan v1.1 拍板進 main
  └─ 8b83a2d  [DOC] update workstation pivot master plan to v1.1 (integrate review)
              整合 Hank D3 v1 review B-01（schema 失準）+ 18 項 review 全部接受
              Phase 2 規模從 XL → XXL 可拆 2a/2b 平行
              Phase 0 新增 B5 RFQ/QT API
              第 4 章 Pilot 整章重寫

段 2（上午）：docs 重整（C 方案 — 按 NX 模組分）
  ├─ 1cbea65  [CHORE] docs restructure step 1/3: add README + remove obsolete flows index
  ├─ a9d5d61  [CHORE] docs restructure step 2/3: migrate 107 files to NX-module structure
  ├─ 9007805  [CHORE] docs restructure step 3/3: add D3 intent v1 (Alex's parallel artifact)
  └─ 950bf32  [DOC] update CLAUDE.md doc paths after docs/ restructure
  
  108 個檔案中：107 個 git mv（rename detection 全 100% 保留 blame）
  + 1 個 git rm（廢 flows/README.md）+ 2 新建（docs/README.md、D3 intent v1）
  全部一次 push、無「半遷移」中間態

段 3（下午前段）：Phase 0 D3 schema 落地
  ├─ 78e24a1  [WP-PHASE0-SCHEMA] add D3 impl + trigger specs (await Alex review)
  └─ afa84d0  [WP-PHASE0-SCHEMA] apply D3 schema + 4 triggers (C-strategy two-stage migration)
  
  - Nx04SoItem 加 4 欄（transferSourceType/transferStatus/fulfillStatus/coId）+ 5 indexes
  - 新建 Nx04Co model + sourceSoItemId 反向追蹤
  - 4 個 trigger：reserved_qty 自動同步 / 防直寫 / itemStatus 雙寫 / source_type deprecation
  - C 方案兩階段 ALTER（拒絕 sentinel），phase0_so_data_model + phase0_so_data_model_tighten

段 4（下午中段）：D4 SYS-C Translator 從意圖到落地
  ├─ 3a2b8ba  [WP-PHASE0-D4] add D4 translator intent + impl specs (await Alex review)
  ├─ f9ef049  [WP-PHASE0-D4] amend D4-impl spec: trigger coupling warning + lock_timeout WARN
  ├─ 3543aff  [WP-PHASE0-D4] add vitest infra to nx-api (for D4 translator tests)
  ├─ a16a755  [WP-PHASE0-D4] D3 patch: drop NOT NULL on st_item.source_so_item_id
  ├─ 2656dab  [WP-PHASE0-D4] add D4 translator service + DTO + Controller + Exception filter
  └─ 46823bb  [WP-PHASE0-D4] add D4 translator unit + integration tests
  
  - 4 class 分層：Translator + TransferSourceResolver + RefreshmentDocCreator + AdvisoryLock
  - retry 含 P2034/40P01/55P03，exponential backoff 50/200/800ms
  - 26 unit tests 全綠 + 6 integration tests gated by INTEGRATION_DB=1
  - Lock timeout 觸發走 WARN log（per Crown Q5 add-on）

段 5（下午後段）：D5 navigation policy + B5 schema patch
  ├─ 001cfb3  [WP-PHASE0-D5] add D5 intent + D3 §2.1 v1.1 patch + README decision tree
  └─ c80dee2  [WP-PHASE0-B5] D3 patch: add nx02_qt table for B5 RFQ/QT separation
  
  - D5：navigation context policy 4 條原則（consume+clear / fallback / discriminated-union+version / same-tab）
  - 新建 docs/_shared/spec/intent/ 子目錄（跨模組設計契約專屬）
  - B5：意圖 v1 描述「1 RFQ : N QT (N家供應商)」跟既有 schema 衝突 → 新建 nx02_qt 獨立表
  - nx-table.csv 補 entry（用 43.5 序號避免重編 nx03~nx10）

段 6（傍晚）：今日收尾（本 dailylog + env example 檔）
```

---

## 0425-B｜今日重大決策

### B-1 團隊分工正式定義（重大組織變動）
```
Crown = 決策者（提需求、最終拍板）
Alex  = 整合者（彙整需求、寫意圖文件，不寫具體 schema/SQL/Prisma DSL）
Hank  = 執行者（看 repo 全貌、對照真實狀態實作 + 寫具體細節）
```
過去模式：Alex 寫 spec → Hank 照寫
新模式：Alex 寫意圖（要什麼結果）→ Hank 對照真實 codebase 寫具體 spec + 實作

### B-2 D3 v1 失準事件（重要的方法論校正）
Alex 沒讀真實 schema 就閉門造車寫了 1059 行 schema spec，
Hank review 抓出 19 個問題（B-01 整份失準、4 個 blocker、5 個 concern）。
→ 改寫意圖版（233 行純意圖、無 schema 細節）+ Alex checklist 追加「先 grep 既有結構」。

### B-3 docs 重整採 C 方案（按 NX 模組分）
A/B/C 三案對焦：A=按類型 / B=按 lifecycle / C=按業務模組。
Crown 拍 C — 跟 apps/nx-api/src/nx0X/ 程式碼結構 1:1 對齊、跨資料夾引用最少。
108 檔重組無痛完成，rename detection 全 100% 保留 blame。

### B-4 schema 演化方法論：兩階段 ALTER（拒絕 sentinel hack）
D3 source_so_item_id NOT NULL 一開始走 sentinel default `_PENDING_BACKFILL_`，
shadow DB validation 直接炸（19 字元 > VARCHAR(15)）。
Alex/Hank 對焦後採 C 方案：ADD COLUMN nullable + 配套 tighten migration。
寫進 spec：「prod backfill migration 在 100000 跟 100100 之間插」。

### B-5 D3 patch A3（純 nullable）
nx03_st_item.source_so_item_id NOT NULL 跟既有 transfer.service.ts（手動調撥）衝突。
Step 1 grep 後發現「triggerSource 在 header 不在 line item，跨表 CHECK 不可行」。
Alex 承認 SQL 細節盲點、改採 A3（純 nullable，application-layer 自律）。
nx02_ti_item.source_so_item_id 維持 NOT NULL（業務語意上 SO 觸發才合法）。

### B-6 B5 採方案 B（新建 nx02_qt 獨立表）
意圖 v1「1 RFQ 對 3 家同行各 1 QT」跟既有 schema「1 RFQ : 1 supplier」衝突。
不修意圖、不擠進 RfqItem、不打掉既有 schema — 新建 nx02_qt 表，
精確映射意圖；既有 nx02_rfq / nx02_rfq_item 結構不動。

### B-7 業務 muscle memory 拍板（B5 Q1~Q5）
- Q1：同行報價數量 < RFQ 要求 → 部分採用算 completed，業務自己處理缺額
- Q2：QT 採用後不能反悔 → 走 TI 取消單流程（屬 NX02 範疇）
- Q3：RFQ 逾期機制 → Phase 0 不做
- Q4：歷史 archive → Phase 0 不做
- Q5：access control → list 開放、寫入限 PURCHASE_ADMIN
- B5 unique constraint 移除：保留同 partner 多次報價歷史（採用時舊 pending 標 rejected）

---

## 0425-C｜明日預計工作

1. **Alex 重寫 B5 意圖版**對齊新 nx02_qt schema
   - §3.2 「對某 RFQ 新增 QT」邏輯改寫成「insert 新 QT row、舊 QT 維持」
   - 補一段：採用 QT 時同 partner 較舊 pending QT 全部標 rejected
2. **Hank 寫 B5-impl spec**（5 個 API endpoint：list / addQt / adoptQt / rejectQt / cancelRfq）
3. **Alex review** → Hank 寫 service + tests
4. B5 push 後 → **Phase 0 後端 API 主軸完成**

剩餘 Phase 0 工作（按 plan v1.1 倒數）：
- B2 反查 API（committed_stock 反查）
- B5 落地（明天）
- 然後進 Phase 1 W2-mini

---

## 0425-D｜架構債更新

- 無新增
- A003 / A004 / A015 仍 pending（春酒後處理）
- A016 已處理（D3 schema drift 收斂、B-01 消滅）

---

## 0425-E｜教訓記錄

### E-1 Alex 兩次「沒讀既有 schema 就寫意圖」失誤
- D3 v1：1059 行 schema spec 整份失準（表名/欄位/ID prefix/模組劃分全錯）
- B5 v1：「1 RFQ : N supplier」跟既有 schema 「1 RFQ : 1 supplier」結構衝突
- → Alex checklist 追加：**意圖版前必先 grep 既有結構或 search project_knowledge**

### E-2 「present_files 給 Crown 下載、Hank 看不到」
- D3 意圖版第一次貼：Crown 寫「present_files」但 Hank 環境沒收到
- D3 §2.1 兩段 patch：Crown 寫「已給你」但實際沒貼進訊息
- D5 意圖版上一輪訊息已收到本輪沒重貼，需確認
- env example 模板今天結尾又一次截斷
- → 學到：Crown 要傳檔給 Hank **直接貼訊息內容**，不要透過 present_files

### E-3 破壞性指令協議救了至少 2 次
- D3 補丁前 grep 發現 schema vs caller 衝突 → 沒順手跑 migrate dev、停下回報 Alex 改 A3
- B5 sentinel 衝突前 dry-run 發現「跨表 CHECK 寫不出來」→ 沒順手做、停下對焦 Alex 後採方案 B
- 兩次都因為「破壞性指令前停下」+「停下後才 grep + 發現問題」 → 規矩值得

### E-4 Background prisma migrate dev 在 Windows 上 stdout 被 buffer
- D3 patch + B5 schema 兩次 migrate dev 在 background 跑，output file 一直 0 bytes
- 但 `prisma migrate status` 確認 DB 端已套用 — 命令確實成功只是 console 輸出被 cache
- 解法：直接用 `migrate status` 確認結果，不依賴 background output
- 工程啟示：Windows + Prisma + bash background 組合的已知怪行為，記得避開或繞過

### E-5 工程辯論（Hank 推 A vs Alex 推 C）的成果
- 兩階段 ALTER 案早上對焦過一次：Alex 提理論性原則（不留 sentinel）、Hank 反駁工程細節（dev 場景同等）
- 最後 Hank 被說服改投 C — 因為 Alex 的「未來不污染 DB」論點工程上的代價（4 行 ALTER）夠小
- 學到：好的工程辯論價值不在「誰贏」而在「對焦後雙方都看到對方沒看到的角度」

---

## 0425-F｜累計 commit 統計

```
main 分支（5 個 commit）：
  8b83a2d  plan v1.1 拍板
  1cbea65  docs C1
  a9d5d61  docs C2
  9007805  docs C3
  950bf32  docs C4 (CLAUDE.md)

feature/wp-phase0-schema 分支（14 個 commit）：
  78e24a1  D3 spec
  afa84d0  D3 schema 落地
  3a2b8ba  D4 spec
  f9ef049  D4 spec amend
  3543aff  vitest infra
  a16a755  D3 patch
  2656dab  D4 service code
  46823bb  D4 tests
  001cfb3  D5 intent + D3 §2.1 + README
  c80dee2  B5 nx02_qt schema

跨分支累計：19 個 commit、約 +75k / -68k 行（generated/prisma 大宗）
```

---

## 0425-G｜環境設定

- Office DB（Docker port 5433）已套用 21 個 migrations
- nexora_core schema 跟 schema.prisma 一致
- LITE/PLUS/PRO 三租戶 seed 完整、業務交易表（SO/SO_ITEM/Stock_Balance）為空（待 TASK-SEED-DEMO-02）
- Home 機 setup：Crown 回家後依 .env.example 設環境、跑 `pnpm prisma migrate dev` 即可

---

*工作日誌結束*
